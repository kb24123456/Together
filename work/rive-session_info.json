{
  "name": "session_info",
  "description": "List the files open in the editor and which tab is active. Read-only. Rive files live in the cloud, so there is no path on disk; use the returned url to refer to a file.",
  "inputSchema": {
    "type": "object",
    "properties": {}
  },
  "annotations": {
    "title": "Session info",
    "readOnlyHint": true,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  },
  "execution": {
    "taskSupport": "forbidden"
  }
}