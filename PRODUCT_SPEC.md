# PRODUCT_SPEC.md

## 1. 文档职责
- 本文件是项目「一起 / Together」的唯一产品规格主文档。
- 负责统一：产品定位、MVP 范围、信息架构、页面需求、版本路线。
- 不负责：工程实现细则、Codex 执行规则、视觉与动效设计细则。

## 2. 一句话定位
- 一款纯单人的 iPhone Todo 效率工具，帮助用户把日常待办、项目推进和纸面笔记快速整理成可执行任务。

## 3. 当前产品结论

### 事实
- 旧逻辑曾把产品定义为双人生活协作工具，单人更像试用态。
- 后续迭代又将主轴切到单人 Todo，但仍在文档、代码和后端里保留双人协作、Supabase、RevenueCat 与多人预留。
- 当前方向已重新收敛：彻底做成纯单人 Todo 效率工具。

### 结论
- 当前版本只服务单人任务管理，不再承诺双人协作、多人空间、情侣关系运营或订阅会员能力。
- `PairSpace / Invite / BindingState / relationshipID / TaskMessage / partner` 等双人语义属于旧实现，已从当前运行时删除，不再兼容扩展。
- Supabase 不再作为后端、恢复源、同步源、头像存储、邀请服务、推送触发或付费 webhook 承载方。
- RevenueCat、Paywall、Pro gate、premium entitlement、订阅恢复等能力不再保留。
- 跨设备同步与恢复只依赖 Apple 体系：本地 SwiftData + CloudKit private database。
- OCR 是当前产品新增主能力：用户拍摄纸面笔记后，先生成任务草稿，再由用户确认写入。

## 4. 目标用户
- 事务密度较高、需要持续处理工作待办和项目推进的个人用户。
- 经常把纸质笔记、会议记录、手写清单转成数字待办的 iPhone 用户。
- 对信息层级、交互流畅度、隐私和原生体验有较高要求的 Apple 用户。

## 5. 核心场景
- 每天进入首页查看全部未归档任务，快速判断现在要做什么。
- 快速收集临时任务，必要时用子任务承接多步骤事项。
- 回看过去创建或截止的任务，并提前看到未来需要处理的任务。
- 对着纸质笔记、手写清单或白板拍照，识别文字并生成可编辑的任务草稿。
- 在 iPhone 与同一 iCloud 账号的 Apple 设备之间恢复自己的数据。

## 6. 核心价值主张
- 不只是记录待办，而是帮助用户更快进入“今天先做什么”的执行状态。
- 所有核心数据默认属于用户本人，不依赖第三方后端账户或订阅状态。
- OCR 导入降低从纸面笔记到可执行任务的整理成本。
- 通过克制、丝滑、原生的 iOS 交互体验形成差异化。

## 7. 当前 MVP 范围

### 一级结构
- 待办：首页默认工作模式，展示全部未归档的非周期任务。
- 定期：与待办同级的工作模式，展示按固定节奏重复出现的任务。
- Profile：通过顶部头像进入个人资料与设置。
- OCR：通过首页系统底部 toolbar 的原生 `Menu` 进入扫描导入。

### 全局入口
- 上下文创建：待办页右下角 `+` 创建待办，定期页创建定期任务；点击后立即分配最终 UUID，但保存前只建立 UI 内存草稿。待办始终回到今天并在今天未完成分组末尾插入空任务行，定期任务在当前周期未完成区域末尾、已完成任务之前插入空任务行；稳定 Draft 视图立即挂载，其真实占位高度在约 `220ms` 内从零平滑展开，使下方日期标题与后续任务随 `LazyVStack` 自然让位，可见内容同时以约 `160ms` 无位移、无缩放的短淡入首次显现。紧凑行仍预留其完整详情展开高度并激活标题输入。约 `140ms` 后只对稳定 Draft ID 执行一次约 `220ms` 的平滑底部预定位，并在列表开始上移的同一时刻启动现有详情逐行展开；预留高度随展开进度等量收回，使标题、属性与“取消 / 添加”整体保持在键盘安全视口上方约 `10pt`，不再等待键盘稳定后做第二次滚动。编辑中修改日期或周期不移动草稿，提交收起后才原地替换或迁移到最终分组；界面不提供“模板 / 待办 / 定期”类型切换。
- OCR 扫描导入
- 排序 / 日期分组
- 首页任务行在列表真实槽位内原地展开并编辑

### 核心能力
- Task：创建、编辑、完成、重新打开、提醒、排序。未完成普通任务始终拥有计划日期；新建默认归入本地今天，编辑器不提供清除日期。历史 `dueAt == nil` 未完成普通任务幂等归一化到迁移当日，且不补写生命周期事件；已完成旧记录保持原样，避免改写历史统计。
- Task Lifecycle Review：记录普通待办从正式创建到最终完成的计划变化，提供单任务履历、完成任务回顾和周/月计划复盘；定期任务不纳入首版。
- Legacy List Compatibility：保留既有清单字段与持久化模型用于历史数据兼容；当前 MVP 不提供独立清单页面或导航入口。
- Task：所有新建事项都落到同一种任务；多步骤事项使用子任务承接。
- OCR Import：拍照/扫描、文字识别、结构化草稿、用户确认后入库。
- Profile：个人资料、通知、主题、默认首页、隐私与数据设置。
- Sync/Restore：SwiftData 本地存储 + CloudKit private database。
- Widget：统一提供主屏小、中、大号“今日概览”；今日范围只包含逾期与今天到期任务，未来任务仅可作为独立“下一项”预览。小号展示珊瑚橙待办数、按 Today 当前排序排列的最近 1 条任务和 baby blue 环形完成进度，任务文字进入详情，不提供完成或新建操作；中号最多展示两条可完成任务；大号按实际容器高度自适应展示逾期与今日分区。三种尺寸均使用系统默认 Widget 背景与内容边距。
- Legacy Project Migration：历史 Project 在运行时一次性迁移为普通 Task；legacy model 暂留一版作为迁移来源，不再有新写入入口。

### 当前明确不做
- 不做双人绑定、邀请、共享清单、共享项目、共享任务。
- 不做多人 Space、团队协作、工作组权限或共享数据库。
- 不做 Supabase 后端、Edge Function、RLS、Realtime、Storage、远端恢复。
- 不做 RevenueCat、订阅会员、Paywall、Pro quota 或付费 gate。
- 不做共同决策、情侣纪念日、关系运营或社交内容流。
- 不让 OCR 结果绕过用户确认直接写入真实任务。
- 不提供任务模板；模板界面、业务逻辑、仓库、运行时持久化模型和旧记录全部删除。历史 V1 schema 仅作迁移输入保留。
- 不做只为了视觉好看却不提升效率的复杂交互。

## 8. 信息架构原则
- Task 是 IA 核心对象；项目不再是产品级对象，旧项目迁移为带子任务的普通任务。
- 用户只感知“我的任务”，不感知 Space、Pair、成员、协作者或权限模型。
- OCR 入口是任务收集方式，不是独立内容库。
- OCR 识别出的标题 + 子项结构落为一条 Task + TaskSubtask，不再生成 Project。
- 旧 `Decisions / Anniversaries / Pairing / Paywall` 模块不再代表当前信息架构。

## 9. 页面规格

### 9.1 待办首页

#### 页面目标
- 用户每天最高频打开的入口。
- 让用户在一个连续任务区里查看全部未归档任务，兼顾当前执行、过去回看和未来准备。

#### 必备模块
- 顶部 toolbar：约 `44pt` 的单一主控行，中间在 `ToolbarItem(.principal)` 内使用两个无外壳 `Button` 切换“待办 / 定期”，右侧完整圆形头像进入 Profile。切换区保持 `144 × 44pt`，两项各占 `72 × 44pt`；当前项使用约 `21.5pt Bold` 主文字色，非当前项使用约 `17pt Regular` 三级文字色。切换时两项中心保持固定，约 `260ms` 内通过同位置双层文字交叉淡变，将字号、字重和颜色同步形变，不产生位移或过冲；按压项缩放至 `0.94`、透明度降至约 `0.82`，松手约 `180ms` 无过冲恢复。不显示轨道、胶囊、玻璃、底部横线、下划线或移动指示物。辅助功能大字号下使用显示当前模式的原生 `Menu`；Reduce Motion 下取消文字缩放与按压缩放，只保留约 `160ms` 的文字透明度和页面淡变。圆形头像视觉尺寸约 `40pt`、命中区不小于 `44pt`；iOS 26+ 隐藏 Toolbar 共享背景。
- 全 App 浅色一级画布统一使用系统纯白，不保留可见冷灰渐变；深色一级画布维持当前中性渐变。待办与定期共用同一个根级动态背景实例，模式切换不重置粒子相位。其上仅允许低密度、低对比度、中性色的向上粒子流；浅色以约 `12%` 活跃单元和约 `0.5...1.1pt` 主半径形成更少但更清晰的冷银灰点，并以真机观感对齐深色模式的粒子显著度；深色粒子参数保持不变。动态粒子不得扩展到 Profile、历史、OCR、Sheet、Widget 或实时活动，粒子运动与列表滚动完全解耦，不读取或写入滚动状态。
- 系统底部 toolbar：左侧使用原生 OCR `Menu`，右侧使用原生 `Button` 新建当前模式任务，两者由 `.bottomBar` 与 `ToolbarSpacer` 排列；安全区、Liquid Glass、命中反馈和容器布局交给系统，不再维护自定义 Dock、圆形材质或 `safeAreaInset`。完整已完成归档统一从 Profile“日志”进入。
- 统一任务流 / 时间线
- 首页只内联展示今天已完成的少量最近记录；本周更早的已完成任务通过 sheet 快速回看，不进入首页主任务流。该 sheet 顶部“查看全部”为无玻璃外壳的文字操作，任务列表顶部使用系统 `.scrollEdgeEffectStyle(.soft, for: .top)`。
- 完整已完成任务通过“已完成”归档页查看：顶部原生标题显示“已完成”和当前范围副标题；顶部快捷菜单支持本周、本月、全部；底部 toolbar 使用系统原生搜索项支持标题、备注、子任务搜索，并在同一 toolbar 放置精确筛选按钮；精确筛选使用原生 sheet，支持具体月份和具体日期；列表分页加载。
- 任务完成、重新打开、列表内原地详情展开
- 子任务内联新增、编辑、勾选与删除
- 系统底部 toolbar：OCR 与当前模式的新建任务
- 任务流只按计划日期分组；普通任务不存在“未排期”或按创建日期生成的分组。
- 首页存在逾期任务时，在同一个顶部 `safeAreaBar` 中按“当前已逾期 N → 日期栏”顺序显示入口，使系统 `.soft` 清晰度边界统一落在两者下方。真正逾期的任务只出现在逾期 Sheet，不再重复保留在“昨天”等主时间线分组。Sheet 支持单项或“全部改到今天”；批量操作逐项原子提交并允许部分成功，成功项迁入今天，失败项留在 Sheet 重试，全部成功后由系统关闭 Sheet。
- iOS 26+ 的日期分组使用原生顶部 `safeAreaBar` 承载当前日期，使系统 `.scrollEdgeEffectStyle(.soft, for: .top)` 的清晰度边界自动位于日期标题下方；日期不进入 Toolbar，也不替换或隐藏模式切换器。列表中的后续日期标题作为分组交接来源正常随内容滚动，其底边越过 `safeAreaBar` 下边界时只发布一次当前分组身份变更，由顶部日期栏接管；不得把连续坐标、滚动方向或逐帧模糊值写入可观察状态。日期栏保持稳定视图身份，并与定期页摘要一致，对完整标题使用原生 Numeric Text 内容转场；首组不重复绘制列表日期标题，交接中的来源标题保留真实布局高度并在栏后隐藏，避免双显、任务跳位和标题高度空隙。iOS 18–25 保持 `LazyVStack(pinnedViews: [.sectionHeaders])` 的原生吸附降级。日期以约 `22pt Semibold` 显示，星期与任务数量以约 `12pt Medium` 显示并按首基线对齐；任务完成或新建时仅数量使用原生数字内容转场。标题只作展示，不响应点击。
- 当今天存在逾期任务时，模式切换下方、日期栏上方固定显示居中的“当前已逾期 N”入口；入口沿用底部完成摘要的轻量文字语法，不使用图标、箭头、胶囊、背景或分割线，仅数字使用高对比度自适应逾期色。整行命中高度不小于 `44pt`，点击打开现有逾期任务 Sheet；入口不隐藏或搬迁主时间线中的逾期任务，数量为零或查看其他日期时不显示。
- 首页任务流不提供下拉刷新；数据更新继续由启动加载、日期切换、同步通知与前后台刷新链路负责。
- 任务标题右侧只显示状态图标：紧急任务使用珊瑚色旗标，已关注任务使用 baby blue `scope`；两种状态同时存在时按“紧急 → 关注”并排显示，不显示日期 / 时间。折叠态标题保持统一字号和字重，标准内容尺寸最多显示两行，超出时尾部截断；不得为维持单行而按长度缩小文字或自动横向滚动，进入详情后完整显示标题，辅助功能大字号下放宽常规两行限制。有备注时先显示小字号备注，有具体时间、提醒或子任务进度时在备注下方（无备注时直接在标题下方）显示紧凑属性行。

#### 关键交互
- 创建类型由当前页面唯一决定：待办页创建普通任务，定期页创建定期任务；创建界面不提供“模板 / 待办 / 定期”切换，也不提供模板能力。
- 待办和定期创建在进入时立即分配最终任务 UUID。该 UUID 同时标识创建会话、持久化记录和保存后的任务行；保存时不得重新生成身份。
- 创建开始即在真实 `LazyVStack` 的确认位置插入 UI-only Draft 行；稳定输入视图和展开预留空间随紧凑行一同建立。首次挂载只让外层真实占位高度以约 `220ms` 展开、内容以约 `160ms` opacity 淡入；不重建输入框，不为下方日期与任务维护独立位移。当前会话校验通过后立即请求一次标题焦点，约 `140ms` 后用稳定 ID 做一次约 `220ms` 的平滑底部预定位，并同步激活展开时钟。编辑期间日期、时间、紧急或周期变化不移动该行，草稿属性只决定提交后的最终分组与排序落点。
- 创建 Draft 固定插在当前未完成分组末尾；定期页的已完成任务仍保持在 Draft 之后。紧凑态的展开预留空间按现有详情估算高度一次性派生，并随同一 `layoutProgress` 反向收回；列表不监听键盘通知或连续位置，不在键盘稳定后再次强制滚动。当内容短于视口时用底部对齐补足滚动距离；若 Dynamic Type 或大量子任务令 Draft 高于可用视口，外层列表继续自然滚动并优先保证当前输入项与“取消 / 添加”可见，不压缩控件、不嵌套滚动。
- 创建项以标题和单行属性带为默认紧凑结构；备注、子任务通过“添加备注 / 添加子任务”按需展开，日期、时间、提醒和紧急属性保持在同一行。标题非空才允许确认添加；时间按 5 分钟粒度，清除时间同步清除依赖该时间的提醒。创建项不提供麦克风或语音录入入口。
- 提交先进入不可重复触发状态，并沿现有详情反向动画收起。持久化成功且落点未变化时，同一 UUID 的 Draft 行原子替换为真实任务行；日期或周期变化时，无动画准备最终分组和位置后由目标行轻量显现。失败时保留 UUID、全部草稿、编辑器和可重试错误。
- “放弃”不写入持久化，也不显示确认弹窗；创建详情先沿现有约 `320ms` 反向动画收起，同一 Draft 外层真实占位在该时钟内连续缩至零，紧凑占位只在最后约 `120ms` 淡出，随后才无动画删除内存对象。下方日期标题与任务只随 `LazyVStack` 自然回填，不瞬间收拢，也不飞回 `+`。保存成功并在同位置替换时不得运行这段退出动画，避免持久化任务先消失再重现。
- 定期页 `+` 插入只含定期字段的临时任务行。待办创建、定期创建和两类详情共享 `HomeMorphSession`、列表内 `TaskMorphContainer` 与相同编辑表面，不再存在根级创建 Overlay、独立创建卡或第二套动画参数；不提供类型切换或模板入口。
- 任务详情不使用独立 Sheet、导航页面、Overlay 或悬浮卡。点击行主体后，同一 UUID 的列表行先取得活跃所有权；其真实高度和上下各约 `14pt` 的呼吸空间连续增加，相邻内容先随 `LazyVStack` 布局自然撑开，再叠加按稳定任务序号派生的有界合成位移，`ScrollView.contentOffset` 不由详情动画驱动。详情保持连续列表背景，不增加任何矩形背景色块、材质、圆角、描边、分割线或阴影；只允许用上下透明衰减的无边界亮度场形成连续焦点平面。浅色展开区域使用纯白净化场把局部粒子压至外围显著度的约 `15%...20%`，不再用石墨灰染暗焦点；净化场与约 `460ms` 展开、`320ms` 收起共享时间轴。展开区域整体约放大至 `1.05`。浅色模式的非活跃任务统一使用约 `0.8pt` 模糊和 `0.52` 透明度，并取消额外压暗；深色模式继续使用约 `1.45pt` 模糊和 `0.49` 透明度。两种模式都不再按与焦点任务的距离改变清晰度，尺度仍按稳定任务序号距离从相邻约 `0.95` 退至远处约 `0.87...0.88`。焦点上方继续向上偏移约 `8...24pt`，下方保持约 `12...36pt` 的级联下移；位移不改变真实行高，下方仍主要由位移表达传播。
- 折叠与展开复用同一完成框和标题 View。两者作为刚性身份组保持内部间距和顶部对齐，在约 `460ms` 内沿内部轨迹 `(0, 0) -> (-7pt, +14pt) -> (-16pt, -12pt)` 的连续左下再左上弧线直接到达终点；该 Y 轴轨迹补偿展开态顶部新增的 `14pt` 真实布局空间，使屏幕上的下潜与上升接近等幅，最终视觉尺度约 `1.12`，终点不得追加回落、Overshoot 或 Spring。紧凑备注与元数据溶解并收起；展开态备注、每个子任务、添加行、底部属性和错误信息按视觉行从 `(+14pt, +24pt)` 沿左上弧线经过约 `(+5pt, +9pt)` 回到原位，透明度从 `0` 到 `1`。详情可在身份组运动途中开始显现，但首行不得早于身份组到达目标位置，当前最早约 `480ms` 落定；相邻行默认错开约 `50ms` 且总错峰不超过 `220ms`，各行落定后独立开放交互。长标题布局宽度不变，不得重新换行抖动。
- 展开详情内联编辑标题、备注、子任务、日期、时间、提醒和紧急状态。标题、备注和子任务保持紧凑无分割线布局；详情首行相对标题槽位向上收紧约 `20pt`，使标题与首个副标题保持约 `4pt` 的视觉间距；底部属性轨道相对最后一个添加 / 子任务行向上收紧约 `6pt`，但所有控件命中区仍不小于 `44pt`。底部属性保持现有设计语言。关闭或切换任务前先保存 draft 并刷新源行摘要；保存失败保持当前详情，不执行不可逆收起。
- 同一时间只允许待办/定期详情或创建中的一个 Morph 会话。外层列表始终可滚动；已有详情活跃时，点击另一任务、模式、OCR、个人页或日期/周期标题只保存并收起当前项，不打开或执行被点击目标。创建 Draft 活跃时，上述入口及底部 `+` 的首次点按都只收起键盘并保留展开草稿，不保存、不丢弃、不排队或执行目标；用户必须先明确“添加”或“取消”。非活跃任务的完成框、Swipe、菜单与重排暂停；创建草稿的完成框不可操作且不进入统计。
- 创建项在属性轨道下方提供明确的“取消 / 添加”动作行：左侧“取消”立即放弃当前 UI-only Draft，右侧“添加”仅在标题包含非空白字符时可用并提交整个任务。创建态标题使用单行输入以保证软件键盘“完成”触发提交，长内容在输入时水平滚动，保存后的列表标题继续按现有多行规则展示；备注“完成”只结束备注输入，新子任务“完成”只加入当前子任务并保留 Draft。VoiceOver Escape 与“取消”等价。已有详情保存失败或创建保存失败都保持展开。
- 已有详情或创建行展开时，其他任务统一退后；日期 / 周期标题与同模式下的非活跃任务使用相同景深强度，浅色为约 `0.8pt` blur / `0.52` opacity，深色保持约 `0.5pt` blur / `0.52` opacity，标题自身仍只使用约 `0.98` scale 且不参与任务行波浪位移。顶部与底部 toolbar、头像均保持系统原生清晰外观；已有详情中的 OCR 与普通导航首次点击只保存并收起，创建 Draft 中则只收起键盘并保留草稿。底部滚动边缘始终由系统 `.scrollEdgeEffectStyle(.soft, for: .bottom)` 管理，不接入详情或创建状态。
- 身份组约 `460ms` 到达终点，创建动作行作为属性轨道后的最后一行以约 `40...50ms` 错峰进入，详情整体最迟约 `700ms` 完成，且动作只在输入就绪后可用。收起约 `320ms`：详情透明度、弧线位移与真实高度共享同一连续布局时钟，创建动作行最先退出，随后从底部属性向备注逆序退出；身份组沿同一波浪路线精确反向返回紧凑端点，其他任务和上下呼吸空间同步恢复，系统底部滚动边缘不接入此时间轴。保存阶段不插入转圈或延迟进度；“添加”留在稳定槽位并锁定重复提交。保存成功后文字先向中心收束，再由同一中心连续写出勾号，约 `440ms` 无过冲落定后复用既有反向收起；失败则直接恢复“添加”并保留草稿。非活跃任务按其与展开任务相差的稳定视觉任务序号执行向外传播的缩放、位移和逐行延迟，模糊与透明度的最终目标值保持统一；每行使用约 `360ms` 的独立合成运动，上方从约 `15ms` 开始、每级增加约 `60ms`、最多 `260ms`，下方从约 `20ms` 开始、每级增加约 `75ms`、最多 `340ms`，使上一行明显推进后下一行才启动，同时把最晚完成时间控制在约 `700ms`。该距离只在列表 / Morph 状态变化时计算，不采样连续坐标或向 Observation 逐帧写值。已有详情外部点按继续使用收起状态机；创建态外部点按只结束输入。完成信号由带会话 token 的可取消生命周期任务发出，不监听逐帧动画阈值。Reduce Motion 取消曲线位移、缩放、级联和任务 / Header 模糊，只保留约 `220ms` 的真实高度与交叉淡变；非活跃任务只降至约 `0.65` 透明度。
- 待办与定期任务的底部属性统一使用固有宽度的单行轨道，左边缘与子任务完成框 / 添加符号视觉列对齐；属性使用系统语义填充区分胶囊边界，控件间距约 `2pt`、文字胶囊水平内边距约 `8pt`，文案完整且命中区不小于 `44pt`。添加备注 / 添加子任务保持次级但可操作的正文对比度；真实子任务之间保留约 `3pt` 的可辨间隔。可用宽度不足时仅属性轨道横向滑动并隐藏滚动条，不截断、不缩小命中区，也不自动换为两行。
- 待办任务不提供重复能力；固定节奏统一由定期任务承载。紧急任务在同一日期分组内置顶，并按创建时间升序排列。
- 子任务始终在创建项或展开详情内新增、编辑、勾选和删除，不进入独立集合设置 Sheet。
- 通过原生右滑执行快捷推迟和删除：周五的主推迟动作自适应为“推迟到下周一”，其他日期为“推迟到明天”。周五长按菜单同时保留“推迟到明天”和“推迟到下周一”；其他日期不额外显示周一入口。两个快捷日期都以操作时的本地日历为基准，保留原具体时刻与提醒提前量；只有日期的待办不自动补充具体时刻。删除立即提供触觉反馈并先提交持久化；只有成功后才让任务文字粒子化、控件淡出并收拢列表，失败时原行保持可见。
- OCR 结果继续先进入草稿确认；确认写入后任务立即出现在时间线，不经过模板或类型选择。

#### 结构与状态流
1. 原生导航栏与 toolbar
2. ScrollView / LazyVStack 任务流
3. 原生底部 toolbar OCR 与创建入口

- 打开应用 -> 进入任务首页
- 点击任务 -> 同一列表行真实增高 -> 后续任务随布局自然下移 -> 内容形成并单次下沉过冲 -> 完整组上提归位 -> 收起时恢复原行高
- 完成任务 -> 卡片沉降到已完成状态
- 点击本周已完成 -> 原生 sheet 快速查看本周不含今天的已完成任务 -> 可进入完整已完成页
- OCR 导入 -> 草稿确认 -> 写入任务 -> 刷新首页

### 9.2 历史清单兼容

#### 页面目标
- 保护旧版本任务的清单关联数据，不把清单重新引入当前一级信息架构。

#### 必备模块
- 保留 `PersistentTaskList` 与任务 `listID` 字段以兼容既有 SwiftData / CloudKit schema。
- 当前版本不提供清单列表、清单详情或批量整理入口。

#### 结构要点
- 清单不注册为导航 route，也不进入底部或顶部 toolbar。
- 后续若重新评估清单能力，必须先更新本规格，再设计迁移与入口。

### 9.3 历史项目迁移

#### 页面目标
- 保护旧版本用户数据，把历史 Project 安全转换为普通 Task。

#### 必备模块
- 一次性 Project-to-Task migration。
- Project 本体使用原 UUID 创建或更新为 Task。
- ProjectSubtask 转为 TaskSubtask。
- 原 projectID linked tasks 并入迁移后 Task 的子任务，并保留原任务 notes、dueAt、hasExplicitTime、remindAt、createdAt、completedAt、sourceTaskID。

#### 结构要点
- `PersistentProject` / `PersistentProjectSubtask` 仅作为 legacy migration source 暂留。
- 迁移后旧 Project 不再进入产品 UI，也不再有新写入入口。
- 不在本轮物理删除 legacy model，避免 SwiftData + CloudKit schema 风险。

### 9.4 日历

#### 页面目标
- 本轮从首页主结构移除，不再作为一级 surface。

#### 必备模块
- 无。

#### 结构要点
- 历史 Calendar 页面、ViewModel、路由与首页周/月日历代码已删除；任务日期分组、日期选择器、提醒和周期计算继续使用 Foundation `Calendar`，不属于日历页面能力。

### 9.5 我

#### 页面目标
- 承接个人设置、隐私与数据控制。

#### 必备模块
- 个人信息
- 整理：保留“已完成”，新增“计划复盘”入口；入口常驻并显示本周完成任务数量，不使用红点、评分或连续打卡
- 任务提醒：一个总开关统一控制任务临近提示与提醒调度；满足日期、具体时间和提醒条件时自动优先使用 AlarmKit，失败时降级为普通通知
- 每日摘要：一个总开关同时控制 09:00 早间摘要与 18:00 晚间总结；早间显示今天需要完成的任务数，晚间显示今天仍未完成的任务数。两者与 Today / Widget 使用同一“逾期 + 今天到期”口径，不把未来任务、已完成任务或归档任务计入；历史缺失日期在统计前归一化，数量为零时仍发送明确完成态。
- 已完成自动归档
- 应用锁定与 iCloud 同步状态
- 关于、反馈、隐私协议与数据删除

#### 结构要点
- 顶部为横向个人资料入口，保留 76pt 头像，不显示“独立工作空间”等无信息量副标题，并与首个设置分组保持充足留白。
- Profile 首页使用与任务页同源的平铺 `ScrollView` 分组与 AppTheme 字体；通过组间留白和紧凑标题分层，不使用行间分割线、跨组分隔线或 iOS 设置式的 inset-grouped 白色卡片容器。
- 任务提醒、临近窗口与每日摘要直接在 Profile 首页调整，不再打开独立“提醒与闹钟” sheet；每日摘要行固定显示 `09:00 · 18:00`，不增加独立时间选择页。离散值使用系统 `Menu`，且菜单只锚定在行尾当前选项，不让标题或整行触发。
- 通知与 AlarmKit 不作为两项权限设置展示。任务提醒开启时按需请求系统权限；关闭时取消 Together 已安排的任务提醒，但不修改系统授权。权限被拒绝时在当前分组就地提供系统设置入口。
- iCloud 状态使用按内容自适应初始高度的原生 sheet。
- Profile 的“显示”分组提供“外观”选项，支持“深色 / 浅色 / 跟随系统”，默认跟随系统；偏好只保存在当前设备 `UserDefaults`，不进入 SwiftData 或 CloudKit。仍不在 Profile 暴露默认推迟时间、快捷时间预设或缓存清理。
- Profile 的“显示”分组提供“动态背景”开关，默认开启，只保存在当前设备 `UserDefaults`，不进入 SwiftData 或 CloudKit。关闭时粒子先物理减速再淡出；开启与冷启动从静止平滑加速。
- 高风险数据操作进入二级页，不在 Profile 首页直接暴露

### 9.6 待办履历与计划复盘

#### 页面目标
- 让用户知道一项待办何时正式创建、从创建到最终完成经过多久、期间推迟与恢复了多少次，并能解释计划为何漂移。
- 通过本周 / 本月汇总识别个人排期习惯；不把结果包装成绩效分、效率评分或审计日志。

#### 记录范围
- 首版只覆盖普通待办，不覆盖定期任务或周期实例。
- 正式创建以首次成功持久化为准；创建器草稿、点击新建时间与预分配 UUID 时间不计入生命周期。
- 只记录已提交的净领域变化：正式创建、首次计划、推迟、提前调整、完成、恢复。取消排期与重新计划只保留旧履历读取兼容，不再生成。
- 标题、备注、提醒、入口来源、停留时长、保存前撤回的编辑和逐个子任务操作不进入履历。完成事件仅额外快照当时未完成子任务数量。
- 推迟由排期结果判定，与快捷按钮或手动编辑来源无关；已有计划向后移动才计一次。首次计划和同日只改变时间精度不计入推迟次数。
- 归档保留履历；永久删除待办时一并删除履历及其统计贡献。

#### 单任务指标
- 正式创建时间、首次计划、首次完成时间、最终完成时间、最终完成耗时。
- 推迟次数、累计推迟时长、最终计划相对首次计划的偏差、恢复次数。
- 无法从升级前旧数据可靠还原的事件显示“更新前未记录”，不得显示为 0 或反推伪造；已知的创建时间与当前完成时间可继续展示。升级后新产生的推迟 / 恢复可以显示为“更新后记录 N 次”，但历史不完整的待办不进入首次计划兑现率分母。
- 日期型计划按本地日历当日结束判断是否按时，不将其伪装为 `00:00` 的具体时刻；含具体时间的计划按真实时间点判断。

#### 未完成待办内联履历
- 展开详情中的只读区域仅显示“创建至今 + 自然相对时长”，不进入首页折叠任务行。
- 内联区域不显示推迟摘要、事件列表或“更新前未记录”等历史完整性文案；完整指标与履历统一在已完成任务回顾中展示。

#### 已完成任务回顾
- 从“已完成”归档点击任务进入原生导航 push 的只读回顾页；归档原有恢复和删除操作继续留在 swipe / menu，不迁入回顾正文。
- 页面按“结果 → 计划 → 时间线”排列：先展示完成耗时、推迟 / 恢复摘要，再展示首次与最后计划、计划偏差，最后展示全部履历事件。
- 时间线显示到分钟；只有日期的计划只显示日期。历史不完整时用明确说明标出履历起点，不以空白或零值误导。

#### 计划复盘
- 从 Profile“整理”中的常驻入口进入原生导航页面；toolbar 使用系统 `Menu` 在“本周 / 本月”间切换。
- 完成样本按最终完成时间归入周期，并且当前必须仍处于完成状态；恢复后尚未再次完成的待办暂时退出样本。归档不改变归属周期。
- 主要指标为完成数量、最终完成耗时中位数、首次计划兑现率、最终计划按时率、发生推迟的任务占比和无计划完成数量。无计划任务不进入两项按时率分母。
- 趋势只比较当前周期与相邻上一周期的完成耗时中位数、首次计划兑现率和推迟任务占比；当前与上一周期各至少有五项有效完成样本时才显示，使用中性描述，不做红绿好坏判断。
- 当前计划风险与完成统计分离，并按“已逾期 → 已推迟但仍未完成”去重归类；普通任务不再生成取消排期或长期未排期风险。
- 周五入口可增加“本周可回顾”次级文案，但不自动弹出、不发送推送、不显示红点；计划复盘始终由用户主动进入。

#### 空态与边界
- 没有完成样本时显示说明性空态，并继续展示当前计划风险；样本不足时隐藏趋势而不是显示不稳定结论。
- CloudKit 同步后以事件身份去重；同一任务在 App、通知、Widget 或 Live Activity 完成时产生相同语义的完成事件，不按入口分别统计。

### 9.7 OCR 导入

#### 页面目标
- 把纸面内容或用户主动粘贴的多行文字转换为可确认的任务草稿。

#### 必备模块
- 由 toolbar OCR `Menu` 触发的原生来源：相机 / 照片 / 粘贴文字
- 同一来源 Sheet 内的相机预览、inline PhotosPicker、识别中与失败状态
- 使用系统 `PasteButton` 的文字来源 Sheet
- 原生自适应 Sheet 承载任务草稿确认与信息录入
- OCR 识别进度与失败提示
- 原始识别文本预览
- 结构化任务草稿列表
- 任务标题、备注、子任务与日期 / 时间 / 提醒 / 紧急属性编辑
- 用户确认写入

#### 关键交互
- 用户点击底部 toolbar 左侧 OCR 后，由系统 `Menu` 展示“相机 / 照片 / 粘贴文字”命令；不维护自定义菜单容器、按钮材质或 morph 转场。
- 相机使用原生 `.medium` Sheet；照片使用支持 `.medium/.large` 的原生 Sheet并默认打开到 `.large`。相机切换照片、识别中和失败状态复用同一个来源 Sheet，不重复争抢 presentation controller。
- 相机预览和照片网格填满 Sheet 内容区，操作按钮遵循 Sheet 安全区域；尺寸、圆角、阴影、拖拽和 Home Indicator 避让交给系统 Sheet。
- 照片页使用系统 inline `PhotosPicker`，用户选图后在当前来源 Sheet 中继续 OCR 识别。
- 粘贴文字使用默认 `.medium` 的原生来源 Sheet，并提供系统 `PasteButton`；App 不预读或监听剪贴板，只有用户明确点击粘贴后才接收文字。
- 相机 / 照片由系统识别文字，粘贴来源直接使用用户提供的文字；两条路径均经本地确定性规则生成候选草稿并保留原始行序，不依赖 Apple 智能或自然语言推断。
- 识别或文字解析成功后来源 Sheet 先完成关闭，再由根层原生 Sheet 接管确认编辑；短内容默认半屏，长内容或键盘输入自动升级为全屏。
- OCR 草稿只使用“任务 + 子任务 + 属性”的统一语言；不再出现“项目草稿 / 待办草稿”分流。
- 用户可编辑标题、备注、子任务、日期、时间、提醒和紧急属性。
- 用户确认后才写入 SwiftData。

### 9.8 定期任务

#### 页面目标
- 让用户按每天、每周、每月及可选的每季、每年维度查看和完成固定节奏任务。

#### 结构与交互
- 维度选择与当前维度完成摘要固定在列表上方；用户可自主选择展示哪些维度，隐藏维度不删除任务；当前完成进度使用系统圆形进度表达。
- 主体为与首页同源的连续任务流；任务点击后由同一个列表容器增加真实行高，仅将后续任务自然向下推开，不修改外层 ScrollView 的 content offset，不使用根级详情表面、独立详情 Sheet 或定期页局部焦点层。
- 展开内容只包含定期任务标题、备注、维度、目标日和目标时间；修改先进入 draft，收起后统一保存。
- 定期任务折叠与展开复用同一标题区；有备注时备注为主副标题、目标节奏位于下一行，无备注时目标节奏优先，无目标才显示状态。目标日和目标时间可分别不设置或清除；新建与切换维度不自动生成周二或 09:00。
- 展开态的目标时间与待办共用 SwiftUI 原生 Popover；系统负责箭头、定位、展开方向与原生出现 / 收起动画。滚轮与清除只修改临时值，“完成”提交到当前 draft，点 Popover 外取消；任务仍只在收起展开卡后统一持久化。
- 定期任务提供独立提醒属性，支持目标时刻与常用提前量；默认使用本地通知，iOS 26+ 可由用户选择 AlarmKit 原生闹钟，旧系统自动降级为通知。
- 定期任务支持原生右滑“推迟到明天”和删除；推迟只影响当前一次显示与提醒，不永久修改重复节奏。删除复用待办相同的“持久化成功后再播放文字粒子化与列表收拢”反馈。

### 9.9 关注任务与实时活动

#### 页面目标
- 用户可从待办详情用原生 `scope` 图标关注一个或多个尚未完成的普通待办；关注态使用 baby blue，并在锁定屏幕与 Dynamic Island 持续查看其中最需要注意的任务。
- 锁定屏幕与 Dynamic Island 的 expanded、compact、minimal 形态统一使用 baby blue 作为关注强调色；系统背景、正文和完成操作继续使用系统语义色。

#### 结构与交互
- 关注是待办的长期持久化状态，独立于紧急、完成、排序和提醒；定期任务与已完成待办不提供关注入口。
- 最新关注的待办为 primary；允许关注任意数量，但系统层始终最多只有一个 Live Activity，其中只按关注时间倒序展示前三项和剩余数量。
- 完成待办会取消关注，重新打开不会恢复；删除、归档同样不能继续出现在实时活动中。
- Live Activity 是关注状态的临时投影。用户或系统结束、移除或令其到期时不取消关注，也不在 App 恢复 active 后自动续建；下一次明确修改关注状态或重新开启系统实时活动权限时才允许创建。
- 锁定屏幕与 Dynamic Island 可直接完成待办，必须复用应用层完成语义；点击任务标题通过稳定 UUID 深链到首页对应详情，目标不存在时安全回退。
- 系统实时活动关闭时，关注仍成功保存，并在 App 内显示轻量提示“已关注，但实时活动未开启”。

### 9.10 系统级快捷创建

#### 页面目标
- 让用户从 Shortcuts、Spotlight、Siri 及支持机型的 Action Button 直接进入 Together 的现有待办创建流程，不依赖 Apple 智能、Foundation Models 或后台任务写入。

#### 结构与交互
- 首版只提供一个 App Shortcut：“在 Together 新建任务”；系统短标题为“新建任务”，使用 `plus` SF Symbol，不暴露定期任务、日期、提醒、紧急、子任务或自然语言解析参数。
- Shortcut 可选接收一个“任务标题”文本参数。参数只表示单条任务标题：去除首尾空白并把连续空白或换行归一为单个空格；归一后为空时按无标题处理。多任务内容继续使用 OCR 菜单中的“粘贴文字”。
- 执行后必须把 App 带到前台并固定回到待办“今天”，进入现有 UI-only Draft；无标题时建立空白 Draft，有标题时预填标题。运行 Shortcut 本身不得直接写入 SwiftData、CloudKit、统计或履历，用户仍需在现有创建行中确认。
- App 尚未完成启动或当前被应用锁遮挡时，快捷创建请求只保存在当前进程内，待启动完成并解锁后再交给根路由；不得在锁定遮罩后展开 Draft 或弹出键盘。
- 已有详情活跃时，先沿现有保存并收起状态机处理当前内容，成功后再串行打开下一条 Draft；保存失败时保持当前内容并继续保留待处理请求。创建 Draft 已活跃时，快捷创建请求继续留在单一交接队列中，首次意图只结束当前输入；用户明确“添加”或“取消”结束当前 Draft 后再接收下一条，不得隐式保存、丢弃或覆盖当前输入。
- 快速连续触发必须经过单一内存交接队列和现有 `HomeMorphSession`，任何时刻只允许一个活跃 Morph 会话；不得新增第二套创建页面、后台持久化服务或并行创建状态机。

## 10. 必要二级页面
- 首页任务行由同一列表容器原地展开详情
- 上下文创建入口：由当前待办/定期页面决定类型，统一在当前列表插入并展开临时空任务行；保存后同位置原子替换，跨日期或周期时收起后迁移并局部显现
- 已完成任务归档页：原生 `List` + navigation toolbar；顶部快捷范围菜单，底部 toolbar 原生搜索项、精确筛选按钮和精确日期 / 月份筛选 sheet。
- 已完成任务回顾页：从归档任务进入的只读原生 push 页面，按结果、计划、时间线组织。
- 计划复盘页：从 Profile“整理”进入的只读原生 push 页面，支持本周 / 本月范围、描述性趋势与当前计划风险。
- OCR 来源 Sheet：相机 / 照片、识别中与失败处理
- OCR 确认 Sheet：任务草稿编辑、属性设置与确认导入
- 全局快捷创建入口

## 11. 当前实现结论
- 当前首页改为统一任务区：首页不再区分任务与项目，不再展示顶部周视图、月历或项目切换入口。
- 项目是 legacy 数据迁移来源，不再是新增或展示对象。
- `Decisions / Anniversaries / Pairing / TaskChat / Paywall / Supabase Sync` 属于旧实现或旧业务能力，已从当前运行时与导航删除。
- 当前单人模式下不展示协作信息、订阅信息或第三方后端状态。

## 12. 版本路线
- V1：统一待办首页、任务/子任务、定期任务、Widget、本地 SwiftData。
- V1.1：CloudKit private database 同步与恢复稳定化。
- V1.2：OCR 扫描导入 MVP。
- V1.3：普通待办关注状态与单实例本地 Live Activity。
- V1.4：普通待办履历、已完成任务回顾、周/月计划复盘，以及周五“推迟到下周一”。
- 后续版本只围绕个人效率增强，不规划双人/多人协作路线。

## 13. 数据与隐私边界
- 用户任务、待办履历、清单、设置、OCR 草稿只保存在本地 SwiftData 与用户自己的 iCloud private database。
- 不向 Supabase、RevenueCat 或其他第三方后端发送任务内容、OCR 文本、项目数据或订阅状态。
- OCR 识别默认使用 Apple 原生 Vision / VisionKit 能力。
- 多行文字导入只通过用户主动点击的系统 `PasteButton` 接收文字，不直接读取或后台监控系统剪贴板。
- 若使用 Apple Foundation Models 做结构化解析，必须提供不可用时的本地规则 fallback。

## 14. 成功标准
- 用户能在 10 秒内进入首页并理解当前任务重点。
- 快速新建与完成任务的路径足够短，可高频使用。
- OCR 与多行文字导入能把常见纸面待办或复制的逐行清单转换成可编辑草稿，并且不会未经确认直接入库。
- CloudKit 同步/恢复不依赖订阅状态或第三方后端。
- 首页、详情与 OCR 确认切换保持明显的原生顺滑感。
- 普通待办从 App、通知、Widget 或 Live Activity 发生的创建、排期、完成与恢复均能形成一致履历；升级前未知历史不被误报为零。
- 用户能从单任务回顾解释计划变化，并能在周/月复盘中区分完成结果与当前未完成风险。

## 15. Open Questions
- OCR 第一版是否只支持印刷体/清晰手写，还是同时支持复杂会议笔记结构。
