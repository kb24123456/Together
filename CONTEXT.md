# Together

Together 是面向个人任务管理的 iPhone Todo 产品；本词汇表用于统一产品界面与交互中的核心概念。交互呈现词汇描述最终用户可观察的视觉与行为，不指定 SwiftUI、UIKit、快照、动画器或视图层级等实现路径。

## Profile

**任务提醒**:
统一控制任务临近提示与 Together 提醒调度的个人偏好；系统按任务条件自动选择通知或原生闹钟，用户不需要分别管理两种投递方式。
_Avoid_: 提醒与闹钟、通知开关、闹钟开关

## 任务模式

**待办**:
不按固定周期自动出现、由用户安排和完成的个人任务。
_Avoid_: 普通任务、单次任务、任务模式

**定期**:
按每天、每周、每月、每季或每年等固定周期重复出现的个人任务。
_Avoid_: 例行、周期事务、重复任务

**关注任务**:
用户主动加入持续关注集合的未完成待办；当前版本只允许关注待办，完成任务会退出关注集合，重新打开后不会自动恢复关注。系统层的实时展示被移除或到期不改变任务的关注状态。“关注”是任务领域状态，不表示输入焦点或首页视觉焦点。
_Avoid_: 关注已完成任务、关注定期任务、焦点任务、置顶任务、紧急任务

## 交互呈现

**任务模式切换器**:
首页顶部用于在“待办”与“定期”两个紧密相关子视图之间切换的无外壳双按钮。它位于 Toolbar principal 槽位，与右侧头像共同构成约 44pt 的单一主控行并在滚动期间保持稳定：普通字号下整体为 144 × 44pt，两项各占 72 × 44pt；当前项使用主文字色与 Semibold，非当前项使用三级文字色与 Regular。按下项缩放至 0.94、透明度约 0.82，松手约 180ms 回弹；辅助功能大字号下退化为显示当前模式的系统 Menu。Toolbar 共享背景在支持的系统上隐藏。Reduce Motion 下取消缩放、回弹和空间运动，只保留必要淡变。
_Avoid_: 轨道、双按钮胶囊、常驻选中底块、下划线、匹配几何指示物、`glassEffect`、分段控件外层玻璃壳

**原生日期栏**:
iOS 26+ 待办时间线中的当前日期、星期和任务数量由 Toolbar 下方左侧的系统 `safeAreaBar` 承载，使日期保持清晰，并让系统 `.soft` 边界自然位于日期栏下方。首组日期不在列表重复绘制；后续日期标题作为分组交接来源随内容滚动，到达日期栏底部时只切换一次当前日期身份并在栏后隐藏、保留布局高度。它不进入 Toolbar、不替换模式切换器，也不依赖滚动方向或连续坐标状态；iOS 18–25 使用原生 pinned Section Header 降级。日期栏和交接标题都不绘制背景遮罩、材质、圆角、描边或阴影。日期使用主字号，星期和任务数量使用小字号并与日期首基线对齐；任务完成或新建使数量变化时，只有数字执行原生内容转场。标题只表达当前分组，不响应点击、不打开日历或触发滚动定位。
_Avoid_: Toolbar 日期副本、自定义滚动方向阈值、逐帧可观察几何状态、同时显示两份日期、日期移入状态栏、系统大标题导致导航栏增高、日期与星期上下堆叠、可见卡片式矩形背景或渐变材质

**顶部模糊层**:
首页任务流使用系统滚动边缘材质。任务内容滚入边界后由系统渐进降低清晰度；待办日期栏、模式切换器、头像和底部 toolbar 保持正常对比度。待办顶部与底部都使用 `.scrollEdgeEffectStyle(.soft, ...)`，并以原生 `.safeAreaBar(edge: .top)` 让系统自动把顶部边界延伸到日期栏底部，不叠加日期材质或自定义 blur / mask。定期页通过 ScrollView 原生 `.safeAreaInset(edge: .top)` 同步布局固定维度 Header 和任务内容，顶部边缘效果关闭、底部使用系统 `.soft`。系统可随设备、Dynamic Type、导航栏、安全区和可访问性设置调整实际曲线。
_Avoid_: 任务在顶部控件后仍保留清晰文字轮廓、整段均匀模糊、把模糊背景放进日期 Header、Header 吸附后才生成模糊、叠加多层材质、与画布不一致的不透明色块、硬边或分割线

**系统底部滚动边缘**:
待办与定期任务流的底部边缘完全交给系统 `.scrollEdgeEffectStyle(.soft, for: .bottom)`。不再按任务行位置自定义汇聚、压缩、淡出、模糊、遮没阈值或命中切换，详情与创建状态也不修改该系统效果；任务真实行高、身份、滚动惯性、toolbar 安全区和辅助功能行为继续由 SwiftUI 与系统容器管理。
_Avoid_: 自定义底部模糊、顶部任务堆叠、待办与定期使用不同的系统底部边缘策略、日期标题或辅助入口随任务压缩、浅层任务重叠、三层以上轮廓堆叠、整屏任务速度尾随、滚动时改变任务真实行高、全列表密度切换、果冻式逐行回弹、可读任务互相覆盖、扭曲文字、复制通知中心卡片墙

**任务原位抬升**:
任务行与详情是同一稳定 UUID 在连续列表中的两种形态；打开时原行真实增高并只把后续任务自然向下推开，不创建悬浮表面，不修改 ScrollView content offset。关闭后同一行恢复紧凑高度。
_Avoid_: 屏幕中心生成卡片、详情表面中途替换、详情 Sheet、详情页面跳转、复制任务身份、主动滚动列表

**潜伏任务表面**:
普通任务行与已有详情始终直接使用连续列表画布；展开不增加任何矩形背景色块、明度差、材质、圆角、描边、分割线或阴影。创建卡继续使用自身独立表面规则。
_Avoid_: 常驻卡片墙、展开时插入白底或浅色矩形、圆角焦点卡、阴影或边框

**表面横向展开**:
已有详情不增加可见表面边界；稳定完成框与标题作为刚性身份组沿同一路线左移，在最终态共同到达约 `x = -12pt` 并局部放大至约 `1.09`，内部间距和对齐关系不变。
_Avoid_: 整行缩放、内容重新居中、所有列表行同时横向移动、展开态另设内容列

**展开锚点**:
已有详情以原任务行顶部和标题文字轴为锚；行高向下增长，外层列表继续保留原生滚动能力，不为展开自动定位或强制居中。长内容由外层列表承接。
_Avoid_: 强制居中、固定屏幕位置、展开时主动滚动背景时间线、底部内容裁切、详情内嵌第二滚动区

**锚定表面绽放**:
已由“单行波浪内联展开”取代：真实布局从首帧让位；完成框与标题身份组在约 `460ms` 内沿内部轨迹 `(0, 0) -> (-7pt, +14pt) -> (-16pt, -12pt)` 的连续曲线直接到达约 `1.12` 视觉尺度。Y 轴终点补偿顶部新增的 `14pt` 真实空间，使屏幕上的下潜 / 上升接近等幅，不追加终点回落或 Spring。活跃区域整体约 `1.05`、上下各增加约 `14pt` 真实空间；其他任务按稳定序号形成逐行连续的透明度和模糊梯度，尺度仍有界，上方上移 `8...24pt` 并以每级约 `60ms`、最多 `260ms` 错峰传播，下方以 `12...36pt` 下移和每级约 `75ms`、最多 `340ms` 的错峰强化真实下滑。背景行自身约 `360ms`，整体最晚约 `700ms` 完成。待办与定期共用完成框、标题轴线、静态层级、属性轨道与展开 / 收起语义，字段及日期分组 / 周期维度仍各自独立；快速反向使旧收起 token 失效。
_Avoid_: 外壳过冲、连续振荡、列表位移回弹、内部控件各自弹跳、固定 sleep 拼动画、全屏动态模糊

**详情内容揭示**:
紧凑备注和属性摘要在原位溶解并收起高度；展开态备注、每个子任务、添加行、底部属性和错误信息按最终宽度预排版后以视觉行级联。每行从约 `(+14pt, +24pt)` 沿左上弧线经过约 `(+5pt, +9pt)` 回到原位，透明度从 `0` 提升至 `1`；内容可在身份组途中开始进入，但首行最早约 `540ms` 落定，不得先于标题。相邻行默认错开约 `55ms`，总错峰不超过 `260ms`，整组最迟约 `800ms` 完成，各行落定后独立开放交互。收起在约 `360ms` 内按相反顺序退出，行透明度 / 位移与真实高度共用同一连续时钟，不允许标题之外内容瞬间卸载。
_Avoid_: 整块矩形显现、逐控件独立弹跳、文字动画中重新换行、折叠摘要与详情属性长期重叠、关闭时任务身份闪断

**减弱动态转场**:
用户启用“减弱动态效果”后，已有详情只保留约 `220ms` 的真实高度变化与交叉淡变，取消横向位移、局部放大、背景缩放、级联和过冲；非活跃任务仅降至约 `0.65` 透明度。创建流程继续沿用其直接建立编辑态的降级。
_Avoid_: 完整 Hero 动画、长距离位移、背景缩放、动态模糊、回弹、弹性落定

**首页交互会话**:
待办首页同一时间唯一拥有视觉与输入控制权的创建或详情交互；创建草稿存在时不展开其他任务，详情状态收到创建意图时先回到任务行，提交期间不接受新的冲突意图。
_Avoid_: 创建与详情并存、重复提交、旧回调修改新状态

**焦点舞台**:
创建会话期间，视觉上位于稳定背景平面之前并独占输入的根级创建区域；已有任务详情不进入焦点舞台，只在真实列表行内展开。该词只描述用户感知到的前后景关系，不规定具体视图容器或框架。
_Avoid_: 长期模态 Sheet、列表与舞台双重身份、活动中途替换前景实体、把舞台名称解释成特定技术层级

**来源与落点**:
创建表面离开前的真实起始位置，以及提交后同一 UUID 在列表中的最终位置。已有任务详情始终留在来源行，不产生空位、替身或第二份内容。
_Avoid_: 来源任务的模糊副本、邻近任务补位、近似位置、动画途中漂移的目标、可见替身卡、到达后再跳到真实位置

**焦点任务表面**:
从创建按钮开始、在焦点舞台中形成创建卡并最终消融到列表的唯一视觉实体。已有任务详情不使用焦点任务表面。
_Avoid_: 毛玻璃卡片、背景纹理穿透、两套内容交叉淡变、透明卡壳、身份元素各自漂移、端点处重建视觉身份

**稳定背景平面**:
首页交互会话期间位于焦点任务表面之后、视觉上保持稳定的单一应用内容画面；它包含应用导航栏、顶部控件、时间线和底部 Dock，但不包含系统状态栏、键盘或焦点舞台。编辑造成的列表变化在提交前不得在该平面中形成可见重排。唯一例外是用户确认提交后，若最终任务落点不在可视区域，仍处于景深退让状态的时间线可以执行一次受控、连续的定位，将真实落点带入可视区。
_Avoid_: 多块背景各自动画、冻结状态栏或键盘、编辑期间可见的列表重排、提交前滚动背景、最终落点突然跳入、前后两种背景交叉露出

**背景景深退让**:
创建 Overlay 与已有详情使用各自的景深语法。已有详情展开时，活跃任务整体约放大至 `1.05`、身份组最终约 `1.12`，后方只保留无边界透明亮度场；非活跃任务按稳定视觉任务序号形成逐行连续梯度：模糊从相邻约 `0.35pt` 增长到第 4 行约 `2.15pt`、第 8 行约 `5.95pt`，最大 `6pt`；透明度从相邻约 `0.60` 每级降低约 `0.055`，最低约 `0.20`；尺度从相邻约 `0.95` 退到远处约 `0.87...0.88`。背景任务每行使用约 `360ms` 的独立合成运动；上方上移 `8...24pt`，从约 `15ms` 开始、每级增加约 `60ms`、总错峰不超过 `260ms`；下方下移 `12...36pt`，从约 `20ms` 开始、每级增加约 `75ms`、总错峰不超过 `340ms`，以下方逐行下移为主形成传播水波。日期 / 周期标题使用固定约 `0.5pt` 模糊、`0.98` scale 与 `0.52` opacity；Toolbar、头像保持清晰，Dock 保持安全区位置和清晰轮廓但约缩至 `0.98`、透明度降至 `0.70`。距离只在列表 / Morph 状态变化时计算，Reduce Motion 关闭空间位移、缩放和模糊。首次点击仍只保存并收起，不执行背景目标动作。
_Avoid_: 全屏或均匀模糊、逐帧读取任务坐标、把滚动几何写入 Observation、将大标题重模糊成深色污迹、让 Dock 消失或侵入安全区、首次背景点击同时执行原动作

**焦点 Toolbar 退场**:
待办创建项或详情卡建立焦点时，自定义底部 Dock 保持原有安全区位置，但其外观与导航栏、列表一起进入稳定背景平面并停止命中，不单独执行另一套退场。焦点任务表面稳定回到任务行或创建 Seed 返回来源后，真实页面恢复清晰与交互。
_Avoid_: Dock 位置消失导致列表高度跳变、Dock 独立动画、让真实 `+` 与 Seed 同时可见、卡片未稳定即恢复真实页面、转场期间改变来源或落点

**任务展开意图**:
同一时间只允许一个任务拥有展开状态；当前任务在展开或收起途中收到背景点击或重复选择时，从实时形态和速度立即反向。详情活跃期间点击其他任务只视为关闭当前详情，不排队也不继续展开被点击任务；当前详情完全回到紧凑行后，用户必须重新选择其他任务。
_Avoid_: 多卡并发展开、等待旧动画完成后反向、关闭后自动打开另一任务、静默丢弃点击

**有界可中断转场**:
详情原位抬升、详情收起、创建 Seed 进入和创建回退都允许从用户当前看到的形态、进度和速度立即反向；反向不会跳到端点或重新播放已经经过的阶段。只有创建提交已经进入保存过程后锁定冲突交互，等待成功收缩或失败恢复编辑。
_Avoid_: 动画期间全局锁定点击、反向时从端点重新播放、反向瞬间改变速度、旧交互污染新会话、保存中取消或重复提交

**统一视觉进度**:
已有详情用单个局部 `TaskExpansionMotion` 承载多条独立 Keyframe track；真实行高、身份区、交叉淡变和完整组落定各有时序，但共享同一可中断触发目标。逐帧值不发布到列表根状态。
_Avoid_: 单一 Spring 绑定全部属性、明显断开的串行动画、改向时部分 track 继续前进、端点重建整行

**实体落定感**:
已有详情的运动以真实列表让位开始；完成框与标题作为同一身份组先向左下、再向左上落定，详情各视觉行随后沿同向左上弧线级联升起。每条路线只允许一次克制的阻尼落定，不反向驱动真实行高或相邻任务。
_Avoid_: 突然起跳、不可辨认的伪回弹、夸张过冲、多次回弹、背景或内容回弹、终点呼吸、机械匀速、长时间拖尾

**即时按压响应**:
手指接触任务行主体或创建按钮的当帧，目标实体立即产生极轻、短促的压实反馈；抬手确认后才从当前形态连续进入详情或创建转场。若手指移出有效命中范围，反馈自然恢复且不启动卡片。按压反馈不得抢夺任务身份或形成明显缩小、弹跳和延迟。
_Avoid_: 抬手后才响应、明显缩放、按压回弹、移出后仍误触、按压反馈与后续转场断开

**任务身份区**:
折叠任务行与展开详情共同表达同一任务身份的稳定内容集合，仅由完成框和标题组成。二者可按统一关键帧局部放大、下沉和横向舒展；备注与属性摘要不属于稳定身份，进入详情时溶解并由展开内容组替换。
_Avoid_: 卡片标题栏、转场替身内容、展开态专用标题、标题闪断、把备注或折叠摘要当作稳定身份

**紧凑任务标题**:
折叠任务行中保持统一字号和字重的任务名称；标准内容尺寸最多显示两行，超出时尾部截断，进入详情后完整显示。辅助功能大字号下放宽常规行数限制，以可读和可访问完整内容为先，不通过缩小文字或自动横向移动换取单行完整展示。
_Avoid_: 按标题长度逐行缩放、单行跑马灯、多个标题同时运动、用极小字号容纳任意长度、详情仍截断完整标题

**内容副标题**:
任务身份区中可选的备注内容；没有备注时不使用“进行中”等状态文案填充，子任务进度与任务属性由折叠任务摘要表达。备注与折叠任务摘要使用一致的次级字号和视觉行高。
_Avoid_: 状态占位副标题、混合属性副标题

**任务完成框**:
任务标题或子任务标题左侧的低对比度细实线圆角矩形状态标记；视觉尺寸跟随对应文字行高，主任务约为 24pt、子任务约为 20pt。主任务完成框只与主标题行垂直居中，不受备注或折叠摘要高度影响；子任务完成框与主任务完成框共用左侧轴线，子任务标题与主标题共用文字轴线。完成后沿用同一位置和尺寸显示打勾反馈，视觉框变小不改变至少 44pt 的实际触控区域。
_Avoid_: 虚线轮廓、接近圆形的框、40pt 可见大框、按整段任务内容居中、主从任务轴线漂移、用可见尺寸代替触控尺寸

**折叠任务摘要**:
折叠任务行中位于备注下方的轻量只读摘要，固定按时间、提醒、子任务进度的顺序表达已配置信息；子任务进度由完整环形进度与完成数表达，未配置属性不占位。它不属于稳定任务身份，展开开始后在原位快速淡出，随后由详情子任务和卡片底部属性按钮承接；属性修改只在展开态完成。
_Avoid_: 改变元数据顺序、裁切进度环、共享属性带、展开态重复摘要、折叠摘要参与几何移动、摘要与详情按钮同时长期可见

**任务属性按钮**:
详情卡与添加卡内容流末尾用于配置任务字段的紧凑控件。待办与定期详情共用同一固有宽度单行轨道和胶囊/圆形视觉语法，轨道可见左边缘与子任务完成框或添加符号列对齐；胶囊使用系统语义填充，控件间距约 `2pt`、文字胶囊水平内边距约 `8pt`，可见尺寸之外保留至少 `44pt` 命中区。空间不足时只让属性轨道横向滑动并隐藏滚动条，不截断、不压缩命中区或自动换行；按下提供轻微压实、明度变化和选择反馈。
_Avoid_: 属性栏轴线漂移、强制等宽列、自动换为两行、混用不连续曲率、描边、渐变、按下无反馈

**紧凑详情内容流**:
详情卡与添加卡中按标题、备注、子任务、添加入口和任务属性按钮顺序自然排列的内容结构；属性按钮紧跟最后一个可见内容，只保留短间距和卡片底部内边距，不固定在预留底槽。添加卡以内容自然高度吸附键盘，内容增加时从顶部方向增长。
_Avoid_: 固定详情高度、属性栏底部固定槽位、用 Spacer 制造大块空白、短内容占用长卡片、键盘出现时压缩卡片高度

**系统呈现**:
由 iOS 完整负责容器、定位、系统箭头和出现 / 收起转场的界面呈现；应用可以提供呈现内容，但不干预系统外壳与动画。
_Avoid_: 无箭头原生浮层、自定义原生动画

**上下文创建器**:
由用户当前所在页面决定创建待办或定期内容的轻量输入界面；创建器内部不再提供模板、待办与定期之间的类型切换。待办页面进入待办草稿，定期页面进入定期草稿。
_Avoid_: 统一类型创建器、创建器类型切换、模板模式

**属性编辑区**:
上下文创建器下部用于配置待办或定期属性的互斥工作区；切换属性时在原位替换内容，日期与离散选项直接进入当前草稿，时间由区域内“保存”提交临时选择，最终只由卡片外的提交动作创建任务。再次选择当前属性会关闭该区域，选择文本内容则返回文字编辑。
_Avoid_: 属性 Sheet、日期时间 Sheet、属性弹窗

**有效创建草稿**:
上下文创建器中标题或备注至少一项包含非空白内容的待办或定期草稿；只有有效创建草稿才能执行卡片外的主提交动作。
_Avoid_: 已保存任务、已完成草稿、可提交属性

**任务创建身份**:
待办创建会话开始时分配的最终任务身份；创建按钮转场、待办草稿、持久化任务、列表任务行和后续详情都继承同一个身份。放弃草稿时该身份不会形成真实任务。
_Avoid_: 临时视觉身份、保存时重新分配身份、草稿与任务映射

**创建 Seed**:
焦点任务表面从当前页面自定义底部 Dock 添加按钮取得的初始圆形形态；它承载任务创建身份，在焦点舞台中连续变形成创建卡，并一直保持视觉连续性直到提交进入最终任务行或放弃返回添加按钮。
_Avoid_: Seed 与创建卡片同时可见、两个表面交叉淡变、进入途中交还列表、从屏幕中心生成卡片

**待办创建项**:
使用任务创建身份、位于焦点舞台中的临时任务形态；它在稳定的创建焦点位完成输入，不随最终任务落点或列表排序移动。编辑期间背景中不出现第二份创建卡；保存后同一焦点任务表面收缩并进入真实任务行，放弃时则返回创建入口且不形成真实任务。
_Avoid_: 列表内第二份编辑卡、编辑器替身、保存后重新分配身份、保存后播放独立插入动画

**创建焦点位**:
创建 Seed 离开 Dock 后直接展开到的稳定、键盘安全卡片区域；它在整个编辑会话中保持主要位置与宽度，不因草稿排序、列表变化或键盘高度的小幅变化重新选点，只有即将侵入安全区时才进行最小必要平移。
_Avoid_: 先停靠列表、按剩余空间反复居中、键盘每次变化都重新布局、跟随最终任务落点移动

**最终任务落点**:
待办草稿依据最终日期、时间和紧急状态在任务时间线中的分组与排序位置；编辑期间待办创建项不跟随它移动，背景也不显示对应任务。提交开始后该位置保持稳定，焦点任务表面随后直接收缩并进入该任务行。若落点原本不可见，景深背景只在提交后执行一次连续定位将它带入可视区，前景任务身份在此期间保持连续，不进入虚假中转点。
_Avoid_: 创建入口的中途停靠点、编辑卡位置、提交阶段继续排序、不可见落点、突然滚动、保存后另播插入动画

**创建可见区域**:
待办创建项进入文字编辑时，由窗口安全区域和键盘最终位置共同建立稳定可见窗口。卡片先按当前内容确定完整高度，键盘出现后只按系统键盘节奏整体平移，使卡片底边稳定停在键盘上方；卡片高度、内部间距、任务排序和最终任务落点都不随键盘改变。内容增长时卡片向上扩展，触及顶部安全区后才由卡内滚动承接。
_Avoid_: 卡片被键盘遮挡、先显示高卡再压缩吸附、键盘出现后瞬间跳位、按剩余屏幕空间反复居中、键盘驱动卡片高度或任务排序

**键盘协调阶段**:
创建卡的内容高度确定后，卡片与键盘使用同一系统动画节奏进入最终吸附位置；键盘只推动整张卡片平移，不触发第二次高度或内部布局动画。提交或放弃时，键盘下沉与焦点任务表面返回在同一时刻开始并重叠执行。
_Avoid_: 卡片先落定后突然跳向键盘、卡片飞行途中重算高度、等待键盘完全消失才返回、键盘变化重启主要 Morph、用固定延迟猜测键盘时序

**创建回退**:
用户放弃待办草稿时，键盘开始下沉，编辑内容在表面边界内收敛，待办创建项从创建焦点位缩成唯一可见的创建 Seed；稳定背景平面保持不变，真实 Dock `+` 继续隐藏，Seed 严格沿进入路径返回后才与真实按钮形成一个连续终点。该过程不形成真实任务。
_Avoid_: 编辑器直接消失、Seed 返回时添加按钮尚未出现、Seed 与添加按钮长时间重叠、任务行短暂插入、放弃后残留草稿

**创建提交**:
将有效待办草稿转为真实任务的事务边界；提交开始后任务身份和最终任务落点保持稳定，列表中不提前预留空行。保存成功后，邻近任务以同一视觉进度平顺让出一行空间，键盘下沉与同一待办创建项从创建焦点位收缩、移动到正在形成的最终空位同步进行，编辑内容在表面边界内反向收敛。到达后页面解除背景景深退让；失败时保持创建焦点位、原草稿和焦点舞台，不重建视觉实体。
_Avoid_: 编辑期间预留空行、空位提前跳开、卡片到达后列表才突然补位、乐观插入任务行、等待键盘完全收起后才开始卡片动画、编辑器与任务行交叉淡变、保存失败后重建草稿、提交中重新排序

## 定期任务

**周期**:
定期任务重复出现的时间范围，例如每天、每周、每月、每季或每年。
_Avoid_: 日期、目标日

**目标日**:
定期任务在当前周期内预期发生的相对日位；它从属于周期，不是独立的绝对日期。
_Avoid_: 日期、截止日期

**目标时间**:
定期任务在目标日上预期发生的可选时刻。
_Avoid_: 日期、提醒时间

## Widget

**今日范围**:
由已逾期和今天到期的未完成任务组成；未来任务和无日期任务不计入今日进度。未来任务可作为独立的“下一项”预览，但不属于今日列表。
_Avoid_: 全部未完成任务、所有任务

**小号概览 Widget**:
仅用于快速查看今日任务状况的小号桌面组件，不承接任务完成操作；点击组件进入 App。
_Avoid_: 小号任务清单、可完成小组件

**今日概览 Widget**:
统一覆盖主屏小号、中号和大号尺寸的桌面组件；小号只提供概览，中号与大号同时提供概览和任务完成操作。锁屏 Widget、控制中心 Control 和灵动岛 Live Activity 不属于此概念。
_Avoid_: 今日重点 Widget、今日清单 Widget
