# 本地持久化成功后才完成任务提交形变

用户提交待办草稿后，创建会话进入不可重复触发的提交状态，冻结任务身份与创建落点，并先使用预分配 UUID 完成本地持久化；只有写入成功，待办创建项才收缩并把视觉所有权交给真实任务行。写入失败时保留同一编辑卡、UUID 和草稿内容并恢复编辑。相比乐观生成任务行再回滚，这一选择可能让收缩动画稍晚开始，却避免向用户展示尚未成为业务事实的任务，也消除了失败路径中的任务行撤销与草稿重建。
