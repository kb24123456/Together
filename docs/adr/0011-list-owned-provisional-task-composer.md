# 待办创建项由时间线持有

待办编辑卡稳定后成为时间线中的待办创建项：它使用预分配的任务身份、参与真实列表布局并占据任务插入槽位，保存时由同一对象收缩为任务行。根层 Overlay 只承载底部创建入口到列表 seed 的短暂运动，并在目标完成布局后把视觉所有权交给时间线；相比继续使用独立悬浮编辑器，这一选择增加了列表内键盘避让与临时项排序责任，却避免了编辑器与保存后任务行之间的层级切换和替身交叉淡变。
