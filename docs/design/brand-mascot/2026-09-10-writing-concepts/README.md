# 书写形象概念对比 · 2026-09-10

最新确认：用户已认可 A4（`writing-A4-raised-eyes.png`）静态形象。已据此用原 Rive 部件制作独立短动画，见 `../2026-09-10-writing-a4-motion/README.md`；用户随后已确认短片，正式接入见 `../2026-09-10-writing-a4-integration/README.md`。下文保留概念迭代记录。

用户明确不满意上一轮已安装的手部布局，本轮只提供静态方向选择。先确认姿态和表情，再制作短动画，形象/表情/动作经用户明确认可后才继续接入。三张均非定稿、非Rive运行截图，尚未验证小尺寸或动态效果。

使用内置 `image_gen`，以当前角色截图锁定身份，以用户曾认可的三态参考图中间书写姿态为补充。保留黑色圆球、大圆手、简洁双眼及现有铅笔；不增加细手臂、嘴或独立眉毛。完整提示词、参考文件及原始生成文件映射保存在 `prompts.json`。

- A：前置双手，较克制的书写姿态。`writing-A.png`
- B：轻微偏转、双眼较不对称。`writing-B.png`；输出眼线倾斜偏强，需要用户反馈，不能视为已通过表情验收。
- C：较低的面部位置、更明显的埋头姿态。`writing-C.png`

用户选择 A，并要求眼睛更宽、更可爱，减少反派感。新增 `writing-A2-wide-eyes.png`：以 A 为唯一参考，拉长两条直眼线、减弱内收倾斜，保留舒展眼距及原有姿态。生成提示和原始文件见 `writing-A2-prompt.json`。已检查生成静帧，等待用户确认；小尺寸和动作尚未验证。原始 A 保留。

用户要求继续加宽眼线、放缓倾斜。新增 `writing-A3-gentler-eyes.png`，以 A2 为唯一参考，进一步向外延长双眼并减小倾角；仍为两条圆端直线，保留手、笔和姿态。使用内置 `image_gen`，提示及来源见 `writing-A3-prompt.json`。已检查生成静帧，待用户视觉确认；未修改 Rive 或 App，未验证动态与 App 实际尺寸。A、A2 均保留。

用户要求眼睛上移。新增 `writing-A4-raised-eyes.png`，以 A3 为唯一参考，将双眼提升到接近球面中央的位置，保留宽直线眼型及原有手部、铅笔和姿态。内置 `image_gen` 提示及来源见 `writing-A4-prompt.json`。已检查静帧；生成位移大于提示中的初始目标，不将提示数值视为实际测量结果。当前为待用户确认的静态稿，尚未进入 Rive 或 App，A3 保留。

本轮没有修改Rive、App、静态降级素材或安装真机。当前App资源仍为上一轮 `f271aa4f51dd5673f273463a6f29d1f1041f5944b214ed6d0f52a12509243691`，不代表用户认可该版本。后续不得把本页生成PNG替代可编辑的Rive部件。
