# 流泪 B：暂用动效预览

日期：2026-09-08。用户对 B 的静态精修稿反馈为“勉强接受”；本稿是暂用方向，不代表品牌定稿或动态验收通过。本轮停止静态微调，只增加可单独观看的短动效。

## 当前：流泪与球面转向

用户对原 4 秒动效反馈“还不错”，授权继续制作球面转向。本轮保留原预览，新增 `Tears_B_Turn_Preview`（`0-174151`），60 fps、576 帧、9.6 秒循环。

观看路径：同一个 `Tears B · Preview` 画板 → Animate → **Tears_B_Turn_Preview** → 时间轴左侧播放按钮。`State Machine 1` 仍播放原 4 秒片段；要看转向请直接选择新时间轴。

| 时间 | 可见过程 |
| --- | --- |
| 0–1.6 秒 | 正面睁眼，闭眼后两行泪水错开流下 |
| 1.6–3 秒 | 轻转向左侧并停顿 |
| 3–5.2 秒 | 转向右侧，回到原稿认可的偏右视角 |
| 5.2–6 秒 | 回到正面 |
| 6–7.6 秒 | 轻低头，再抬回正面 |
| 7.6–9.6 秒 | 收泪、睁眼、停顿，接回循环 |

眼睛和泪水共同附着到半径 184 的球面，转向时同时变化位置、宽度与曲率。泪流的伸长和轻微流动在球面投影前处理；起点随眼睛移动，转向中不中断流动相位。使用现有两只眼、两行泪水的六顶点曲线，无新增五官或装饰。

本轮 152 条轨道、11,495 个关键帧全部从 Rive 回读匹配。半帧采样共 1,153 个姿态，每条轮廓 192 个点，未发现自交、越球或泪眼断缝；最小球缘余量约 20.59 画板单位。曲线近似及关键帧简化的最大投影误差约 0.52 画板单位。首尾眼形一致，泪水两端均隐藏，无可见循环跳变。

四个代表姿态已用 Rive 原生渲染检查，连续播放也已在编辑器中开启检查。原 4 秒时间轴与正式 `Mascot` 状态机结构经回读比对保持不变。仍是独立评审预览，未接入 App、未新增业务输入或运行时自由转向控制；小尺寸、深色模式和真机验收不属于本轮范围。

制作数据：`motion-turn.json`；几何辅助：`build-turn-preview.py`；验证：`verification-turn-geometry.json`、`verification-turn-readback.json`；原生姿态捕获：`turn-front.png`、`turn-left.png`、`turn-right.png`、`turn-down.png`。

## 原 4 秒预览（保留对照）

打开 [Together Sphere Motion Study](https://editor.rive.app/file/together-sphere-motion-study/2561771)，选择 `Tears B · Preview` 画板，进入 Animate，选择 `Tears_B_Preview`，点击时间轴左侧播放按钮。画板位于原角色画板下方。默认 `State Machine 1` 也直接播放此时间轴。

- 画板：`0-169138`，512 × 512，透明背景，排除运行时导出。
- 时间轴：`0-169142`，60 fps，240 帧，4 秒循环。
- 过程：睁眼停顿 → 闭眼 → 两行泪水错开流下 → 泪水淡出 → 睁眼恢复。
- 泪水相差 6 帧启动，流动阶段只小幅改变轮廓；身体等比缩放不超过 0.25%，下沉不超过 1.4 画板单位，转动不超过 0.45 度。
- 仅保留两条白色闭眼轮廓与浅蓝泪流，无眉毛、嘴巴、装饰或新增肢体。

## 范围与验证

原 `TogetherSphere` 的 26 条时间轴关键帧及 `Mascot` 状态机完整结构，与本轮前快照逐项比对一致。本预览未接入 App，也未接入正式行为映射。新增画板复用其自带时间轴和默认状态机。

新增 152 条属性轨道、4,761 个关键帧全部从 Rive 读回匹配；未保留意外产生的画板位置关键帧。独立几何复核按 0.1 帧采样，未发现眼线自交或可见轮廓越出球体。首尾可见属性一致；隐藏泪流的缩放值在循环点重置，不构成可见跳变。

已在 Rive 原生编辑器中开启播放，并观察睁眼、流泪、收泪后恢复等阶段；同时检查了起始与恢复位置。此检查不代替用户对可爱程度、节奏的认可，也不包含小尺寸、深色模式、Apple runtime、真机性能或导出文件验收。

## 文件

- `reference-provisional.png`：用户勉强接受的静态参考，保留作比较。
- `motion.json`：当前对象 ID、几何定义与关键帧制作数据，不是可导入的 Rive 文件或运行时资源。
- `verification.json`：本轮回读与范围检查摘要。
