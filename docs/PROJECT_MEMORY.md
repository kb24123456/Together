# Together Project Memory

> Codex 项目记忆入口。只记录长期有效、可被未来任务复用的事实。不要记录密钥、token、隐私数据或临时噪声。

## 当前状态

- 日期：2026-08-16
- 项目路径：`/Users/papertiger/Desktop/Together`
- Git 根目录：`/Users/papertiger/Desktop/Together`
- 产品主轴：iPhone-only 的纯单人 Todo 效率工具。
- 当前策略：彻底移除双人协作、多人预留、Supabase、RevenueCat、Paywall、PremiumGate；本地 SwiftData 是数据真相源，CloudKit private database 是唯一跨设备同步/恢复能力。
- 新增主能力：OCR 扫描导入纸面笔记，先生成任务草稿，用户确认后再写入 SwiftData。
- Chronicle：暂不开启；先使用项目文档、AGENTS 和 Skill 机制承接记忆。

## 当前进行中交接

- 分支：`main`
- 当前任务：2026-08-13 普通待办“关注任务 -> 单实例 Live Activity”已实现并完成 Simulator 编译与针对性测试，待真机验收锁定屏幕、Dynamic Island 和系统权限切换。
- 已确认边界：
  - 不迁移旧 Supabase 数据，允许删除。
  - 不保留 RevenueCat、Paywall、PremiumGate、Supabase webhook。
  - CloudKit 作为唯一跨设备同步/恢复能力，本地 SwiftData 必须保留。
  - OCR 第一版接受“识别 -> 草稿确认 -> 入库”。
- 当前结果：
  - 2026-08-16 首页待办日期栏与系统 `.soft` 边界最终交接：iOS 26+ 将当前日期放入原生 `ScrollView.safeAreaBar(edge: .top, spacing: 0)`，日期仍位于 Toolbar 下方左侧且不进入 Toolbar；系统因此自动把 `.soft` 清晰度边界扩展到日期栏底部。首组不再在列表重复绘制日期，后续日期标题作为真实分组交接来源随内容滚动，到达日期栏下边界时切换 `HomeTimelineDateBarSelection`；来源标题在栏后只隐藏显示、保留真实高度，避免双显和任务跳位。Header 几何只转换为“是否越界”的 `Bool`，仅跨阈值时更新状态，不发布连续坐标、滚动方向或自定义模糊参数；iOS 18–25 继续使用原生 pinned Section Header 降级。关键文件：`HomeView.swift`、`HomeMorphSessionTests.swift`、`PRODUCT_SPEC.md`、`DESIGN_GUIDELINES.md`、`CONTEXT.md`；项目 `xcode27-swiftui-modernization` skill 已补充“连续滚动几何不得进入根 Observation”的防回归规则。验证未启动 Simulator：Swift parse、`git diff --check`、Xcode 27 beta 3 generic iOS 无签名 App build 通过；完整 `build-for-testing` 仍被既有 `TogetherTests.swift:3608` actor/protocol 隔离错误阻断，临时排除既有失败源后的聚焦测试目标编译链接通过。仍需真机验收初始日期清晰度、`.soft` 起点、上下滑跨日期交接、快速反向、Dynamic Type、VoiceOver 及 60/120Hz 帧时间。
  - 2026-08-16 首页顶部滚动与模式切换最终方案（取代同日曾尝试的系统 segmented Picker、固定 Header 几何测量和动态 contentMargins）：待办与定期顶部只启用系统 `.scrollEdgeEffectStyle(.soft, for: .top)`，`TaskEdgeFlowPolicy` 删除全部顶部 offset / scale / blur / opacity / 遮没逻辑，仅保留底部向 Dock 汇聚、压缩、淡出和深层禁用命中；对应测试明确验证顶部越界任务仍为 identity。Toolbar principal 的普通字号切换器改为无轨道、无胶囊、无玻璃、无指示器的两个等宽 Button，总体 `144 × 44pt`、单项 `72 × 44pt`；选中项主文字色 + Semibold，未选中项 tertiary + Regular，按压期缩放至 `0.94`、透明度降至约 `0.82`，释放使用约 180ms 轻弹簧；真实模式改变仍只触发一次既有触觉和页面级转场。辅助功能字号继续使用原生 Menu，Reduce Motion 取消缩放 / 弹簧 / 空间运动，只保留必要透明度反馈；该 Reduce Motion 取舍后续作为默认，不再重复确认，除非与新需求冲突。ToolbarContent 级 `.sharedBackgroundVisibility(.hidden)` 保留，头像保持 40pt 视觉 / 44pt 命中。待办日期继续是 `LazyVStack(pinnedViews: [.sectionHeaders])` 的唯一原生 Section Header，不绘制背景遮罩、Material、卡片、阴影，不引入几何跟踪或日期副本；其顶部过渡仅由系统 `.soft` 负责。定期页固定维度 Header 改由任务 `ScrollView.safeAreaInset(edge: .top, spacing: 0)` 原生占位并使用同色全宽实底，删除 Header 高度测量、`ZStack` 覆盖和动态 `.contentMargins`；Morph 顶部保护边界直接合并 `ScrollGeometry.contentInsets.top`，避免首帧零高度重叠。关键文件：`AppRootView.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`TaskMorphContainer.swift`、`TaskEdgeFlow.swift`、`HomeMorphSessionTests.swift`、`TaskEdgeFlowTests.swift` 及三份根规范。验证未启动 Simulator：相关 Swift parse、残留搜索、`git diff --check` 通过；Xcode 27 beta 3 generic iOS 无签名 App build 与排除既有失败测试源后的聚焦 `build-for-testing` 均 exit 0。完整测试目标仍被既有 `TogetherTests.swift:3608` actor/protocol 隔离错误阻断，未执行测试用例；需真机验收切换器按压 / 切换、日期 Header 原生推替、定期首任务初始位置、上滑 `.soft`、Morph 顶部边界、Dynamic Type、VoiceOver、Reduce Motion 与 60/120Hz 帧时间。
  - 2026-08-16 首页日期标题最终回退为原生 Section Header 吸附：待办 / 定期切换器继续固定在系统 Toolbar 中间，40pt 圆形头像继续位于右侧；日期不再进入 Toolbar。待办时间线恢复 `LazyVStack(pinnedViews: [.sectionHeaders])`，所有日期组（含首组）只拥有一份真实 Header，滚到 Toolbar 下方的滚动区域顶部左侧后由系统原地吸附，下一组由原生容器自然顶走上一组。日期继续使用 22pt 主标题、12pt 星期 / 数量首基线布局，任务完成或新建时仅数量执行 `.numericText` 转场。已删除 `HomeTimelineToolbarContext`、Toolbar 左侧日期视图、Header 越界几何上报、额外滚动坐标空间及四项对应状态测试；不保留滚动方向、阈值或日期副本。顶部逾期 / 定期临近提示继续移除，`RoutinesSummaryCard` 保持删除。关键文件：`AppRootView.swift`、`HomeView.swift`、`HomeMorphSessionTests.swift`、`PRODUCT_SPEC.md`、`DESIGN_GUIDELINES.md`、`CONTEXT.md`。验证未启动 Simulator：相关 Swift parse、残留搜索通过；Xcode 27 beta 3 在独立临时 DerivedData 下完成 generic iOS 无签名 App 构建（exit 0），仅报告仓库既有 actor-isolation、NSLock 与废弃 API warnings。仍需真机验收 Header 吸附 / 组间顶替、数量动画、Dynamic Type 及 60/120Hz 滚动帧时间。
  - 2026-08-14 任务列表与详情几何收敛：待办与定期普通任务行的垂直外部间距统一为 `8pt`，相邻单标题任务保留约 `16pt` 内容空白，44pt 命中区不变；详情卡与相邻行的真实外部呼吸间距同步为 `8pt`。待办详情的主任务标题、子任务标题、折叠属性摘要和底部属性栏共用 `38pt` 文字左轴，定期详情的任务标题、备注入口和属性栏共用 `54pt` 左轴。详情表面继续保持水平 `28pt` 内边距，垂直改为顶部 `18pt`、底部 `20pt`，内部行距统一为 `4pt`，修正顶部偏空。外壳过冲仍只改变绘制范围与阴影，圆角在可见的展开、过冲、落定与收起全过程固定为 `45pt`，不再执行 `0 -> 47 -> 45` 的圆角插值。关键文件：`TaskMorphContainer.swift`、`HomeView.swift`、`HomeInlineTaskDetailCard.swift`、`RoutinesListContent.swift`、`RoutinesTaskRow.swift`、`HomeMorphSessionTests.swift`；产品、工程、设计和术语规范已同步。验证未启动 Simulator：相关 Swift parse、`git diff --check`、Xcode 27 beta 3 generic iOS 无签名 App build，以及排除既有失败源 `TogetherTests.swift` / `SharedSchemaCompatibilityTests.swift` 后包含新几何断言的聚焦测试目标编译链接均通过。完整测试目标仍被既有 `TogetherTests.swift:3608` actor conformance 错误阻断，未运行测试用例；仍需真机验收长短标题、零/多子任务、待办/定期、快速反向、Reduce Motion 与不同 Dynamic Type 下的间距和圆角观感。
  - 2026-08-14 Morph 与边缘流场交接跳闪修复：真机视频逐帧确认，展开前和收起后的顶部 / 底部任务标题跳动并非列表身份或 `TaskMorphContainer` 架构冲突，而是 `TaskEdgeFlow` 由 `morphSession.isActive` Bool 控制：预挂载会话在无动画事务中先把流场瞬切为恒等值，正式展开需等待两个 render turn；收起完成后 session 又在无动画事务中瞬间恢复流场。现将共享策略改为 `0...1` 连续 intensity，所有 offset、scale、opacity 与顶部 blur 按强度插值；待办与定期列表统一改由 `isFocusDepthActive` 驱动，使预挂载与最终 session 清理不改变目标值，只有正式展开 / 收起事务连续切换强度。深层遮没只在完整流场强度下停止命中与辅助功能聚焦，交接中不产生不可见命中状态滞留；真实行高、任务身份、Viewport Driver 和 Morph 外壳均未修改。新增流场中间态插值测试及 session 预挂载 / 展开 / 收起时序断言。验证未启动 Simulator：相关 Swift parse、`git diff --check`、Xcode 27 beta 3 generic iOS 无签名 App build，以及临时排除既有失败源 `TogetherTests.swift` / `SharedSchemaCompatibilityTests.swift` 后的聚焦测试目标编译链接均通过；完整测试目标仍被既有 `TogetherTests.swift:3607` actor conformance 错误阻断，未运行测试用例，仍需真机复验顶部 / 底部首尾帧、快速反向、待办 / 定期和 Reduce Motion。
  - 2026-08-14 真机反馈修正（其中圆角参数已由上方“任务列表与详情几何收敛”取代；其余部分取代下方同日“轻 bounce 外壳”、底部模糊与日期提前接管的初版参数）：`TaskEdgeFlowPolicy` 在普通与 Reduce Motion 模式下都将底部 blur 固定为 0，只保留向 Dock 汇聚、轻微压缩、淡出与深层遮没，顶部 `.soft` / 轻 blur 不变。`HomeTimelineChromeState` 的接管边界改为 0，且 `HomeView` 直接采样日期标题本体而不是含 8pt padding 的 Header 外框；首次接管和相邻日期切换都必须等标题可见上边缘真实触碰列表顶边，18pt 上滑 / 24pt 下滑意图阈值保留。详情真实行高、内容、外部间距和 viewport 继续由约 350ms 无过冲主进度驱动；外壳新增可取消的 `.compact / .overshot / .settled` 局部视觉阶段，约 380ms 时向左右各多延展 4pt、向下多延展 12pt，圆角 / 阴影达到 47pt 与 18pt/y8pt，再用 140ms 阻尼回落到 45pt 与 16pt/y7pt，总落定约 520ms。内容与真实布局不参与过冲；收起从当前呈现值直接改向紧凑端点，不先外扩，过冲等待任务会随目标变化取消；Reduce Motion 直接使用 140ms settled/compact 转换，无过冲、无新增落定触觉。关键文件：`TaskEdgeFlow.swift`、`HomeView.swift`、`TaskMorphContainer.swift`、`TaskEdgeFlowTests.swift`、`HomeMorphSessionTests.swift`；产品、工程、设计与词汇表同步更新。验证未启动 Simulator：相关 Swift parse、`git diff --check`、Xcode 27 beta 3 generic iOS 无签名 App build、临时排除 `TogetherTests.swift` 与 `SharedSchemaCompatibilityTests.swift` 后的聚焦 `TogetherTests` 真机目标编译链接均通过。完整测试目标仍被既有 `TogetherTests.swift:3607` actor 对全局 actor-isolated protocol 的 conformance 错误阻断，未运行测试用例；仍需真机验收过冲可见度、快速开合改向、长短详情、待办/定期、日期触边时机、底部无模糊、Reduce Motion 与 60/120Hz 帧时间。
  - 2026-08-14 历史初版（其外壳参数已由上方真机反馈修正取代）：任务详情展开改为“锚定表面绽放”，待办与定期任务继续复用列表拥有的唯一 `TaskMorphContainer`、真实行高和 UIKit Viewport Driver，不新增 Overlay 或动态模糊。共享 `TaskMorphBloomMotion` 使用约 350ms、无过冲的主进度；横向外边界先建立，纵向详情、真实外部间距与 `UIScrollView.contentOffset` 随后单调推进，避免列表跟随表面弹性回摆。只有圆角表面背景、描边与阴影从同一展开目标派生约 420ms / 轻 bounce 的一次收束；任务身份区保持稳定，定期任务内部揭示已移除独立位移、缩放和弹簧。收起阶段再次点击当前任务会使 session revision 前进并从当前 presentation 反向展开，旧收起回调在清理详情前检查 token，不能误清除已恢复会话；需要重排的详情反向时恢复来源 placement，下一次收起仍能正确判断 relocation。Reduce Motion 保留 140ms 无弹性高度与淡变，并取消表面收束、背景缩放。关键文件：`TaskMorphContainer.swift`、`HomeMorphSession.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`RoutinesTaskRow.swift`、`AppRootView.swift`、`HomeMorphSessionTests.swift`；术语与规则同步到 `CONTEXT.md`、`DEVELOPMENT_GUIDELINES.md`、`DESIGN_GUIDELINES.md`。验证未启动 Simulator：Swift parse、`git diff --check` 通过；标准 generic build 被本机缺少 iOS 26.5 platform 阻断，改用 iPhoneOS 26.5 SDK、独立临时 build root 并临时排除 `Assets.xcassets` 后，App target 完整编译链接成功；同条件下 Debug `TogetherTests` target 完整编译链接成功，未执行测试用例。
  - 2026-08-14 历史初版（其底部模糊参数已由上方真机反馈修正取代）：待办与定期任务共用“边缘流场滚动”。真实任务行继续使用原生 `ScrollView` 惯性和稳定布局，只通过共享 `TaskEdgeFlowModifier` 的 `visualEffect` 按其在滚动视口中的位置改变 offset、轻量 scale、blur 与 opacity；中央阅读区保持恒等变换，顶部只收束淡出，底部向 Dock 下方的汇聚点非线性压缩。连续几何不写入 Observation 或列表状态，只有深层遮没阈值跨越时才更新行内 Bool，使不可读任务停止命中并退出 VoiceOver 顺序；浅层任务仍可读可点。Morph 活跃时流场回到恒等变换，Reduce Motion 取消位移与压缩，只保留平滑清晰度过渡。日期 Header、周期维度、首页状态和归档入口未接入。关键文件：`Together/Features/Shared/TaskEdgeFlow.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`TogetherTests/TaskEdgeFlowTests.swift`；规则同步到 `CONTEXT.md` 与 `DESIGN_GUIDELINES.md`。验证未使用 Simulator：Swift parse、`git diff --check`、Xcode 27 beta 3 generic iOS 无签名 App build 通过；临时只排除工作区既有失败源 `TogetherTests.swift` 与 `HomeMorphSessionTests.swift` 后，包含 6 个新增策略测试的 `TogetherTests` 产品完整编译链接且 `TEST BUILD SUCCEEDED`。未执行测试用例。
  - 2026-08-14 单人头像重装 / CloudKit 恢复链路修复：启动时 `AppContext.restorePersistedUserProfileIfNeeded()` 不再只把当前 `User` 原样写回，而是调用 `LocalUserProfileRepository.mergedUser(_:)`，从 `PersistentUserProfile` 合并资料并用 SwiftData 的 external-storage 头像 payload 重建 Application Support 下的本地头像缓存；每次成功 CloudKit import 及现有两次收敛重试都会强制重新合并，覆盖头像 blob 晚于资料元数据到达的情况。头像文件暂时不存在但仍有明确 `avatarAssetID` 时，不再把远端头像元数据误判为删除；只有资产引用也已清除时才清空文件名。关键文件：`AppContext.swift`、`LocalUserProfileRepository.swift`、`TogetherTests.swift`。新增测试覆盖启动合并、CloudKit 导入三次合并与缺失本地文件时保留远端资产元数据。验证未使用 Simulator：相关 Swift parse、`git diff --check` 通过；Xcode 27 beta 3 对连接真机的完整 Debug 签名构建成功，临时排除 `.xcassets` 后 `TogetherTests` target 也完整编译、链接并 `BUILD SUCCEEDED`。修复版已按相同 `com.pigdog.Together` / Build 46 覆盖安装，未卸载或清除容器；系统因真机尚未显式信任当前 Apple Development 证书而拒绝启动。需用户在“设置 -> 通用 -> VPN 与设备管理”完成信任后再启动并验收头像恢复。本机只有 Apple Development 私钥；虽有 Ad Hoc profile，但缺少对应 Apple Distribution 私钥，未擅自创建、撤销或更改证书。
  - 2026-08-14 恢复真机单 App 工作流：按用户明确选择撤销 Debug / 正式包隔离。Debug、Release 与 TestFlight 重新统一使用 `com.pigdog.Together`、`com.pigdog.Together.TogetherWidget`、`together://`、`group.com.pigdog.together.shared`、`iCloud.com.pigdog.Together` 及正式 entitlements；工程不再生成或引用 `Together Dev`、`.dev` Widget、独立深链、App Group 或 CloudKit 容器，两份 Debug 专用 entitlements 已删除。这样可在同一 App 与同一数据身份中测试并日常使用；接受 Xcode Development 构建会覆盖同 Bundle ID 的 TestFlight 二进制、且 PPQ 在线验证失败时唯一安装的 Together 可能暂不可用这一系统约束。Apple Developer 后台此前生成的 `.dev` identifier / profile 未被工程引用，未执行外部删除。验证未使用 Simulator：plist / entitlements lint、相关 Swift parse、`git diff --check` 以及 Debug / Release 主 App与 Widget `-showBuildSettings` 身份对照均通过；generic iOS Device build 在选取 destination 时被本机缺少 iOS 26.5 platform 阻断，未进入编译。
  - 2026-08-13 首页顶部与状态入口收敛：待办 / 定期切换改为系统 `Picker(.segmented)`，保留原生轨道、选中态和辅助功能语义，不在外层增加玻璃壳、描边或第二层容器；超大 Dynamic Type 仍退化为系统 `Menu`。顶部主控行约 44pt，切换器居中、头像靠右，不为日期常驻预留空槽。待办页在首个日期分组前按需显示无容器状态入口：“逾期”只给警示图标和数量使用系统红，“定期临近”保持中性；同时存在时横向并列，宽度不足时由 `ViewThatFits` 转为纵排，两项各自跳转逾期汇总或定期待处理周期，不吸附且不在定期页重复。日期分组保留 `LazyVStack(pinnedViews: [.sectionHeaders])` 的单一原生 Header 身份和约 44pt 透明布局高度，仅显示文字；Header 自有背景、Material、网格补绘、遮罩、分割线以及额外透明度 / 位移动画全部删除，下一日期完全依赖系统原生推替。首页只使用系统 `.soft` scroll edge effect 形成顶部连续过渡，不硬编码模糊高度或渐变遮罩。关键文件：`AppRootView.swift`、`HomeView.swift`、`RoutinesSummaryCard.swift`；规范同步到 `PRODUCT_SPEC.md`、`DESIGN_GUIDELINES.md` 和 `CONTEXT.md`。验证：相关 Swift parse 与 `git diff --check` 通过；当前 Xcode 26.6 的 scheme destination 服务仍报“iOS 26.5 is not installed”。改用 iPhoneOS 26.5 SDK 直接构建 target 后可进入工程构建，但先被资产编译器要求匹配的 26.5 Simulator runtime 阻断；临时排除 `.xcassets` 后可进入 Swift 编译，随后被现有 `TogetherCore` checkout build 目录的诊断文件写权限阻断，未达到完整链接。本轮按用户要求未启动或操作 Simulator；顶部材质曲线、日期吸附 / 推替和 Dynamic Type 由真机验收。
  - 2026-08-13 普通待办关注与 Live Activity：领域 `Item` 增加 `isFollowed / followedAt`，只允许未完成、非定期待办关注；完成会清除关注，重新打开不恢复。SwiftData V3 使用一对一 `PersistentTaskFollow` companion，而不是直接修改历史 `PersistentItem`，避免 V2/V3 复用同一 `@Model` 时出现 duplicate version checksum 崩溃；`TogetherCore` 的既有模板删除 V2 与关注 V3 已分别提交，发布并推送 `1.1.0`，主工程依赖已升级。`TaskFollowActivityCoordinator` 是唯一 ActivityKit 生命周期入口：全 App 最多一个 Activity、异常重复实例立即收口、最多传 3 个按 `followedAt` 倒序的轻量 snapshot，0 项立即结束；系统移除或到期后 active 不自动重建，仅显式关注变更或权限 false -> true 时可创建。共享 `CompleteFollowedTaskIntent: LiveActivityIntent` 同时属于 App 与现有 Widget target，在 App 进程调用统一 `completeTask` 后 reconcile；锁定屏幕与 Dynamic Island 提供完成按钮和 `together://task/<UUID>` 深链。详情卡使用纯 `scope` 圆形图标、34pt 可见 / 44pt App 内命中区和 VoiceOver 状态；系统实时活动关闭时关注仍保存并显示非阻塞提示。关键文件：`TaskFollowShared/TaskFollowActivityModel.swift`、`Together/LiveActivity/*`、`TogetherWidget/TaskFollowLiveActivity.swift`、`LocalItemRepository.swift`、`DefaultTaskApplicationService.swift`、`HomeInlineTaskDetailCard.swift`、`AppContext.swift`、`TogetherSchemaV1.swift`。验证：`TogetherCore swift test` 7/7；Xcode 27 beta 3 generic iOS Simulator Debug `build` / `build-for-testing` 与 Release `build` 通过；主 `TogetherTests` suite 139/139、V3 schema compatibility 3/3 通过；全套 155 项只有工作区既有 `HomeMorphSessionTests.expandedListSpacingAddsRealSeparationAroundActiveCard` 的 3 个间距断言失败（实现 6/6/76，测试仍期望 12/12/40），与本功能无关。尚未真机验证锁屏按钮、Dynamic Island 各尺寸、权限关闭/重开、用户手动结束后不续建、VoiceOver 与 payload 在真实系统宿主中的呈现。
  - 2026-08-12 首页列表视觉精简：导航主行位置和高度保持不变，待办日期分组继续使用同一 `LazyVStack` pinned header 身份，到达顶部后成为顶部功能区第二行；吸附背景使用经背景校色的超薄材质，通过纵向 mask 向列表连续渐隐并延伸到标题下方，不再绘制整块 Material 或底部 hairline。iOS 26+ 首页滚动边缘使用系统 `.soft` 顶部过渡，未复制日期标题或将其写入 `UINavigationBar` 内部。顶部待办/定期改为两个原生 `Button` 组成的轻量切换器，整组不绘制轨道、外围玻璃、描边或阴影，只给选中项低对比中性实色胶囊；超大动态字体仍退化为当前模式 `Menu`。头像改为 38pt 单一圆形图像、0.5pt 描边和 44pt 命中区。首页和定期任务中文及一般 UI 改用 Apple 系统字体，独立日期、时间、计数和进度数字显式保留 rounded design；标题为 17pt semibold、列表最多两行且不缩放/不跑马灯，详情与辅助字号放宽行数；备注与属性统一为 14pt medium。行水平 inset 保持 28pt，垂直 inset 收敛为 10pt，标题与次级信息间隔 4pt，标题交互区仍至少 44pt。主任务 24pt / 子任务 20pt 完成框改为低对比细实线，背景渐变更中性、网格间距改为 44pt 且降低对比；定期列表、两类创建卡同步同一排版和完成框规则。7 个不再注册或引用的 Resource Han Rounded 字体文件已从资源中移除，干净构建产物不再携带约 97MB 冗余字体。关键文件：`AppTheme.swift`、`GradientGridBackground.swift`、`AppRootView.swift`、`HomeView.swift`、`HomeInlineTaskDetailCard.swift`、`HomeTaskCreationCard.swift`、`PeriodicTaskCreationCard.swift`、`RoutinesListContent.swift`、`RoutinesTaskRow.swift`、`Info.plist`，规范同步到 `PRODUCT_SPEC.md`、`DESIGN_GUIDELINES.md` 与 `CONTEXT.md`。验证：`plutil -lint`、相关 Swift parse、`git diff --check` 和 Xcode 27 beta 3 generic iOS 无签名 build 通过；视觉、交互、滚动吸附、Dynamic Type、VoiceOver、Reduce Motion 与 60/120Hz 表现按用户要求由真机验收，后续默认不再使用 Simulator，除非用户在具体任务中重新明确要求。
  - 2026-08-11 已有待办详情完成统一日期时间编辑：`DateTimePickerSheet` 使用不透明实色表面、自定义固定六周的七列月历、珊瑚色选中态、上月 / Today / 下月导航，以及同一 Sheet 内的时间与提醒行；无标题栏、完成按钮、Toggle、Material、Liquid Glass 或自定义时间滚轮。未设置时间时“添加时间”建立按 5 分钟取整的 draft 时间；设置后由原生 SwiftUI `.compact` `DatePicker` 直接触发系统时间选择器，字段右侧的唯一清除图标按钮立即清除时间与提醒，不保留长按清除操作。月份标题和导航保持单行，空间不足时 Today 退化为日历图标；月份和年份使用方向一致的原生数字内容转场，Reduce Motion 下直接更新。Sheet detent 按固定六周内容紧凑计算，圆角交回系统 presentation 自适应。提醒使用同页系统 `Menu`，没有具体时间时禁用；日期、时间、提醒提前量通过 `HomeViewModel.updateDraftSchedule(date:time:reminderOffset:)` 原子写回当前 `TaskDraft`，清除时间同步清除提醒。独立 `CustomTimePickerSheet` 提供大号时间、横向刻度拖动、5 分钟步进、轻触觉反馈和停手吸附，但仅有本地 Preview 引用，未接入正式任务编辑。关键文件：`TaskEditorSharedComponents.swift`、`CustomTimePickerSheet.swift`、`HomeInlineTaskDetailCard.swift`、`HomeViewModel.swift`、`TogetherTests.swift`。验证：Xcode 27 beta 3 generic iOS 无签名 build 通过；iOS 27 Simulator focused tests 5 / 5 通过；正式详情入口、月历窄屏布局、添加时间、系统 compact 时间 Popover 和提醒菜单已在 iPhone 17 Pro Simulator 检查。完整 `TogetherTests` 为 152 / 153，唯一失败是工作区先前未提交的详情 Morph 水平 inset 改动使 `expanded.leading` 为 6 而既有测试仍期望 12，与日期时间改动无关；本轮 follow-up 按用户要求只需最终构建，不运行测试或 Simulator，仍需真机检查 12/24 小时制、Dynamic Type、VoiceOver、Reduce Motion 与清除图标命中手感。
  - 2026-08-09 首页创建最终落位改为双阶段视觉交接：真机验证确认 ADR-0022 的 Overlay 跨层飞入列表会在同一时间窗叠加目标分组切换、无动画滚动、`LazyVStack` 重排、目标 Frame 回传和 CGRect 插值，造成明显卡顿。ADR-0023 取代该最终落点，但保留 Dock 到根级编辑卡的短 Hero、稳定 UUID、失败保留 Draft 和现有详情 Morph。保存成功后 Overlay 在原位用约 220ms 将高度收窄到 1pt 并同步消融内容、表面和阴影；完成后以无动画 transaction 切换最终日期/周期并释放冻结列表。目标行先以高度零建立，无动画定位完成后，仅该行测量一次自然高度，并用约 300ms 的 `height 0 -> natural / opacity 0 -> 1 / scale 0.985 -> 1` 本地动画推动相邻行。普通行没有新增 Geometry 或动画状态，旧 `landingTargetFrame / landingProgress / target capture / reservation / Overlay landing` 路径已删除；revision token 继续拒绝过期完成回调。关键文件：`docs/adr/0023-two-stage-creation-dissolve-and-list-reveal.md`、`AppRootView.swift`、`HomeMorphSession.swift`、`TaskMorphContainer.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`HomeMorphSessionTests.swift`。验证：修改 Swift 文件 parse、旧 landing 路径残留搜索、`git diff --check` 通过；使用 iPhoneOS 26.5 SDK、无签名并临时排除 `.xcassets` 后，App 与 TogetherTests target 均真实 Swift 编译和链接且 `BUILD SUCCEEDED`，仅报告仓库既有 warnings。按用户要求未运行模拟器、测试用例、UI 测试或 Instruments；消融与长出的视觉间隔、屏外/跨分组定位和 60/120Hz 帧率由用户真机验收。
  - 2026-08-09 历史阶段（已由 ADR-0023 取代）首页创建曾按 ADR-0022 使用目标行隐藏 reservation、全局 Frame 捕获和 Overlay 跨层落点；该路径真机卡顿，不代表当前代码。
  - 2026-08-09 首页详情纵深与收起命中范围补齐：非活跃任务既有 `0.94` 等比合成缩放 / `0.62` 不透明度参数现由共享 `taskMorphBackgroundDepth` 同步应用到顶部模式控件、头像按钮、待办日期分组标题、已完成标题和定期周期固定区；不增加模糊、几何测量或逐帧状态。展开态下这些可见区域临时覆盖单一“保存并收起”命中层，第一次点击不同时切换模式、打开个人页或切换周期；VoiceOver 只暴露收起动作。系统状态栏与导航栏空白区域继续由系统持有。验证：`git diff --check` 与 Xcode 27 beta 3 generic iOS 无签名 `build` 通过，仅报告仓库既有 actor-isolation warnings；按用户要求未运行模拟器、UI 测试或 Instruments，Toolbar 宿主中的缩放连续性与命中范围由用户真机验收。
  - 2026-08-09 首页详情单实体表面修正：真机关键帧确认旧 `TaskMorphDepthLayer` 的反向圆角 aperture 与真实 `TaskMorphContainer` 分别使用布局 Frame 和动画 presentation progress，动画途中无法逐帧严格重合，暗化平面因此把透明镂空暴露为第二个圆角矩形。现已删除 `TaskMorphDepthCoordinator / TaskMorphDepthView / TaskMorphDepthLayer`、根层暗化视图、活动卡 Frame 上报及 aperture progress 分支；动画中只由真实 `TaskMorphContainer` 绘制一套背景、描边、圆角、裁切和阴影。紧凑态裁切半径保持为零，避免虚线完成框在展开端点出现缺口。卡内内容改为显式 X / Y 同值的 `1.05` 合成缩放，非活跃任务使用 `0.94` 等比缩放和 `0.62` 不透明度；展开表面使用水平 `20pt`、垂直 `16pt` 基础内边距，活动行提升列表层级并配合略增强的单卡阴影，Reduce Motion 取消缩放。UIKit 每帧只保留既有 O(1) `UIScrollView.contentOffset` 写入，不再计算 mask path 或同步第二套几何。ADR-0021、`PRODUCT_SPEC.md`、`DEVELOPMENT_GUIDELINES.md` 与 `DESIGN_GUIDELINES.md` 已同步。验证：残留搜索、`git diff --check` 与 Xcode 27 beta 3 generic iOS 无签名 `build` 通过，仅报告仓库既有 actor-isolation warnings；按用户要求未运行模拟器、UI 测试或 Instruments，单矩形连续性、二维缩放可见度和 60/120Hz 由用户真机验收。
  - 2026-08-09 历史阶段（已由上一条单实体修正取代）首页详情曾保留单个 5% 黑色暗化平面与反向圆角 mask；该 aperture 路径因与真实卡片 presentation geometry 不同步而被删除，不代表当前代码。
  - 2026-08-09 首页详情 Morph 打开/关闭对称性与相邻呼吸间距修正：真机关键帧确认旧打开路径在同一 transaction 中同时执行“非活跃透明行 -> 活跃实体卡”和 `.compact -> .expanded`，使背景、阴影与详情整体淡入；关闭却在活跃所有权不变时只收缩几何，因此两端不对称。现有任务打开先由 `HomeMorphSession.prepareExpansion` 无动画建立同一 UUID 的活跃 `.compact` 容器并预挂载零高详情，随后由 revision token 守卫的下一渲染回调执行 `activatePreparedExpansion`；关闭继续用同一 spring 反向到 `.compact` 后才释放所有权。`TaskMorphContainer` 的内容身份在准备态已经建立；实体表面现由后续景深阶段改为随展开进度连续显现，详情继续通过边界裁切揭示，不整卡淡入。列表 placement 不再用减少外部 padding 抵消卡片内边距，改为活动容器自身增加 10pt、真实计入 `LazyVStack` 行高的 top / bottom 呼吸间距；相邻行没有独立 offset。逐帧仍只有活动行 `CGFloat` 插值和至多一次 O(1) `UIScrollView.contentOffset` 写入，不新增 `CADisplayLink`、列表级 Observable progress、排序或 IO。关键文件：`HomeMorphSession.swift`、`TaskMorphContainer.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`HomeMorphSessionTests.swift`、ADR-0021。验证：`git diff --check`、Xcode 27 beta 3 generic iOS `build` 与 `build-for-testing` 通过；按用户要求未运行模拟器、UI/单元测试或 Instruments，展开首帧、严格逆向观感、相邻间距幅度和 60/120Hz 帧率仍由用户真机验收。
  - 2026-08-09 首页详情双向外撑根因修正：真机关键帧证明“真实行高 + `onGeometryChange` 反推 presentation height + 声明式 `ScrollPosition.scrollTo`”并不共享确定的逐帧提交时序，实际仍退化为只向下展开。现由活动行局部 `AnimatableModifier` 提供唯一显式 progress `p`；`TaskMorphContainer` / `LazyVStack` 继续持有完整真实高度 `ΔH`，窄 UIKit `TaskMorphScrollViewResolver` 只解析底层 `UIScrollView`，`TaskMorphViewportCoordinator` 在同一进度下直接执行无附加动画的 O(1) `contentOffset = openingOffset + U × p`，使上方实际移动 `U × p`、下方实际移动 `(ΔH-U) × p`，收起用同一进度严格反向。逐帧值不进入首页级 `@State` / `@Observable`，不广播所有行、不使用 `CADisplayLink`，稳定 UUID、`LazyVStack` 懒加载和 pinned header 保留；新增纯几何测试覆盖 40/60 分配与顶部空间钳制。ADR-0021、`PRODUCT_SPEC.md` 与 `DESIGN_GUIDELINES.md` 已同步。关键文件：`TaskMorphContainer.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`HomeMorphSessionTests.swift`。验证：Xcode 27 beta 3 generic iOS `build` 与 `build-for-testing` 通过，`git diff --check` 通过；按用户要求未运行模拟器、UI 测试、测试用例或 Instruments，列表顶 / 中 / 底、展开中手势干预和 60/120Hz 帧率由用户真机验收。
  - 2026-08-09 首页列表原地 Morph 完成运行时迁移。ADR-0021 取代 ADR-0020；`HomeMorphSession` 统一仲裁待办 / 定期 Draft 与已持久化对象，并显式区分 `heroEntering / active / saving / collapsing / relocating`。`TaskMorphPlacement` 同时保留临时与最终 section，确保创建和详情先在原槽位收缩，再单独释放到最终日期、紧急排序或周期。待办 Draft 固定在当前日期分组首位，定期 Draft 固定在当前周期首位，属性编辑不移动槽位；创建从第一次点击起复用最终 UUID。`AppRootView` 恢复纯 SwiftUI `ZStack`，仅 `HomeHeroTransitionLayer` 使用 Dock 与 Seed 的显式全局 Frame；空态、深链和 Reduce Motion 直接进入编辑态。已删除 `HomeFocusPresentationController / Coordinator / Model / SurfaceView / Session / GeometryRegistry / MotionProfile / HomeRootContainer`，不保留 Feature Flag 或 fallback；SwiftData、CloudKit schema 与应用服务未变。关键文件：`Features/Home/Morph/HomeMorphSession.swift`、`TaskMorphContainer.swift`、`HomeView.swift`、`RoutinesListContent.swift`、`AppRootView.swift`。新增 `HomeMorphSessionTests` 覆盖稳定 UUID、单会话、Hero 交接、保存失败、收缩后重排、第二任务点击和过期回调。验证：Xcode 27 beta 3、iPhone 17 Pro iOS 26.4.1 Simulator 下 `TogetherTests` 145 / 145 通过，`build-for-testing` 与 `git diff --check` 通过；构建只报告仓库既有 Swift 6 actor-isolation、异步 `NSLock` 和 deprecated API warnings。UI、真机、Dynamic Type、VoiceOver、Reduce Motion / Transparency 和 Instruments 性能验收未在本轮执行，由用户接手。
  - 2026-08-09 历史阶段（已由 ADR-0021 与上条迁移取代）首页曾使用唯一根级 `HomeFocusSession`、UIKit `HomeFocusPresentationCoordinator` 和前景 Host，并在焦点期间冻结列表、缩放 / 模糊 / 暗化背景。该架构及 ADR-0020 只保留历史参考，不代表当前代码。
  - 2026-08-09 历史阶段（已由 ADR-0020 与上条全量迁移取代）Today 焦点动效实施阶段 A 曾只完成基础设施，详情和创建尚未切换到新 Host；当时的 SDK/runtime 阻塞和旧路径仍运行结论不代表当前状态。
  - 2026-08-09 历史设计阶段（已由 ADR-0020 取代）参考视频审查曾以 ADR-0019 选择根级 UIKit 瞬态 Host，并把 Dock `+` 视为创建来源；当前架构沿用窄混合边界，但不要求创建卡从 `+` 形变，四类焦点对象统一由根级 Session 和同一落地语法管理。
  - 2026-08-08 历史阶段（已由 ADR-0019 取代）Today 卡片动效架构曾完成第三轮收敛，取代同日早先的 system bottom toolbar Seed。底部改为根层 `safeAreaInset` 持有的自定义 `HomeBottomDock`，内部仍使用原生 SwiftUI `Button` / `Menu`、SF Symbols 与系统 Glass/Material；Dock 始终保留安全区和真实按钮 Frame，焦点期间只切换视觉与命中。当前四项职责严格分离：`LazyVStack` 透明动态占位只负责分组、排序与后续行重排；首页局部焦点层独占详情/创建卡的可见像素；根层只运输创建 Seed；Dock 只提供稳定来源。创建进入先冻结一次紧凑列表目标，再执行“圆形 `+` -> 紧凑实体 -> 原子交给局部卡 -> 向下展开 -> 聚焦标题/键盘”；提交保留同一 UUID，将展开卡收拢为紧凑行后原子交给持久化列表行，并显式关闭通用 inserted-row 动画；放弃严格反向回到被冻结的原始 `+` Frame。详情继续采用列表归属的局部 SwiftUI 原位展开，背景按钮独占命中，点击背景只关闭当前任务。动效使用近临界阻尼、无刻意回弹，背景只有单一轻缩放/变暗/模糊景深平面；Reduce Transparency 改用实色暗层，Reduce Motion 跳过 Seed 空间运输并使用短淡变。关键文件：`HomeBottomDock.swift`、`AppRootView.swift`、`HomeInteractionMotionState.swift`、`HomeView.swift`、`HomeTaskCreationCard.swift`，决策见 ADR-0018。该阶段曾通过修改 Swift 文件 parse、`git diff --check`、Xcode 27 beta signed iOS 26.4 Simulator build 和完整 `TogetherTests`，但这些结果不验证 ADR-0019 的新架构。
  - 2026-08-08 历史阶段（已由上条取代）待办创建入口曾补充根层 SwiftUI seed overlay：从底部 toolbar `+` 的真实全局 Frame 出发，以 `260ms`、`extraBounce: 0` 形变到列表创建卡真实 Frame 后释放，稳定态仍完全由 `LazyVStack` 持有；Reduce Motion 直接跳过 seed，只保留短淡变。Simulator 录屏并抽取关键帧复核了入口、背景景深和编辑卡稳定终态。
  - 2026-08-08 历史阶段（已由上条取代）Today 创建与详情曾采用整张活动卡直接留在 `LazyVStack` 的方案，背景参数为 `0.985` 缩放、`5pt` 模糊和 `9%` 暗层；该方案会让活动卡与背景共用一套视觉变换，现只保留其固定 UUID 创建会话、任务模板删除和数据迁移结论。任务模板的 UI、领域类型、repository、依赖注入和运行时持久化模型已删除；新 `TogetherSchemaV2` 不含模板实体，V1 到 V2 迁移只将旧模板记录作为输入删除。
  - 2026-08-02 任务详情共享元素终点与速度修正：卡片最大展开后标题、完成框和进度偶发整体跳位的根因，是 `viewDidLoad` 阶段覆盖层尚未进入 `UIWindow`，窗口坐标 reporter 无法上报，目标 Frame 被过早冻结并回退到估算值；动画完成后实时 SwiftUI 首次给出真实 Frame，所有权交接即产生跳位。现先在来源位置挂载共享节点保持首帧连续，覆盖层入窗后将详情不可见地放到最终几何，最多让出三个主 actor 调度周期采集全部可见共享元素的真实窗口 Frame，再冻结并启动动画；预布局任务绑定 Overlay 生命周期并在 Recovery 时取消。标准展开由 `120ms` 抬升 + `390ms` 行程组成，收起由 `400ms` 回程 + `100ms` 落地组成，总时长约 `510ms / 500ms`；抬升从 `-1.5pt` 增强到 `-4pt`，稳定元素仍只平移、不缩放，卡面与阴影仍在离开来源后建立。关键文件：`TaskExpansionCoordinator.swift`、`TaskExpansionOverlayController.swift`、`TogetherTests.swift`；ADR-0007 与 `DESIGN_GUIDELINES.md` 已同步。验证：修改文件 Swift parse、`git diff --check`、全部 App Swift 源码 iOS 26.5 Simulator SDK typecheck、生成当前 Together module 后完整 `TogetherTests.swift` 与 Testing 宏 typecheck 均通过；新增测试覆盖展开/收起时长接近、抬升阈值和全部共享元素测量完成条件。标准 `xcodebuild` 仍因本机 Xcode 将已有 iOS 26.5 SDK 判定为 platform 未安装而在 destination 阶段阻塞，未执行测试用例或真机视觉验收。
  - 2026-08-02 任务详情连续转场消除源端横向白板与展开后内容真空：共享元素容器改为完全透明且不再持有整行 `shadowPath` 或阴影，抬升只使用约 `1.012` 缩放与 `-1.5pt` 位移；详情专属内容预挂载在最终布局，由卡片表面裁剪，并与卡壳、共享元素和关闭按钮共用同一个 `UIViewPropertyAnimator` 执行 `8pt -> 0` 位移与淡入，关闭严格反向。删除独立 `Task.sleep` 揭示任务、SwiftUI 级联显隐状态和逐项 modifier，Reduce Motion 仍为同步短淡变。关键文件：`TaskExpansionCoordinator.swift`、`TaskExpansionOverlayController.swift`、`HomeView.swift`、`TogetherTests.swift`；设计约束同步到 ADR-0007、`DESIGN_GUIDELINES.md`、`PRODUCT_SPEC.md` 与 `CONTEXT.md`。验证：`git diff --check` 通过；使用 Xcode 26.6 / iOS 26.5 SDK、临时排除资产目录后，主 App、Widget 依赖与 `TogetherTests` target 完成真实 Swift 编译和链接。标准 Simulator test 未执行，因为 SDK 26.5 与已安装 runtime 26.4 / 27.0 不匹配；仍需真机验收动画首尾、长内容、Reduce Motion 与连续快速操作。
  - 2026-08-01 首页任务收起动画偶发仅淡出的根因与修复：关闭流程原先先保存详情草稿和完成状态，保存会改变 timeline presentation identity、日期分组或可见任务集合，使来源行的 `TaskSourceGeometryReporter` 在动画开始前短暂注销；`taskIDsDidChange` 还会在 `preparingClose / closing` 阶段把正在进行的正常关闭二次降级为 fallback 淡出。现于 `requestClose()` 接受关闭意图时冻结最后有效来源几何，若 reporter 已短暂缺失则回退到本次会话不可变的 opening source；保存后的列表刷新不再决定回程目标。来源行在 `preparingClose / closing` 阶段消失只允许列表完成自身刷新，不再重启 fallback 动画；真正处于打开态且来源被外部删除时仍保留受控淡出。新增 50 轮来源几何交替存在/消失的目标解析测试及关闭阶段 live-source 策略测试。关键文件：`TaskExpansionCoordinator.swift`、`TogetherTests.swift`；决策补充到 ADR-0006。验证：`git diff --check` 通过；iOS 27 Simulator 与真机“猪皮小狗”完整 `TogetherTests` 均通过，真机为 165/165；普通 App 已重新安装并启动。自动化覆盖状态与目标选择不变量，实际每一帧的原位命中仍需用户真机连续展开收起观察。
  - 2026-08-01 首页普通任务连续动效完成第五轮端点与背景景深修复：将“共享身份、卡片表面、交互能力、背景景深”拆为独立视觉状态。来源端点和抬升阶段保持卡片表面透明，身份快照离开原行后才延迟显现白色卡面；关闭时卡面在返回来源前完成消隐，最终接触只保留原任务行。转场期间不再通过 SwiftUI `.disabled` 锁定标题、备注和完成框，避免生成快照时继承禁用态变灰；改由 Overlay 命中管理、动作 guard 和无障碍隐藏锁定交互，身份内容全程保持不透明。背景景深最终改为真实效果插值：`UIVisualEffectView` 始终 `alpha = 1`，独立 `UIViewPropertyAnimator` 把 `systemUltraThinMaterial` 从 `nil` 推进到 0.60 效果强度并暂停，关闭时按回程时长反向到 0；不再用模糊层透明度混合，避免未模糊背景直通造成“只有变暗、没有失焦”。标准动效完成 85ms 抬升后才建立模糊，Reduce Motion 使用 0.34 强度；不向 SwiftUI Environment 写逐帧进度。继续沿用窗口级 UIKit Overlay 与快照辅助架构，未改用 `matchedGeometryEffect` 或 `NavigationTransition`。关键文件：`TaskExpansionCoordinator.swift`、`TaskExpansionOverlayController.swift`、`TogetherTests.swift`；验收规范同步到 `DESIGN_GUIDELINES.md`、`PRODUCT_SPEC.md` 和 ADR-0005。验证：Swift parse、`git diff --check` 和 Xcode 27 Beta 真机签名 build 通过；App 已重新安装并启动。按本轮要求未执行单元测试；真实失焦强度与 60/120Hz 合成表现仍需用户真机观察，未执行 Instruments trace，不能宣称 ProMotion 已验收。
  - 2026-08-01 首页普通任务连续动效完成第四轮重构：经 `grill-with-docs` 确认“一镜到底”最终边界，折叠行与展开态的完成框、标题、副标题和可见标识保持同一信息结构；标准动效改由 `opacity = 1` 的身份快照独占几何运动，先用 85ms、约 `1.015` 尺度与接触阴影抬离列表，再用 310ms 朝视觉焦点移动并展开，详情在过半后以最多 4pt 位移级联入场；关闭先用 255ms 返回来源 Frame，再用 85ms 消除抬升并恢复来源，不交叉淡化两套身份内容。卡片按内容自适应高度，顶部和底部约 20pt，删除正文与属性栏分割线；标题/备注输入只在被遮挡时整卡最小上移，子任务保持卡壳位置并通过正文键盘 inset 与定点滚动留出 16pt。交互改为不可反转的串行会话：打开、保存、关闭和释放均绑定唯一 session ID，旧异步回调无法改写新会话；关闭期间的新点击只保留最后意图，旧 Overlay 完整释放后自动打开目标，来源隐藏、命中层和恢复状态统一清理。关键文件：`TaskExpansionCoordinator.swift`、`TaskExpansionOverlayController.swift`、`HomeView.swift`、`TogetherTests.swift`；决策见 ADR-0005、ADR-0006。验证：`git diff --check`、Xcode 27 Beta generic iOS `build-for-testing` 通过；连接真机“猪皮小狗”执行完整 `TogetherTests`，161/161 通过，包含 5 个任务轮换 50 次的展开/收起状态压力测试；普通 App 已重新安装并启动。自动化只能证明状态机与清理不变量，仍需用户真机连续快速点击验证实际手感、键盘、日期时间面板、深色模式、Dynamic Type、VoiceOver、Reduce Motion 与 ProMotion；未执行 Instruments trace，不能宣称 60/120Hz 已验收。
  - 2026-08-01 首页普通任务连续动效完成第三轮优化：最终原则收敛为“一镜到底”。删除背景任务行离散让位与所有位移，`LazyVStack` 始终保持原布局；窗口级 Overlay 仅用系统轻模糊、9%/16% 黑色暗层和双层阴影建立任务行抬升景深。卡壳先建立接触阴影再延迟 5% 启动几何展开；源完成框、标题、副标题与紧急标识在 Overlay 中复刻折叠行的字号、间距和 40pt 完成框，目标身份区只在真实越界时缩窄，详情子任务与属性栏延迟 132ms 后以最多 4pt 位移级联入场，关闭严格反向并在保存后使用最新源摘要与 Frame。交互根因修复为确定状态机：关闭/恢复期间背景触摸可穿透真实列表，不同任务的连续点击只保留最新打开意图；旧 Overlay 结束到下一帧释放之间禁止交叉安装，新任务在旧卡片彻底释放后自动接续；同任务反转、保存失败、Recovery 与来源消失都会清理或保留正确意图，避免频繁操作后残留 busy/hidden 状态。`TaskFrameRegistry` 的 reporter 保持非观察状态与幂等注册。关键文件：`TaskExpansionCoordinator.swift`、`TaskExpansionOverlayController.swift`、`HomeView.swift`、`TogetherTests.swift`。验证：`git diff --check` 通过；Xcode 27 Beta generic iOS `build-for-testing` 通过；连接真机“猪皮小狗”执行完整 `TogetherTests`，162/162 通过，并完成签名安装与启动。仍需用户真机验收列表顶部/中部/底部、快速连续切换、展开中反向、长标题/多行副标题、深色模式、Dynamic Type、VoiceOver、Reduce Motion 与 ProMotion 视觉帧率；未执行 Instruments trace，不能宣称 60/120Hz 性能已验收。
  - 2026-08-01 首页普通任务连续动效完成第二轮优化：展开路线确定为“来源标题优先、仅在安全区不足时最小修正”，不再要求卡片视觉居中；窗口级 Overlay 继续负责卡壳几何，来源上方与下方的可见任务行只切换一次离散让位状态并由 SwiftUI 插值，不改变 `LazyVStack` 实际行高或逐帧广播进度。`TaskFrameRegistry` 新增弱引用 live reporter，点按当下同步刷新可见行并解析真实窗口 Frame，临时无效报告不再删除最后有效几何；展开失败写入 signpost，触觉只在请求被接受后发生。详情卡与日期时间面板浅色表面改为纯白 `surface`，使用 0.5pt 语义描边、3pt 接触阴影和 24pt 环境阴影建立无渐变立体感。时间页删除横向尺标，新增窄 UIKit/Core Animation `TaskExpansionDualArcTimePicker`：左侧循环选择 `00...23` 小时，右侧循环选择 `00/05/.../55` 分钟，独立纵向手势、逐档触觉、基于释放速度的受控减速、最终强吸附，`CADisplayLink` 仅在减速/吸附期运行并请求 60–120Hz；VoiceOver 暴露两个 adjustable 控件，Reduce Motion 关闭惯性。关键文件：`TaskExpansionCoordinator.swift`、`TaskExpansionOverlayController.swift`、`TaskExpansionSchedulePanel.swift`、`TaskExpansionDualArcTimePicker.swift`、`HomeView.swift`。新增测试覆盖上下行让位、临时无效几何保留与十二档分钟策略；`git diff --check`、修改 Swift parse 和 Xcode 27 Beta generic iOS `build-for-testing` 通过，新增 App 与测试代码完成编译/链接，未执行真机或模拟器测试用例。仍需真机验收列表顶部/中部/底部来源、快速滚动后立即点按、展开中反向收回、滚轮快慢拖动、深色模式、Dynamic Type、VoiceOver、Reduce Motion 与 ProMotion 帧率。
  - 2026-08-01 首页普通任务连续动效完成第一轮优化：窗口级详情卡由固定约 78% 高度改为按标题区、子任务数量和固定底部属性栏自适应，安全区允许时保持源标题与目标标题的垂直起点一致；详情中间区独立滚动，卡面统一使用暖中性 `surfaceElevated`、更轻描边与阴影。日期/时间入口不再打开底部 Sheet，而是分别记录真实窗口 Frame，由 `TaskExpansionOverlayController` 使用第二个 `UIViewPropertyAnimator` 将对应标签展开为同一实体选择面板，面板可越出详情卡但受安全区约束，关闭时收回当前页面对应标签；点击面板外与 VoiceOver Escape 只关闭选择面板。日期页保留固定六周月历并增加紧凑时间摘要行；时间页使用大号 `HH:mm` 与共享横向离散尺标，分钟严格限定为 `00/05/.../55` 十二个 44pt 吸附点，初次打开不会暗中建立具体时间。Reduce Motion 只使用短淡变，辅助功能字号允许面板内部滚动，旋转后重新计算安全 Frame。关键文件：`TaskExpansionSchedulePanel.swift`、`TaskExpansionOverlayController.swift`、`TaskExpansionCoordinator.swift`、`HomeView.swift`、`TaskEditorSharedComponents.swift`。新增测试覆盖内容自适应与源锚点、面板安全区、十二个分钟停靠点和时间分量替换；`git diff --check` 与 Xcode 27 Beta generic iOS `build-for-testing` 通过，按当前验证策略未启动模拟器、未执行测试用例。仍需真机验收列表往返几何连续性、标签展开/收回、快速连续操作、分钟选择手感、深色模式、Dynamic Type、Reduce Motion 与 ProMotion；双半圆滚轮仅作为真机反馈后的备选，不在当前实现中。
  - 2026-07-30 首页普通任务详情由 `LazyVStack` 行内高度展开迁移为窗口级原位展开卡：新增 `TaskExpansionCoordinator` 单一可逆状态机、仅记录可见行的 `TaskFrameRegistry`、不可变 `TaskTransitionSnapshot` 和 UIKit `TaskExpansionOverlayController`。源任务行保留布局并只隐藏视觉，外层列表锁定；单个 `UIViewPropertyAnimator` 驱动卡壳、身份区、详情内容与轻量背景弱化，支持打开/关闭中途反向，Reduce Motion 退化为短淡变。详情继续复用 `HomeViewModel.detailDraft`，关闭前保存标题、备注、日期、时间、提醒、紧急、子任务及完成状态，再按更新后的源 Frame 收回；源行缺失时使用受控淡出。新增状态机、反向、Recovery、无效 Frame、像素对齐、安全区、无障碍字号、Registry、保存后源摘要一致性和 Overlay 释放测试。使用 Xcode 27 Beta / iOS 27 Simulator 完成 Debug、Release、完整 `TogetherTests` 执行，14 个新增转场测试全部通过；实际点击验证展开、关闭、标题编辑后源行即时回显，并定位修复了 Overlay 子控制器层级崩溃。Instruments SwiftUI 模板在当前 Simulator 报 `Hitches is not supported on this platform`，Time Profiler 命令行录制未按时限结束且未生成可导出 trace，因此 60/120Hz、卡顿和主线程占用仍需匹配真机正式验收，不能记为已通过。
  - 2026-07-30 首页已有任务的日期与时间入口统一打开 `ExistingTaskDateTimeEditorSheet`：点击日期优先显示固定 42 单元的紧凑月历，点击时间优先显示约五行高度、5 分钟步进的原生滚轮，两页保持挂载并共用固定 detent。日期选择和时间滚动立即更新详情 draft；顶部删除导航标题、关闭按钮和拖拽指示线，通过点击背景、系统下滑或 VoiceOver Escape 关闭。日期不提供清除，时间未配置时以当前时刻就近取整到 5 分钟作为滚轮种子，实际滚动才建立具体时间，并提供即时生效的系统红色“清除时间”。修改安排保留提醒提前量，清除时间同步清除提醒。关键文件：`TaskEditorSharedComponents.swift`、`HomeView.swift`、`HomeViewModel.swift`。
  - 2026-06-18 首页统一任务区方案已进入实现：`AppRootView` 顶部 toolbar 左侧 OCR、右侧头像进入 Profile；底部 toolbar 保留新建和周期事务入口；`HomeView` 主列表改为 `ScrollView` / `LazyVStack`，不再使用顶部周视图、月历、项目切换或 `List.onMove`。
  - 2026-06-18 项目产品语义合并到任务：所有新建都落到 Task；`PersistentProject` / `PersistentProjectSubtask` 仅暂留为 legacy migration source。`ProjectToTaskMigrationService` 将旧 Project 用原 UUID 迁移为普通 Task，ProjectSubtask 与 linked task 并入 TaskSubtask，并保留 source metadata。
  - 2026-06-18 OCR 导入改为只生成任务草稿；“标题 + 子项”结构落为一条 Task + 多个 TaskSubtask，不再写 Project。
  - 2026-06-18 首页任务日期标签规则：无截止显示创建日期；有截止但无具体时间显示截止日期；有具体时间显示截止时间。
  - 2026-06-19 首页已完成任务分层：首页主任务流只拉取未完成任务 + 今天已完成任务，今天已完成最多内联展示 6 条；“本周已完成”按钮改为打开原生 sheet，sheet 展示本周工作日内但不含今天的已完成任务，并提供进入完整“已完成”归档页的入口。
  - 2026-06-19 已完成分层真机空态问题修复：`HomeViewModel.reload()` 不再让“本周已完成”计数失败清空首页主任务流；本周计数失败时会 fallback 到同范围分页拉取，以保留 sheet 入口可见性。
  - 2026-06-19 “已完成”归档页重构为完整历史入口：支持本周 / 本月 / 所有分段筛选、搜索标题/备注/子任务、按周几/日期/月分组，使用系统 `List` + 分页加载和任务风格 completed row；恢复任务统一走重新打开任务，不只是取消归档。
  - 2026-06-19 已完成归档页继续原生化：`本周已完成` sheet 和完整 `已完成`页都改为原生 `NavigationStack` / toolbar / `List`；完整页顶部快捷 `Menu` 只负责本周 / 本月 / 全部，底部搜索使用系统 `.searchable` + `DefaultToolbarItem(kind: .search, placement: .bottomBar)`，右侧精确筛选按钮为原生 bottom toolbar item。
  - 2026-06-20 已完成归档页精确筛选改为原生 sheet：底部 toolbar 筛选按钮显示明确 SF Symbol 图标，sheet 使用固定 detent；日期和月份两种模式放在同一固定高度内容区，避免切换时 sheet 高度跳变。
  - 2026-06-20 首页任务详情改为内联展开编辑：`HomeView` 不再呈现 `HomeItemDetailSheet`；任务行点击后在当前任务流内展开标题、备注、子任务和日期/时间/提醒/重复设置。子任务新增、改名、勾选、删除都留在展开区内，日期/时间/提醒/重复复用现有 `TaskEditorUnifiedMenuSheet`，并新增 `.taskInline` 菜单上下文排除 `.subtasks`。
  - 2026-06-20 首页内联详情视觉修正：展开区取消备注、子任务、属性逐项描边圆角卡片，改为平铺视觉；展开态右侧日期位置统一为收起 chevron；子任务行复用父任务 action gutter 对齐规则，属性使用浅底胶囊按钮。
  - 2026-06-21 OCR 导入进入 P2 连续 sheet 方案：`OCRImportView` 改为原生 sheet 内状态流，先显示相机 / 照片来源选择，再在同一容器内切换相机预览、照片网格、识别中和任务草稿确认；OCR 草稿运行时删除 Project 语义，只保留任务、备注、子任务、日期、时间、提醒、重复属性。
  - 2026-06-21 OCR 媒体 sheet 外观修正：OCR sheet 移除固定 presentation 圆角、定制背景和背景交互，交回系统默认暗层、外部点击关闭和设备圆角适配；相机 / 照片状态不再走 navigation toolbar，改为相机预览或照片网格 edge-to-edge 填充 sheet，返回和照片管理入口使用悬浮 overlay。
  - 2026-06-21 OCR 入口改为 UIKit 原生 Popover：底部 OCR 按钮由 `OCRAttachmentBarButtonItem` 提供 UIKit source item，`OCRAttachmentPopoverPresenter` 只 present 一次 `UIHostingController`，附件菜单、AVFoundation 相机、inline PhotosPicker、识别中、失败和草稿确认通过 `OCRAttachmentPage` 状态机在同一个 popover 内切换，并用 `preferredContentSize` 驱动系统几何形变；旧 OCR sheet 不再从 `AppRootView` 呈现。
  - 2026-06-21 OCR Popover 质感修正：附件菜单文案改为“导入纸面内容 / 拍照或选择照片 / 直接识别成可编辑的任务草稿”；媒体页尺寸降低并在 page 变化时通过 presenter 更新 popover source 到当前 view 中心以稳定左右间距；相机和照片页控制按钮优先使用 iOS 26 Liquid Glass；照片页“全部照片”关闭自定义 Popover 后打开系统 `PHPickerViewController` 大屏 sheet，选图后回到 OCR 识别与草稿确认。
  - 2026-06-27 OCR 确认编辑改为根层原生自适应 Sheet：附件菜单、相机、照片、识别中和失败状态继续使用同一个 UIKit popover；识别成功后先完成 popover dismiss，再通过 `AppRouter.activeOCRReviewSession` 把同一个 `OCRImportViewModel` 移交给 `AppRootView.sheet(item:)`。确认页支持 `.medium/.large` detent，按草稿内容初始选高，内容增长或键盘出现时单向升级到 large；禁止拖拽直接关闭，取消统一处理编辑草稿放弃确认，导入失败保留输入。
  - 2026-06-30 例行任务视图重设计第一阶段落地：`PeriodicCycle` 新增 `.daily`，默认维度为每天/每周/每月，季度/年度通过用户偏好或已有任务自动显示；例行任务主页改为总览卡 + 横向维度 rail + 当前周期页头 + 同源任务行，任务点击不再打开 `RoutinesDetailSheet`，改为列表内大圆角白卡展开编辑。目标文案统一为“目标时间/目标日”节奏语言，首页 `RoutinesSummaryCard` 只在存在临期或已过目标时间的未完成例行任务时显示。
  - 2026-07-01 例行任务列表完成第二阶段同构重构：删除总览大卡、线性进度条、旧 `RoutinesDetailSheet` 和未使用的 `RoutinesCycleSection`；维度名称选择与当前周期摘要固定在任务流上方，选中态为 baby blue 短线，完成进度使用原生圆形 `Gauge`。任务主体改为单一 `ScrollViewReader + ScrollView + LazyVStack`，折叠行与首页共用“完成框 / 标题 / 属性副标题 / 备注”层级；展开态使用相同焦点白卡、背景模糊、稳定高度、级联动画、行内文本保存和收起后 draft 持久化语义，只保留维度、目标日、目标时间等例行任务属性。展开期间业务层拒绝切换维度，点击其他任务只收起当前项。
  - 2026-07-01 首页日期分组标题切换为 `LazyVStack(pinnedViews: [.sectionHeaders])` 原生单视图吸附：标题随列表滑到导航栏下方后直接停止，不再使用 GeometryReader / PreferenceKey 驱动的透明副本接管，从根因上移除交接帧跳动。标题继续无可见背景容器；真机确认 pinned header 会被提升到独立渲染层，跨层 `destinationOut` 会把黑色源矩形直接显示，因此遮挡改为仅吸附时按全局坐标续接页面网格的 Canvas，并保留展开态模糊与降噪表现。
  - 2026-07-01 普通任务下线重复属性并新增“紧急”：领域层统一使用 `isUrgent` / `.urgent` / `urgentCount`，持久层继续由 `PersistentItem.isPinned` 与 `PersistentTaskTemplate.isPinned` 承载，未改 SwiftData / CloudKit schema。普通任务创建、OCR、模板、首页展开态与组合设置不再写入重复规则；legacy `repeatRuleData`、occurrence 与最低限度解码链路继续保留。允许多个紧急任务；同一日期组内未完成紧急任务优先并按 `createdAt` 升序。列表用独立 44pt 命中区的珊瑚色 `flag.fill`（无文字/背景）关闭紧急，完成后隐藏但保留属性；展开态和组合设置中的紧急只改 draft，收起后统一持久化。主要文件：`TaskDraft.swift`、`DefaultTaskApplicationService.swift`、`HomeViewModel.swift`、`HomeView.swift`、`TaskEditorSharedComponents.swift`、`ComposerPlaceholderSheet.swift`、`OCRImportView.swift`。
  - 2026-07-01 新建任务属性行改为单行自适应：`TaskEditorChipSnapshot` / `TaskEditorRenderedChip` 增加显式 `showsTitle`，`TaskEditorChipSemanticValue.hasConfiguredValue` 统一判断配置状态；创建器日期保留“图标 + 日期”，未设置的时间、提醒、紧急、子任务只显示图标，配置后再展开文字。`TaskEditorChipRow` 新增可选非滚动模式，创建器禁用横向滚动并保留五个至少 44pt 高的入口；其他共享消费者保持原滚动行为。紧急开启态使用珊瑚色 `flag.fill`。
  - 2026-07-02 普通任务与例行任务标题区完成稳定化：折叠/展开复用同一标题区外层树和“标题 → 主副标题 → 可选属性摘要”层级；有备注时备注优先、属性位于下一行，无备注时属性优先、无属性回退状态。备注在副标题原位编辑，详情区不再重复渲染编辑器；无备注时“添加备注”将焦点移回标题区。两类副标题统一为 15pt Medium 与 body 74% 中性色。例行规则的目标日、小时、分钟改为可选并保持旧 JSON 兼容，不改持久化 schema；目标日/时间可独立清除，新建和切换维度不再自动生成周二或 09:00，每天时间-only 规则由计算器映射到当天通知，展开态继续使用 Compact DatePicker。
  - 2026-07-02 任务属性与例行提醒统一：普通任务新建和首页展开态移除活跃组合属性 Sheet，日期/时间改为 Compact DatePicker、提醒改为 Menu；新建子任务 UI 对齐展开态的虚线完成框、行内保存与按需删除。例行创建和展开工具条增加独立提醒提前量与通知/AlarmKit 投递方式；提醒字段继续编码在 `reminderRulesData`，旧完整规则解码为目标时刻通知，不改 SwiftData schema。展开态完成新增延迟列表提交路径，使草稿保存、完成反馈、详情收起、背景恢复和重排按顺序发生。验证：`plutil -lint Together/Info.plist`、`git diff --check` 和 Xcode 27 beta generic iOS `build-for-testing` 通过；focused tests 已完成编译，但模拟器测试 runner 长时间无结果后人工终止，不能视为测试通过。AlarmKit 权限、静音/专注模式与锁屏呈现仍需 iOS 26+ 真机验收。
  - 2026-07-02 日期/时间属性入口统一：普通任务与例行任务的新建、展开态不再把 Compact DatePicker 和独立清除小叉直接塞入等分工具条；入口统一为图标 + 简短值按钮，日期使用原生 `.medium` Sheet + Graphical DatePicker，时间使用约 320pt 紧凑 Sheet + Wheel DatePicker，清除与完成位于 Sheet 内。属性工具条使用自适应 Layout，标准宽度单行、空间不足时两行；紧急入口同步复用 44pt 共享属性按钮。验证：Swift parse、`git diff --check`、Xcode 27 beta generic iOS `build-for-testing` 通过；仍需真机确认日期/时间 Sheet 高度与动态字体换行。
  - 2026-07-03 例行首行展开卡高度修复：真机底部大留白的根因是 `AdaptiveTaskAttributeToolbarLayout` 采纳了 `Menu` 返回的异常理想高度，进而让详情高度膨胀并让聚焦滚动误判为超高卡片、采用顶部对齐。属性 Layout 改为每行固定 44pt，只用内容理想宽度决定单行/双行；例行焦点白卡同时取消负向顶部扩展，仅保留下方 16pt 品牌延伸，避免顶部对齐时被 ScrollView 裁切。验证：Swift parse、`git diff --check`、独立 DerivedData generic iOS `build-for-testing` 通过；仍需真机复测首项完整圆角与卡片底部节奏。
  - 2026-07-03 例行首次展开大位移修复：前一版动态 focus runway 会在首次测量时先插入约半屏透明占位，滚动定位尚未完成便把首项推到下方，而且透明占位没有命中响应；第二次因测量缓存存在才恢复。现已删除 viewport / focused-row 测量、上下透明 runway 和延迟二次定位，恢复为展开后直接滚动到详情锚点；焦点卡已无负向顶部扩展，因此首项停在列表顶部也不会裁切。
  - 2026-07-03 例行展开卡节奏对齐：例行详情旧固定 `126pt` fallback 与实际“添加备注 + 属性栏”两行不匹配，导致卡片被撑到接近普通任务高度并在属性栏下方留下空白。`RoutineInlineLayoutMetrics` 改为按可见行计算 fallback，统一使用 32pt 紧凑行、2pt 行距和 44pt 属性栏；有添加备注时为 86pt，无入口时为 52pt，不伪造普通任务的子任务空间。
  - 2026-07-03 例行维度栏与卡外交互完善：维度栏改为名称与末尾管理入口全宽等分，Gauge 中心数字提升为 12pt 等宽粗体；每天至每年均可通过 UserDefaults 偏好显示或隐藏且至少保留一个，旧季度/年度偏好自动迁移，隐藏维度任务与提醒不删除，首页临期跳转可临时显示隐藏维度。展开态使用命名坐标空间记录聚焦卡 frame，`SpatialTapGesture` 仅在卡外收起，修复 ScrollView 背景手势无法命中空白区域；长按菜单提供编辑/收起与删除。focused tests、`git diff --check`、generic iOS `build-for-testing` 通过；仍需真机确认等分栏、大字体和空白点击手感。
  - 2026-07-03 首页进入例行任务改为同画布模式转场：`HomeView` 常驻普通任务层与例行任务层，网格画布不参与切换；普通任务轻微缩小 / 上移 / 模糊淡出，例行维度区与任务层从下方进入，前五行以 20ms 间隔级联，返回按反向顺序播放。根标题使用稳定 principal 容器做 2pt blur / opacity 替换，不缩放，时长为 0.42 秒无回弹 `.smooth`；Reduce Motion 仅保留 180ms 淡变。隐藏例行层禁止命中与辅助功能访问，但预加载数据；退出模式会收起草稿并清理临时维度。历史 Calendar 页面、ViewModel、路由以及首页不可达的周/月日历与翻页手势已删除；Foundation `Calendar` 仍服务任务日期分组、DatePicker、提醒和周期计算。主 App 全量 iPhoneOS SDK typecheck 与 `git diff --check` 通过；标准 `build-for-testing` 因本机 Xcode 将已有 iOS 26.5 SDK 判定为 platform 未安装而在 destination 阶段阻塞，仍需真机确认转场节奏与快速连续点击。
  - 2026-07-14 普通任务与例行任务统一接入 SwiftUI 原生 leading `swipeActions`（右滑露出操作），均提供“推迟到明天”和删除，禁用 full swipe；详情展开期间不暴露滑动操作。普通任务沿用已有 due date 推迟逻辑；例行任务新增可选 `deferredUntil`，作为 SwiftData / CloudKit 可同步的一次性可见性与提醒延后状态，不修改 `PeriodicCycle` 或提醒规则，到次日自动重新进入列表。例行删除以当前 personal space 为权限边界，兼容重装恢复后历史 `creatorID`；删除同时取消真实通知标识和 AlarmKit 提醒。关键文件：`HomeView.swift`、`RoutinesListContent.swift`、`RoutinesViewModel.swift`、`PeriodicTaskApplicationService.swift`、`PersistentPeriodicTask.swift`、`LocalReminderScheduler.swift`。
  - 2026-07-14 例行任务勾选完成动画与普通任务完全同构：原位置先播放虚线轮廓收束、珊瑚填充与光晕扩散、勾号弹入和整行轻微压缩回弹，未展开任务约 320ms 后、展开任务约 680ms 后才提交完成状态并用无额外回弹的 smooth 动画重排；取消完成使用同款勾号淡出恢复，Reduce Motion 保留短淡变。动画临时态由 `RoutinesViewModel` 管理，持久化结果在反馈窗口结束后才写入列表，避免先重排后动画。关键文件：`RoutinesTaskRow.swift`、`RoutinesListContent.swift`、`RoutinesViewModel.swift`。
  - 2026-06-25 中文输入法提交修复：删除 `CompositionAwareTextField` / `TextInputCommitter` 的 UIKit TextField 桥接和经验延迟提交路径；首页展开态标题、备注、子任务新增/编辑、任务详情子任务、OCR 草稿、项目子任务和快速创建子任务统一回到 SwiftUI 原生 `TextField` + `@FocusState` + 本地编辑 buffer；针对微信输入法 marked text 不落入 SwiftUI binding 的真机现象，新增 `TextInputSnapshotReader`，在保存/添加瞬间读取当前 UIKit 第一响应者的可见文本，再结束焦点并提交。
  - 2026-06-14 普通任务子任务已落地：新增独立 `TaskSubtask` / `PersistentTaskSubtask`，`Item.subtasks` 与 `TaskDraft.subtasks` 走本地 SwiftData hydrate；不复用 `ProjectSubtask`，不引入 Supabase 或多人能力；删除父任务会 tombstone 普通任务子任务。
  - 普通任务子任务应用服务已接入 `TaskApplicationServiceProtocol` / `DefaultTaskApplicationService`，支持新增、勾选、改名、删除；子任务完成只更新子任务和父任务 `updatedAt`，不会自动完成父任务。
  - 普通任务创建器和首页 Timeline 已接入子任务：创建器通过“子任务” chip 内联编辑；首页任务展开区可新增/编辑/删除/勾选子任务；Timeline 折叠态保留子任务进度摘要。
  - 任务模板能力已彻底删除；历史 V1 模板实体只作为 V2 迁移输入，用于删除旧模板记录，不存在运行时入口、仓库或持久化模型。
  - 项目子任务 UI 已调整为与项目标题左边缘对齐，保留子任务 checkbox 44pt 命中区。
  - 已删除旧 Supabase / RevenueCat / Paywall / PremiumGate / webhook 代码与 Supabase 目录。
  - 已删除双人协作、配对、邀请、重要日期、任务聊天、共享 mutation UI 与旧 CKSyncEngine/outbox/record codec 链路。
  - `PersistenceController` 生产配置使用 SwiftData `cloudKitDatabase: .private("iCloud.com.pigdog.Together")`；in-memory preview/test 使用 `.none`。
  - OCR MVP 已落地：支持拍照或选图；`VNRecognizeTextRequest` 后台识别；本地 parser 生成任务草稿；用户确认后才写入任务。
  - 测试 target 已重建为当前架构的 Swift Testing 基础测试，覆盖 Item 状态机、单人 SessionStore、OCR parser。
- 当前验证：
  - 2026-07-30 统一日期时间 Sheet 验证：Swift parse、`git diff --check` 通过；使用当前 Xcode 工具链临时重建 TogetherCore simulator module 后，主 App 全量 Swift typecheck 与 `TogetherTests` 全量 typecheck 均通过，新增测试覆盖固定六周月历、五分钟时间种子、旧分钟值仅显示取整但不静默写回，以及提醒联动。标准 generic / simulator build 在 destination 阶段被“iOS 26.5 is not installed”阻塞，因此未执行链接和 simulator tests，仍需在匹配 runtime 或真机上验收 Sheet detent、月历点击、时间滚轮、Dynamic Type 与深色模式。
  - `git diff --check` 通过。
  - 2026-07-14 右滑操作、例行任务删除/推迟与完成动画验证：签名 iOS 26.4.1 模拟器完整 `TogetherTests` 125/125 通过；覆盖动画窗口结束前保持原任务状态、推迟不修改重复规则、次日恢复显示、历史 creator 身份删除、提醒迁移和真实通知标识取消。Xcode 27 `build-for-testing` 通过；尚未做真机滑动手感、完成动效和通知到达验收。
  - 2026-06-14 普通任务子任务验证：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet` 通过；覆盖创建 hydrate 排序、整单更新过滤空标题、勾选最后子任务不自动完成父任务、删除父任务隐藏子任务、模板保存/恢复子任务、Timeline entry 子任务数据。
  - 2026-06-14 普通任务子任务构建：`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；仍有既有 Swift 6 actor isolation / Widget / Notification warning，本次新增的 `TaskSubtaskDraft` Equatable warning 已收敛。
  - 2026-06-19 已完成任务分层 focused 测试：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet` 通过；覆盖 CompletedTaskRange、首页今天已完成过滤、本周 sheet 排除今天、计数失败不清空首页主任务流、已完成页子任务搜索与所有历史分页。
  - 2026-06-20 首页内联详情验证通过：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet`、XcodeBuildMCP `build_run_sim` 均通过；新增测试覆盖展开任务生成 draft、切换展开任务保存上一条、日期/时间/提醒/重复走现有 update path、子任务新增/改名/删除、`.taskInline` 不暴露 `.subtasks`、无子任务任务可收起、父子任务对齐 metrics。
  - 2026-06-21 OCR Popover focused 测试通过：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet` 通过；新增覆盖 OCR 识别成功进入 review、多图 OCR 合并解析、OCR apply 写入 title/notes/subtasks/dueAt/hasExplicitTime/remindAt/repeatRule。`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；XcodeBuildMCP `build_run_sim` / `stop_app_sim` 通过。仍需真机验证 UIKit sourceItem Popover 形变、AVFoundation 相机预览、inline PhotosPicker、limited library 和权限空态。
  - 2026-06-25 中文输入法提交修复验证：`rg -n "CompositionAwareTextField|TextInputCommitter|AfterInputCommits|AfterInputSettles|waitForFocusedInputToCommit|inputFocusBinding|subtaskFocusBinding|titleFocusBinding" Together TogetherTests -S` 无残留；`git diff --check` 通过；`DEVELOPER_DIR=/Users/papertiger/Downloads/Xcode-beta.app/Contents/Developer xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；`DEVELOPER_DIR=/Users/papertiger/Downloads/Xcode-beta.app/Contents/Developer xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet` 通过。仍需真机验证微信输入法一次输入/分两次输入中文后直接保存。
  - 2026-06-27 OCR 原生确认 Sheet 验证：新增测试覆盖短草稿 medium、长草稿 large、内容/键盘单向升级、session 保留同一 ViewModel 与中文草稿、编辑变更检测；完整 `TogetherTests/TogetherTests` 通过。仍需真机验证 popover 退场到 sheet 入场、系统 detent 手感、微信输入法和属性子 sheet。
  - 2026-06-30 例行任务重设计验证：`git diff --check` 通过；`DEVELOPER_DIR=/Users/papertiger/Downloads/Xcode-beta.app/Contents/Developer xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO` 通过；新增 focused tests 曾在 iPhone 17 / iOS 27 simulator 通过，覆盖 daily 周期 key/range、目标节奏文案、默认/自动可见维度、临期摘要、内联编辑收起后持久化；随后修正每周目标文案按 `calendar.firstWeekday` 推导，最终代码已通过测试编译，复跑 focused tests 卡在 Xcode runner `waiting for workers to materialize` 后手动中断。仍需真机验证维度切换动效、展开编辑键盘避让、动态字体、深色模式和临期卡入口。
  - 2026-07-01 例行任务同构重构验证：新增 Swift Testing 覆盖维度摘要完成比例、展开期间禁止切维度、维度与目标时间只在收起后持久化、点击第二条只收起当前项、已完成例行任务仍可展开；先验证旧行为对应测试失败，再完成修复。`DEVELOPER_DIR=/Users/papertiger/Downloads/Xcode-beta.app/Contents/Developer xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,id=F77C3721-9F5D-4BDC-A880-2DC0E0429B7B' -only-testing:TogetherTests/TogetherTests -quiet` 通过；generic iOS `build-for-testing` 通过。仍需真机验证固定维度区、圆形 Gauge、中文输入、键盘避让、深色模式、动态字体和 Reduce Motion。
  - 2026-07-01 首页原生日期标题吸附验证：旧 `HomeTimelineSticky*`、GeometryReader / PreferenceKey 偏移追踪和顶部 mask 无残留；首次真机验证发现 `destinationOut` 在独立 pinned 渲染层中失效并显示黑色标题带，已改为普通 Canvas 网格切片。修复后 `git diff --check`、完整 `TogetherTests/TogetherTests`、generic iOS `build-for-testing` 均通过；仍需真机复验网格连续性、标题推挤和展开态模糊效果。
  - 2026-07-01 普通任务紧急属性验证：`git diff --check`、主 App 全量 Swift typecheck、Xcode beta generic iOS `build-for-testing` 与 iOS 27 模拟器完整 `TogetherTests/TogetherTests` 均通过。新增覆盖 Mock/Local repository 同时保存多个紧急任务、日期组内紧急优先且按创建时间排序、展开草稿收起后保存、折叠旗帜即时关闭，以及 OCR 紧急属性写入。仍需真机验证 44pt 旗帜命中、长标题/动态字体、深色模式、连续切换与收起重排。
  - 2026-07-01 新建任务属性 chip 自适应验证：先用 `taskPropertyChipTitleVisibilityTracksConfiguredValue` 证实旧实现缺少配置状态 API，再完成实现；Xcode beta generic iOS `build-for-testing` 与 iOS 27 模拟器完整 `TogetherTests/TogetherTests` 通过。尝试启动模拟器做截图验收时，测试工具使用默认 Xcode 无法识别 runtime；改用 Xcode beta 手工安装后 App 停留在既有冷启动空白页，未能进入创建 Sheet，因此仍需真机确认五个入口的实际宽度、动态字体压缩和配置切换动效。
  - 2026-07-02 标题区与例行属性完善验证：`git diff --check`、主 App 全量 Swift typecheck、Xcode beta generic iOS `build-for-testing` 通过；新增测试覆盖旧规则 JSON、daily time-only 通知、无默认目标、目标日/时间分别清空并在收起后持久化、备注优先展示及无属性状态回退。聚焦测试已完成 App 与测试 target 编译，但 iOS 27 模拟器 Test Runner 启动后无测试事件，等待后主动中断，因此本轮不能声明测试执行通过；仍需真机验证折叠/展开标题几何连续性、Compact DatePicker、小叉命中、中文输入、动态字体、深色模式与 Reduce Motion。
  - 2026-06-27 OCR 底部入口与媒体 Popover 收敛：首页底部 toolbar 改为左侧 OCR、右侧新建，完整已完成归档统一由 Profile“日志”进入；OCR 菜单锚定左下图标，相机/相册/处理中/失败页在同一 popover 内切换为距屏幕左、右、底各 12pt 的媒体 frame，外框不避让 Home Indicator，内部控件单独叠加设备底部安全区；所有关闭路径恢复 OCR source item 后退场。`git diff --check`、Xcode 27 generic iOS build 和 build-for-testing 通过；新增几何与安全 padding 测试已编译，但模拟器 Test Runner 在建立连接前异常退出，仍需真机验证连续形变、实际边距和缩回锚点。
  - 2026-06-28 OCR 呈现层回归系统原生 popover：删除透明 `.overFullScreen` overlay presenter、source frame、手工同心圆角、固定 12pt 外框和自定义 `GlassEffectContainer`。首页底部原生 SwiftUI OCR `Button` 直接持有 `.popover`，菜单、相机、照片、处理中和失败状态复用同一 `OCRAttachmentPopoverSession`，由系统负责 Liquid Glass 外壳、source item 替换、圆角、边距与缩回动画；识别成功仍在 popover 关闭后移交根层确认 Sheet。Xcode 27 generic iOS 无签名 build 通过。
  - 2026-06-28 OCR 原生 popover 真机问题修正：确认 SwiftUI `.presentationSizing(.fitted)` 对运行中内容换页只重新测量并直接提交 frame，不能提供所需的原生连续 resize；已删除人为延迟和内容空白淡出，改用零尺寸 `UIViewControllerRepresentable` 定位同一系统 popover 的 hosting controller，并仅更新 `preferredContentSize`，由 `UIPopoverPresentationController` 执行菜单、相机和照片之间的双向系统几何过渡。确认 Sheet 改为在 popover 根内容 `onDisappear` 后等待根 presentation controller 释放再呈现；确认列表使用 `LazyVStack`，避免大量 OCR 草稿一次性构建编辑器阻塞主线程。Xcode 27 generic iOS 无签名 build 通过，仍需真机复验动画和确认页交接。
  - 2026-06-28 OCR 呈现层改为双层系统 Zoom：旧 SwiftUI `.popover` 与 `preferredContentSize` resize 无法实现菜单到相机的整体彩样形变，现改为 UIKit Zoom Presentation（真实左下 OCR toolbar item ⇄ 当前附件 surface）与 Zoom Navigation（整个 Liquid Glass 菜单 ⇄ 稳定媒体 HostingController）。自定义 `UIPresentationController` 只负责透明背景点击和物理屏幕三边 `12pt` 布局；相机 / 照片外框进入 Home Indicator，内部控件单独避让安全区。识别成功后在外层 dismiss completion 中把同一个 ViewModel 移交根层确认 Sheet，不再轮询 presentation controller。
  - 2026-06-28 OCR toolbar source 真机修正：SwiftUI toolbar 的 accessibility metadata 不保证映射到可枚举的 `UIBarButtonItem`，全局扫描失败会导致点击 OCR 后静默返回。OCR 图标现通过零视觉影响的 `UIViewRepresentable` anchor 注册真实 UIKit 层级，Zoom source provider 每次从 anchor 向上解析当前按钮基座；全局 toolbar 扫描仅作为兜底。
  - 2026-06-28 OCR 双层 Zoom 架构被真机证伪并撤销：外层自定义 Presentation 固定为媒体大 frame、内部再用 Navigation Zoom 从小菜单跳转实时 `AVCaptureVideoPreviewLayer`，导致系统同时快照透明导航根、菜单 HostingController、媒体 HostingController 与实时相机图层，出现白色大容器残影、矩形相机快照、圆角丢失和 PhotosPicker 层级混乱。现只保留 OCR ⇄ 当前 surface 的一层系统 Zoom；presented view 在菜单状态就是菜单实际 frame，菜单 ⇄ 媒体改为同一 ViewController 内 `UIViewPropertyAnimator` 驱动 frame/圆角和内容快照，媒体 ViewModel 与确认 Sheet 数据流不变。
  - 2026-06-28 OCR 菜单与媒体尺寸收敛：菜单明确为单一自定义 `GlassEffectContainer + glassEffect` surface，不使用系统 Menu/popover。相机/照片高度由固定 600pt 改为 `floor(屏幕高度 × 0.5) - 12pt` 的 medium-sheet-like 自适应高度；媒体 `UIHostingController.safeAreaRegions = []`，相机预览铺满整个外框，设备 bottom safe-area 只传给返回、快门和照片入口，修复底部黑边同时保留 Home Indicator 操作避让。
  - 2026-06-28 OCR 外层 Zoom 架构最终移除：真机确认系统 Presentation Zoom 会缩放首页、为 presented view 生成矩形阴影，并让可拖动 UIKit 根容器与内部 Liquid Glass 内容成为两个视觉层。现改为透明 `.overFullScreen` 控制器内的单一持久化 surface：读取真实 OCR toolbar 按钮 frame 作为展开/收回端点，菜单、相机/照片和下拉退场统一由 `UIViewPropertyAnimator` 驱动同一 surface；底层首页保持静止，拖拽直接移动完整 surface，菜单 Liquid Glass 与媒体 ViewModel/确认 Sheet 数据流保持不变。
  - 2026-06-28 OCR 相机最终高度改按画幅驱动：不再套用 medium sheet 高度，相机/照片外框使用 `可用宽度 × 4 / 3` 的竖向 3:4 比例并受顶部安全空间限制；390pt 屏幕对应 366×488pt，处于旧 410pt 过矮与 600pt 过高之间。
  - 2026-06-29 OCR 呈现架构收敛为系统 Menu + Sheet：删除 toolbar source anchor、透明 `.overFullScreen` 控制器、UIKit surface、快照、拖拽和所有自定义 morph。首页左下 OCR 改为原生 SwiftUI `Menu`；相机由 `.medium` Sheet 承载，inline PhotosPicker 默认 `.large` 且支持 `.medium/.large`，相机切照片、识别中与失败复用同一 `OCRSourceSheetSession/OCRImportViewModel`。识别成功先关闭来源 Sheet，再在 `onDismiss` 中移交现有自适应确认 Sheet，避免 presentation controller 竞争。
  - 2026-06-29 首页活跃任务流已按日期分组：有截止 / 发生日期的任务进入对应日期组，无截止任务按创建日期分组并整体排在有日期任务之后；`HomeView` 使用 `LazyVStack(pinnedViews: [.sectionHeaders])` 和平铺文本分组 header（如“明天 周二·7项”，无胶囊 / 描边 / 填充容器）。任务标题右侧不再显示日期 / 时间，折叠态标题获得完整宽度且保持单行；明确时间进入次级信息行，今天已完成区域继续独立。
  - 2026-06-27 OCR Popover 真机 follow-up：真机暴露媒体 `sourceRect` 错把目标 frame 中心当作 UIKit 底边锚点，导致相机整体上移约半个高度；已改为使用目标 frame 的 `maxY`，使底边回到屏幕下方 `12pt`。菜单保留 OCR 原生 source item，并将 left/bottom layout margin 统一为设备 bottom safe-area inset，top/right 保留系统默认值。Xcode 27 generic build 与 build-for-testing 通过；几何测试已编译，模拟器 Test Runner 仍在建连前异常退出，需真机复验菜单左下同心关系和相机三边距离。
  - 2026-06-19 已完成任务分层构建验证：`git diff --check` 通过；`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；XcodeBuildMCP `build_run_sim` 通过并已 stop app。仍有既有 Swift 6 actor isolation / notification warning，本轮未处理。
  - XcodeBuildMCP `build_run_sim` 通过。
  - XcodeBuildMCP `test_sim` 通过，4/4 passed。
- 已知遗留：
  - 首页改为 `ScrollView` 后失去 `List.onMove` 和原生 swipe 行为；本轮用 context menu / 内联详情承接删除、推迟等操作，排序入口后续单独评估。
  - 仍有既有 Swift 6 actor/concurrency warnings，主要在权限服务、repository、widget shared store 和 mock avatar uploader；本轮未扩展处理。
  - OCR 未做真机拍照与真实 iCloud 同步验收；模拟器只能验证构建、启动、照片入口和 parser 测试。
  - `SyncCoordinatorProtocol` 仍作为 repository 兼容 no-op 存在；没有手写 CloudKit 发送/拉取链路，后续可进一步删除 repository 层 `recordLocalChange` 样板。

## 历史交接（旧方向，仅供溯源）

- 分支：`main`
- 当前任务：会员支付主链路已通过真机验收；上架前合规收敛已完成仓库内可控部分，包括法律文案、权限声明、Privacy Manifest、开发者赠权 runbook、App Store Connect 人工核对清单与 ASC 可粘贴送审材料。2026-05-10 已将桌面宣传截图整理为 iPhone 6.9 安全上传包；V1 target 已改为 iPhone-only，build 46 已重新上传 TestFlight，避免继续按 Universal App 补 iPad 截图。
- 当前验证策略：用户已要求后续不做模拟器测试；本阶段只做 `generic/platform=iOS` 编译与 build-for-testing，不启动模拟器。
- 立即续接点：做真机购买 / 恢复购买 / entitlement webhook 端到端验证。Webhook URL：`https://nxielmwdoiwiwhzczrmt.supabase.co/functions/v1/revenuecat-webhook`。
- 会员修复进度：
  - Grace 期不再视为完整 Pro：`PremiumStatus.isPremium` 只对 `.pro` 为 true，`.gracePeriod` 仅允许 `allowsFullLogbook`。
  - Pro quota / 个人同步等 gate 继续读 `isPremium`；Profile 订阅说明等需要展示 grace 的 UI 改读 `isProOrGracePeriod`。
  - `PremiumGate.computeStatus` 在任一权威来源临时失败且无明确 active / grace 结果时优先使用有效缓存，避免 RevenueCat 短暂失败把真实订阅用户降级为 free。
  - 免费 iPhone 恢复是产品认可例外；iPad / Mac 个人同步必须 active Pro。客户端继续用 `SoloSyncGate`，后端新增 `premium_entitlements` 与 `solo_sync_gate_allows` 做服务端兜底。
  - Paywall 已把订阅条款与选中套餐价格说明移动到购买 CTA 附近，并显式处理取消购买 / 等待批准错误状态。
  - RevenueCat webhook Edge Function 已部署为 `revenuecat-webhook` version 1，`verify_jwt=false`，函数内部用 `REVENUECAT_WEBHOOK_AUTHORIZATION` 做自定义 Authorization 校验；未带 Authorization 的请求已验证返回 `401 UNAUTHORIZED`。
  - RevenueCat project `Together` 的 project id 为 `proj9691e26b`；active entitlement `lookup_key=pro` 已存在并绑定当前 App Store / Test Store 产品，与 `RevenueCatConfig.entitlementIdentifier = "pro"` 对齐。
  - RevenueCat webhook integration `Together Supabase Entitlements` 已创建，id 为 `whintgr6c8d5e0e8d`，指向 Supabase `revenuecat-webhook`，覆盖所有 app / environment / event types。
  - 生产 Supabase 已应用 `20260502055454 premium_entitlements_and_solo_sync_gate` 和 `20260502055831 restrict_premium_gate_function_execute`。`anon` 对新增 `SECURITY DEFINER` 函数的执行权已撤销；authenticated 仍可执行必要 RPC/helper，但 entitlement helper 已强制 `auth.uid() = p_user_id`。
  - 真机月订阅购买链路已验证：Free → Apple sandbox 购买弹窗 → 购买成功 → Pro；RevenueCat webhook 写入 `premium_entitlements`，`product_id=com.pigdog.together.monthly1m`、`environment=SANDBOX`、`last_event_type=INITIAL_PURCHASE`、`revoked_at=null`。
  - 已发现并修复退出登录再登录后变 Free 的架构缺口：客户端 `PremiumGate` 不能只依赖 RevenueCat SDK customerInfo 和 `premium_grants`，还必须合并 Supabase webhook 写入的 `premium_entitlements`。生产装配已新增 `SupabasePremiumEntitlementsLoader`，服务器 entitlement 按 `.subscription` 来源参与 Pro / Grace / cache fallback 合并。
  - 已发现并修复购买成功后 Paywall 仍显示购买选项的最终一致性缺口：购买返回 `.success` / `.pending` / `paymentPending` 后，`PaywallViewModel` 会短时间轮询 `PremiumGate.refresh()`，等待 RevenueCat SDK 或 Supabase `premium_entitlements` 任一权威来源转 Pro；Profile 会员页进入 Free 分支前会先 `configurePremiumGate()`，若后端已有 active entitlement 不再显示普通 Paywall。
- 当前未完成：
  - 需要按 `docs/superpowers/runbooks/2026-05-03-app-store-release-readiness.md` 人工核对 App Store Connect、RevenueCat、Supabase 和 TestFlight 真机验收项。
  - 需要在 App Store Connect 后台替换 App 描述、关键词、IAP 元数据、审核备注和隐私标签，并在 build 46 processing 完成后绑定到 iOS 1.0 待提交版本。
  - 需要上传 `/Users/papertiger/Desktop/宣传截图_ASC/iPhone-6.9-safe/` 的 iPhone 6.9 截图，并确认 App Store Connect 不再要求 iPad 截图。
  - 当前本地工程构建号已提升为 `1.0 (46)`；未提交的逾期任务“移到今天”新功能已按保守送审策略移除，本次送审只保留 iPhone-only 与上架准备改动。
  - 2026-05-10 RevenueCat 后台已收敛：current offering `default` 中 `$rc_monthly/$rc_annual/$rc_lifetime` 分别绑定 App Store 产品 `com.pigdog.together.monthly1m`、`com.pigdog.together.yearly`、`com.pigdog.together.lifetime`；多余 entitlement `Create an app called Together Pro` 已删除，MCP 复核 active entitlement 只剩 `pro`。
  - 2026-05-10 Xcode destination 环境已恢复：`xcodebuild -showdestinations` 能列出 Any iOS Device 与 iOS 26.4.1 simulators；generic build、build-for-testing、Release archive 与 TestFlight upload 均已通过。
  - `docs/legal/*.md` 已同步 push 到独立 `together-app-legal` 仓库 commit `6385a3c`；GitHub Pages 线上 Privacy Policy / Terms 已验证显示 2026-05-04 版本。
- 最近验证：
  - 2026-05-10 App Store 送审冻结 build 46：`git diff --check`、`plutil -lint Together/Info.plist Together/PrivacyInfo.xcprivacy TogetherWidget/Info.plist Together/Together.entitlements TogetherWidget/TogetherWidget.entitlements`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -onlyUsePackageVersionsFromResolvedFile -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -onlyUsePackageVersionsFromResolvedFile -quiet`、Release archive 均通过；归档路径 `build/TestFlight-20260510-1027-build46/Together.xcarchive`；`xcodebuild -exportArchive` 返回 `Upload succeeded` / `Uploaded package is processing`。仍需 App Store Connect processing 完成后绑定 build 46，并上传截图与元数据。
  - 2026-05-03 上架合规收敛：`git diff --check` 通过；`plutil -lint Together/Info.plist Together/PrivacyInfo.xcprivacy` 通过；旧麦克风/语音识别权限键与旧 Grace 全 Pro 文案无残留；`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
  - `git diff --check` 通过。
  - `xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过。
  - `xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过。
  - TestFlight build 34 已从当前 `main` archive 并上传到 App Store Connect；`xcodebuild -exportArchive` 返回 `Upload succeeded` / `Uploaded package is processing`。
  - Clean-room 前置状态：测试用户 `bb7a3977-3bbc-447b-989a-3fbe6b8d8eb6` 的 Supabase active `premium_grants` 与 active `premium_entitlements` 均已软撤销为 0；RevenueCat customer `BB7A3977-3BBC-447B-989A-3FBE6B8D8EB6` active entitlements 为空。后续必须使用全新 Apple Sandbox Account 新购，避免旧 Apple 订阅历史污染。
  - Supabase MCP `_list_migrations` 确认 `premium_entitlements_and_solo_sync_gate` 与 `restrict_premium_gate_function_execute` 均已在远端。
  - Supabase SQL 权限检查确认新增函数 `anon_exec=false`。
  - Supabase MCP `_list_edge_functions` 确认 `revenuecat-webhook` active；`curl` 无授权请求确认返回 `401 UNAUTHORIZED`。
  - RevenueCat MCP 确认 webhook integration 已存在；Supabase secrets list 确认 `REVENUECAT_WEBHOOK_AUTHORIZATION` 与 `REVENUECAT_PRO_ENTITLEMENT_ID` 已配置；带授权但缺少 `app_user_id` 的 webhook 请求返回 `400 INVALID_APP_USER_ID`，说明 Authorization 已通过。
  - Build 30 已从当前 `main` archive 并上传到 App Store Connect / TestFlight；`xcodebuild -exportArchive` 返回 `Upload succeeded`。
- 最新功能进度：Task 9 `Full Regression and Project Memory` 已完成；双人任务卡片聊天主链路已完成本地回归；自定义 morph overlay 已废弃，聊天面板改用 SwiftUI 原生 `.navigationTransition(.zoom)`。
- 2026-05-04 Today 任务 Widget 第一阶段已落地到 `main`：小号 Focus、小号/中号 List 均读 App Group snapshot；显示“还剩 N 项”、任务右侧截止时间、左侧虚线圆角方形完成框；点击完成框运行 `TodayTaskCompletionIntent`，写共享 SwiftData store、记录 `.task/.complete` outbox、更新 snapshot 并 reload widget；其他区域通过 `together://today` 进入 App Today。
- Today Widget 关键文件：`Together/WidgetSupport/*` 负责 App 侧 snapshot/context/write/gateway；`TogetherWidget/TodayWidgets.swift` 负责 Widget UI；`TogetherWidget/TodayTaskCompletionIntent.swift` 负责 extension 内完成动作；`TogetherWidget/TodayWidgetShared.swift` 是 extension-safe DTO/store；`Together.xcodeproj` 已新增 `TogetherWidget` extension target；App 与 extension entitlements 均使用 `group.com.pigdog.together.shared`。
- 2026-05-04 Today Widget 数据一致性修复：App 与 Widget context key 统一为 `today-widget-context`；snapshot 现在保存完整 Today 待办队列，Widget 显示层再按 Focus/List 截断为 1/3 条，因此完成首屏任务后隐藏的第 4 条可自然补位；完成 intent 先写入 `animatingCompletionTaskIDs` 生成勾选填充帧，再由后续 timeline 帧移除任务；App 回到 active 时会刷新 HomeViewModel、重写 widget snapshot、reload widget timelines，并触发当前 space 的既有同步路径推动 widget 写入的 outbox。
- Today Widget 已知边界：带签名的真机/TestFlight 构建前，Apple Developer App ID / provisioning profile 必须开启 App Groups 并包含 `group.com.pigdog.together.shared`；本地 `CODE_SIGNING_ALLOWED=NO` 构建已通过但不等价于签名配置完成。`xcodebuild` 会反复删除 `Together.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved`，提交前需用 git 恢复，不能提交该删除。
- 已完成并提交：
  - Today Widget：App Group store 迁移、snapshot 生成/刷新、deep link、completion gateway、extension target、真实 Widget UI 与 AppIntent 已分步提交。
  - Task 1：Supabase `task_messages` comment 约束与 RLS guard，提交 `3e5e480`。
  - Task 2 + Task 3：`TaskMessageType`、`TaskMessageCursor`、`PersistentTaskMessage.content`、`PersistentTaskChatReadState`、`TaskMessageRepositoryProtocol`、local/mock repository 和 repository 测试，提交 `7292bbd`。
  - Task 4：应用服务将用户留言写入 `task_messages`，`assignmentMessages` 作为 legacy fallback；Task 4 comment 写入保持 local-only，不记录 `.taskMessage` sync；`sendReminderToPartner` 的 nudge sync 未改，提交 `0796700`。
  - Task 5：`TaskMessagePushDTO` 支持 `content` 和 `sender_supabase_user_id`；新增 `TaskMessagePullDTO`；Supabase catch-up 在 `pullTasks` 后拉取 `task_messages`；Realtime 监听 `task_messages`；`sendTaskComment` 在本地 comment 写入成功后恢复 `.taskMessage` outbox enqueue。
  - Task 6：新增 `TaskChatTimelineEntry` / `TaskChatTimelineBuilder` / `TaskChatViewModel`，聊天面板状态与 Home 卡片保持解耦；ViewModel 支持加载最近消息、发送 comment、500 字上限、本地已读游标。
  - Task 7：Home 卡片预览切到 `task_messages` 最新 comment，`assignmentMessages` 只保留 legacy fallback；`HomeTimelineEntry` 增加 latest comment / unread 状态，留言区域建立 44pt 以上触控入口。
  - Task 8：新增 `TaskChatPanelView`，实现任务聊天面板、头像消息气泡、nudge/system 居中状态、`TextField(axis: .vertical)` composer、Material 背景模糊和 Reduce Motion 降级；Home 使用稳定的 selected chat ViewModel，避免 body 重建导致输入态丢失。
  - Task 9：完成 focused tests、完整单元测试和 simulator build 回归，并记录阶段性项目记忆。
- 最近验证：
  - Today Widget：`git diff --check` 通过；`plutil -lint Together/Info.plist Together/Together.entitlements TogetherWidget/Info.plist TogetherWidget/TogetherWidget.entitlements` 通过；`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；focused widget/persistence test build-for-testing 通过；未做真机 widget 交互验证。
  - Today Widget 数据一致性修复：focused `TodayWidgetSnapshotStoreTests` / `TodayWidgetSnapshotBuilderTests` / `TodayWidgetSnapshotWriterTests` build-for-testing 通过；`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -allowProvisioningUpdates -quiet` 通过；未做真机 widget 交互复测。
  - Task 2/3：`TogetherTests/TaskMessageRepositoryTests` 通过；一次 review 代理额外跑过完整 `Together` 测试 483/483 通过。
  - Task 4：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -only-testing:TogetherTests/SendReminderToPartnerTests` 通过；`git diff --check` 通过。
- Task 5：`git diff --check` 通过；`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TaskMessagePushDTOTests -only-testing:TogetherTests/TaskMessageSyncTests ...` 通过 DTO / pull DTO 测试；`xcodebuild test-without-building -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests` 通过应用层 outbox 相关测试。
- Task 6：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TaskChatViewModelTests` 通过；覆盖 timeline 排序、忽略未知消息、load 标记已读、send trim/append/markRead、超长拦截。
- Task 7：`xcodebuild test-without-building -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests` 通过，新增 `homeViewModelPairPreviewUsesLatestTaskMessageComment`；`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17'` 通过；`git diff --check` 通过。
- Task 8：`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17'` 通过；`git diff --check` 通过。
- Task 9：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TaskMessageRepositoryTests -only-testing:TogetherTests/TaskMessagePushDTOTests -only-testing:TogetherTests/SendReminderToPartnerTests -only-testing:TogetherTests/TaskChatViewModelTests` 通过；完整 `xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17'` 通过；`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17'` 通过。
- 立即续接点：进行真机 UI/交互验收，重点看原生 zoom 转场、聊天页键盘表现、发送后卡片预览/未读刷新、双端 Supabase catch-up/realtime。
- Task 5 后续真机/后端验收：当前只做本地编译与单测，未对生产 Supabase 实际执行 `task_messages` pull/realtime 联调；上线前需要用双端真机验证 comment 离线重试、catch-up 拉回、Realtime 刷新。
- 已知后续风险：
  - `PersistentTaskChatReadState` 当前只存 `lastReadMessageCreatedAt`，未来做 unread 精确计算时可能需要升级为 `(createdAt, messageID)` 游标。
  - Calendar 预览仍读 legacy `assignmentMessages`；后续如要保持跨页面一致，需要单独把 Calendar 侧预览接到 `task_messages`。
  - SwiftData CloudKit 当前未启用；新 read-state model 的非 optional 字段未来启用 CloudKit 前需统一评估默认值/optional 策略。

## 可信上下文来源

- `PRODUCT_SPEC.md`：产品定位、MVP 范围、信息架构、页面需求。
- `DEVELOPMENT_GUIDELINES.md`：工程约束、架构原则、测试与验证要求。
- `DESIGN_GUIDELINES.md`：视觉基线、安全区域、动效和 Pencil 出图规则。
- `ARCHITECTURE.md`：当前架构方向、Space 演进策略、导航结构。
- `DATA_MODEL.md`：Task / TaskList / Project / Reminder / Space 模型方向。
- `TODO_NEXT_STEPS.md`：近期优先级和明确不要做的事项。
- `docs/superpowers/plans/*`：历史计划与阶段执行记录，使用前需和当前代码校验。

## 已落地机制

- 根目录 `AGENTS.md` 已追加项目记忆与阶段性收尾规则。
- `.agents/skills/stage-memory-update/SKILL.md` 已作为阶段性收尾 Skill。
- `docs/CODEX_MEMORY_WORKFLOW.md` 记录 Codex 记忆机制使用方式。
- `docs/CHRONICLE_RISK_ASSESSMENT.md` 记录 Chronicle 风险评估。

## 产品记忆

- 产品定位：纯单人 Todo 效率工具，不再扩展双人或多人协作。
- V1 一级结构：Today、清单、项目、日历、我。
- Task 是当前 IA 核心对象；清单、项目、日历是不同观察维度。
- 当前首页 UI 保留，语义上作为单人模式 Today 首页继续深化。
- 决策、纪念日、邀请、绑定、关系页属于旧逻辑遗留，后续应删除或下线，不作为产品路线。
- 当前不做多人模式、社区、内容流、开放社交，也不做只为视觉好看的复杂交互。
- OCR 导入规则：OCR 结果先进入草稿确认页，用户确认前不得写入真实任务或项目。

## 工程记忆

- 平台：iPhone only。
- 技术：SwiftUI 优先；仅在 SwiftUI 无法合理实现时局部使用 UIKit。
- 环境目标：Xcode 26.2、Swift、iOS 18+。
- 当前阶段：本地 SwiftData + CloudKit private database；不得继续保留 Supabase 或 RevenueCat 作为核心链路。
- Swift 代码按 Swift 6.2+ 现代写法推进，优先 `async/await` 和 `@Observable`。
- 新开发不得继续扩散 `spaceID / PairSpace / relationshipID / Invite / BindingState / Premium / Supabase` 语义；这些属于删除对象。
- 旧 store 和旧 Supabase 数据允许删除；如果为安全启动保留临时兼容层，必须标明删除阶段和验证方式。
- 状态机逻辑、数据模型转换、repository 行为和关键 ViewModel 行为必须可单测。

## 同步与数据恢复记忆

- 单人模式的稳定恢复主链路改为 CloudKit private database；Supabase 不再作为 canonical backend 或恢复依据。
- CloudKit 适合 Apple-only 辅助备份或系统级同步，但不适合承载 Together 当前的账号恢复、Pro 权限控制、后台诊断和跨端策略。
- 如果同时存在 CloudKit 与 Supabase，不要让二者共用一个“已确认”语义；CloudKit confirmed 不等于 Supabase 已入库。
- 对单人任务恢复问题，优先按层排查：本地 SwiftData 是否有任务、本地 `PersistentSyncChange` outbox 是否有记录、outbox lifecycle 是 `pending / sending / failed / confirmed` 哪一类、Supabase 是否已有对应 `tasks.id`、设备安装表是否是当前 build。
- 已验证事故模式：任务在本地 SwiftData 存在，CloudKit 也可能已有记录，但 outbox 被 CloudKit 标记为 `confirmed` 或因 `record to insert already exists` 变成 `failed`；如果 Supabase push 只扫 `pending / failed` 或再用 `lastPushedAt` 截断，就会漏推到 Supabase，重装后无法从 Supabase 拉回。
- 最终修复策略：`SupabaseSoloSyncService.pushPending` 对同一 solo space 扫描 `pending / failed / confirmed` 的 outbox；`confirmed` 只按是否支持 solo Supabase push 过滤，不再按 `lastPushedAt` 截断；Supabase upsert 成功后删除对应 outbox。
- 回归测试必须覆盖两类 CloudKit confirmed：晚于 `lastPushedAt` 的任务、早于 `lastPushedAt` 的任务；两者都应补推并清空 outbox。
- 真机验收标准：安装当前 build 后打开 60 秒，Supabase `tasks` 能查到测试任务；删除 App 后从 TestFlight 重装，同一账号打开 60 秒，首页能恢复任务。
- 双人邀请码链路的云端真相优先看 Supabase：`pair_invites.status=accepted`、`spaces.status=active`、`space_members` 是否有两行；如果这些成立，问题通常在本地 session 切换、catch-up 或 profile/avatar 回填。
- 已验证双人配对事故模式：`CloudPairingService` 在返回 `PairingContext` 前同步 await `PairJoinObserver.onSuccessfulPairJoin()`，通知权限弹窗和 Supabase sync 启动会阻塞 `ProfileViewModel.apply(pairingContext:)`，导致接受方云端已配对但 UI 仍显示单人模式。修复策略：先返回 context 让 UI 切到 pair，再异步触发通知权限、catch-up、profile push。
- 双人头像恢复不要只依赖新 pair space 的 `space_members.avatar_url`。用户重装或换机后本地头像原始文件可能缺失，但 `user_profiles` 仍有可复用的 user-scoped signed URL；`.memberProfile` push 在没有本地头像 bytes / 没有刚上传的 signed URL 时，应在 `avatar_asset_id` 匹配时回退使用 `user_profiles.avatar_url`。
- 已验证双人配对本地状态事故模式：Supabase `pair_invites=accepted`、`spaces=active`、`space_members` 两行都正确时，接受方仍可能停留单人 UI；根因是本地 SwiftData 里存在旧的未结束 pending pair residue，`currentPairingContext` / resolver 用无序 fetch 的 first 选中旧 pair，导致 available modes 没有 pair。修复策略：本地 pair 解析统一优先选择未结束、active、最新 activated/created 的 pair space，并用服务层测试覆盖“旧 pending + 新 active”。
- 已验证双人同步事故模式：同一对用户如果在 Supabase 中残留多个 `spaces.type='pair' and status='active'` 的双人成员空间，A 端写入最新空间成功，B 端仍可能停在旧本地 active pair，导致任务/纪念日看不到。修复策略：客户端启动恢复不能因“本地已有 active pair”直接跳过云端校验，必须查询当前用户所属的最新 active pair，补齐本地 `PersistentSpace / PersistentPairSpace / PersistentPairMembership`，并把旧本地 active pair 标记 ended/archived；Supabase `accept_pair_invite` RPC 在接受新邀请的同一事务中归档参与者旧 active pair spaces，生产库也要一次性归档历史旧 active pair spaces。
- 双人任务接收不要只依赖 Realtime。真机测试发现任务已进入 Supabase、Edge Function 也返回 `sent=1` 时，接收方仍可能不刷新；修复方向是让 pair APNs、回前台、下拉刷新都触发 Supabase `catchUp()`，并在 catch-up 后刷新首页/清单/项目/日历等 ViewModel。
- 双人解绑不能只靠在线 Realtime `space_members DELETE`。一方解绑后，另一方需要 APNs `pair_unbound` 事件和前台成员校验兜底：如果当前 pair space 在 Supabase 里已没有对方 `space_members` 行，本地应自动解除配对。
- 双人日志页的红色默认头像通常代表历史记录的 `completedByUserID` / `lastActionByUserID` 已无法解析到当前用户或伴侣；这类旧 pair/profile 迁移残留还可能因为 `creatorID` 漂移被 active-task 删除权限挡住。Logbook swipe 删除应允许对已完成历史行走 tombstone fallback，但不要启动即批量删除未知头像日志，避免误删合法历史。
- 双人任务身份判断不能依赖远端 `creator_id` / `sender_id` 中的设备本地 UUID。重装、TestFlight 更新或多设备会让这些本地 UUID 漂移，导致“我创建并指派给对方”的任务在当前设备被误判成“待我回应”。任务和任务留言 pull 时必须优先用 `creator_supabase_user_id` / `sender_supabase_user_id` 映射回当前设备的本地用户与伴侣用户；已有本地脏数据需要一次性全量身份回填。伴侣头像同理，远端 metadata 一致但本地头像文件缺失时也必须补下载，不能只看 avatar version/asset 是否变化。
- iOS widget extension 不能用只包含局部 `@Model` 的 SwiftData `ModelContainer` 打开 app group 里的完整 `Together.store`。这会让扩展用缺少配对、例行任务、项目等实体的 schema 触碰完整库，可能触发主 app 下次启动时 schema/probe 失败并进入危险恢复路径。Today widget 完成任务长期策略：扩展只做最小 SQLite 事务写入 `ZPERSISTENTITEM` / `ZPERSISTENTITEMOCCURRENCECOMPLETION` / `ZPERSISTENTSYNCCHANGE`，app 回前台后 reload Today 并刷新 widget snapshot；未来 widget 新动作也必须沿用“窄动作 + 最小共享写入”或抽出完整共享持久化模块，不能再复制局部 SwiftData 模型。
- `PersistenceController` 不应在完整 store 打不开时静默删除本地库。显式 debug reset 仍可用于开发者有意清空数据；普通启动失败应保留 store 并暴露错误，避免把 schema/迁移问题升级成配对关系、例行任务、项目等真实数据丢失。
- 已验证单人恢复事故模式：本地 SwiftData store 被重建或部分丢失时，`UserDefaults` 中 Supabase solo `migrationCompletedAt / lastPulledAt` 仍可能保留；启动走 `.hasBaseline` 后只按 `updated_at >= lastPulledAt` 拉增量，会漏掉远端旧的 `projects / project_subtasks / periodic_tasks`。修复策略：baseline 账号在当前恢复版本第一次启动时，先推送仍在本地的 outbox，再执行一次 `since:nil` 全量拉取并写入 baseline refresh version；之后恢复增量拉取。

## 设计与动效记忆

- 所有 iOS UI 设计和 SwiftUI 实现必须遵循 Apple 官方设计规范与安全区域约束。
- 当前视觉基线继续使用 `mobile-02-cleanminimal_light` 的干净留白、轻层次、弱阴影、低饱和方向。
- 产品表达应是“效率优先、带一点温度”，不是“情侣感优先”。
- 自定义动画必须状态驱动，拆成 2 到 3 个连续阶段，避免主线程重计算、同步 IO、动画中重排序。
- 自定义动画需验证 Reduce Motion 降级、连续触发和性能。
- 日志页偏信息浏览场景，不应把完成记录列表包在白色圆角分组卡片里；默认使用直接列表、保留 swipe actions，不再额外弹详情 sheet。随着历史任务变多，打开页应避免全量 hydration，优先 repository 层分页、轻量 aggregate 和 SwiftUI `List` 虚拟化。
- Today 重要日期胶囊采用单层胶囊分页，不在静止态露出叠放层；只有多个候选日期时显示 4pt 轻量分页点；横向切换优先使用 SwiftUI 原生横向 ScrollView 分页、scrollTargetBehavior 和 scrollTransition，不在核心文字内容上使用 blur，也不对分页内容叠加横向 offset，避免相邻胶囊互相压住。计数方式切换改为点按视觉上的计数区域，只有支持累计天数的纪念日才暴露为 Button，命中区域至少 44x44pt，生日/节日等不可切换日期显示为静态文本。Today 顶部内容区不再用渐变遮罩压住列表，列表初始内容不保留额外 10pt 空白首行。
- 双人任务聊天面板已放弃自定义 morph overlay 和手写 keyboard metrics。当前长期方向是用 SwiftUI 原生 `.matchedTransitionSource` + `.navigationTransition(.zoom)`：任务卡片作为 zoom source，聊天页作为 `NavigationStack` destination；键盘、安全区和返回转场优先交给系统处理，避免再叠加自定义全屏 overlay / GeometryReader / keyboard observer。
- Today widget 完成动画采用 snapshot 中的 `animatingCompletionTaskIDs` 驱动：第一帧保留被点任务并显示虚线框填充 + 勾选，第二帧再移除整行并让隐藏队列自然补位。即使最后一项完成后 `remainingCount == 0`，也必须先展示完成动画，再切换到空态，避免勾图标单独消失或最后一行直接跳空态。
- Today widget 补位动画采用独立 `appearingTaskIDs` transient state：被勾选任务整行消失后，补位任务先占位为透明和轻微下移，再做出现动画；持久化 snapshot 不应保存出现态，避免下次 reload 反复入场。widget 空态插画应使用 Today 主列表同款 `EmptyCalendar` 小猫占位图，不再使用 `EmptyList` 笔记本插画。
- Widget 使用 `.contentMarginsDisabled()` 后，背景和内容必须分层：背景通过 `.containerBackground(for: .widget)` full-bleed 铺满，前景内容必须显式读取 `@Environment(\.widgetContentMargins)` 并设置最小 padding，不能让标题、勾选框、计数或头像贴到圆角裁切边缘。WidgetKit `placeholder(in:)` 只承担结构占位；真实桌面数据必须来自非预览 `getSnapshot` / `getTimeline`，preview demo 只能在 `context.isPreview` 下出现，不能进入真机 timeline 或真实空态 fallback。
- 真机桌面 widget 可能在同一 `CFBundleVersion` 下复用旧 timeline 渲染缓存。排查 widget 数据/渲染问题时，如果 Console 已证明 provider 读到真实 snapshot，但桌面仍显示旧 UI、探针文案或骨架，应先提升主 App 与 widget extension 的 `CURRENT_PROJECT_VERSION` 重新安装，或删除重加 widget / 重启，再继续判断业务代码。

## 近期优先级

1. 把单人模式语义落到代码层：Task / List / Project / Calendar 为主，弱化旧双人优先命名。
2. 在当前首页 UI 基础上完成 Today 主链路：任务创建、完成、详情展开、日期切换与排序。
3. 明确并实现 `TaskStatus`、`ProjectStatus`、提醒策略单测。
4. 补齐清单页、项目页、周/月日历切换、本地持久化与高质量原生动效。

## 明确不要做

- 不先重写当前首页 UI。
- 不先做绑定流、邀请流、双人主链路。
- 不先做多人模式。
- 不把产品重新做成情侣运营或关系运营工具。
- 不为了快速交付把核心状态写死在 View 层。
- 不做只有炫技价值、没有任务效率价值的动画。

## Claude 历史资料处理原则

- `.claude/worktrees/*` 下的内容可作为历史参考，但不自动承认为当前事实。
- 若 Claude worktree、历史计划和当前根目录主文档冲突，以当前根目录主文档和当前代码为准。
- 合并历史经验时必须标注来源，并优先验证当前代码状态。

## 验证记录

- 2026-06-25：修复真机中文输入与首页完成状态相关问题。根因结论：把 SwiftUI 行内输入替换为 UIKit `UIViewRepresentable` 包装输入框，会和首页展开态 row identity、分区迁移动画、`@FocusState`、高度测量和中文输入法 marked text 生命周期冲突，表现为反复点击才聚焦、输入 1 个字符键盘关闭、尾部中文未落入绑定；最佳方案是高频 SwiftUI 行内输入回到原生 `TextField`，只在提交、失焦、收起或切换任务时读取本地 draft 并保存，提交前 `Task.yield()` 等待输入法落字。处理：删除 `IMETextInput` 及所有引用；首页展开态标题/备注/子任务、快速创建子任务、项目/例行事务/OCR/Profile 等输入均回到原生 SwiftUI 输入；Profile 移除旧双人模式的预设留言/quick reply 数据与 UI 残留；首页完成态使用 presentation identity 与完成后轻量 reconciliation，补齐完成迁移和重复任务 occurrence 测试。验证：`rg` 确认无 `IMETextInput` / quick reply 残留，`git diff --check` 通过，`DEVELOPER_DIR=/Users/papertiger/Downloads/Xcode-beta.app/Contents/Developer xcodebuild build -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,id=631C14FC-CD43-4DFA-90C0-7A540BCC7DA6' CODE_SIGNING_ALLOWED=NO -quiet` 通过，`DEVELOPER_DIR=/Users/papertiger/Downloads/Xcode-beta.app/Contents/Developer xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,id=631C14FC-CD43-4DFA-90C0-7A540BCC7DA6' -only-testing:TogetherTests/TogetherTests -quiet` 通过；仍需真机复测中文输入法、九宫格、表情键盘和展开态切换。
- 2026-06-25 follow-up：真机继续发现子任务输入“测试测试”后点“添加”只保存“测试”，额外输入空格才完整保存。补充根因：按钮点击时 SwiftUI `TextField` 绑定仍可能只包含上一段已提交文本，中文输入法 marked text 尚未落入 binding；只在 `.onSubmit` 里 `Task.yield()` 不覆盖按钮路径。处理：新增 `TextInputCommitter.waitForFocusedInputToCommit()`，提交前通过当前 responder 结束编辑并等待输入法提交；首页展开态、详情 sheet、快速创建、项目子任务和 OCR 子任务的“添加/Return/编辑提交”统一走安全提交路径，禁止按钮直接读取输入 state。验证：残留搜索确认子任务添加入口无 `Button("添加") { addSubtask() }` / `.onSubmit(addSubtask)`，`git diff --check` 通过，Xcode 27 beta app build 通过，`TogetherTests/TogetherTests` 通过；仍需真机复测“测试测试”不加空格直接点添加。
- 2026-06-25 follow-up 2：真机确认“测试任务”输入期间添加按钮仍置灰，说明中文 marked text 未进入 SwiftUI binding 时，按钮可用状态也不能依赖 `text.trimmingCharacters(...)`；编辑已有子任务也不能只靠键盘完成或失焦保存。处理：所有子任务添加入口改为“输入框聚焦时允许点击添加，提交后再根据最终文本 no-op 或新增”；首页展开态、详情 sheet、快速创建内联/面板、项目页编辑态新增明确保存勾选按钮，非编辑态仍保留删除/移除；编辑保存同样先等待输入法提交。验证：残留搜索确认无按 `trimmingCharacters` 直接禁用子任务添加按钮的入口，`git diff --check`、Xcode 27 beta app build、`TogetherTests/TogetherTests` 通过；仍需真机复测按钮置灰与编辑保存。
- 2026-06-25 follow-up 3：再次真机确认中文“测试”仍需空格才能保存，而数字正常。最终根因收敛：SwiftUI `TextField` 的 `Binding<String>` 在中文输入法 marked text 阶段不是可靠数据源；等待、失焦、按钮不置灰只能缓解交互，不能保证保存拿到可见文本。处理：新增极窄 `CompositionAwareTextField`，用 `UITextField` + coordinator 在 `editingChanged`、`textFieldDidChangeSelection`、begin/end editing 和 return 时实时同步 `textField.text`；只用于单行任务标题和子任务标题/新增/编辑，不再用于备注等多行正文；相关焦点状态从 SwiftUI `@FocusState` 降为普通 `@State`/`Binding`，避免 SwiftUI 焦点系统重建输入。覆盖：首页列表任务标题、首页展开子任务新增/编辑、详情 sheet 子任务、快速创建任务/项目子任务、项目页标题/子任务、OCR 草稿任务/子任务标题。验证：Xcode 27 beta app build 和 `TogetherTests/TogetherTests` 通过；仍需真机复测中文“测试”不加空格直接点添加/保存。
- 2026-06-25 follow-up 4：真机继续确认 `CompositionAwareTextField` 在动态 SwiftUI row 中不可用，且原生 `TextField` 通过微信输入法分两次输入中文时只保存第一次、只输入一次时保存为空。follow-up 3 的 `CompositionAwareTextField` 方案已废弃。最终策略：保留原生 SwiftUI `TextField`，但提交瞬间通过 `TextInputSnapshotReader` 从当前 UIKit 第一响应者读取真实可见文本，覆盖 SwiftUI binding 尚未收到的 marked text；首页展开态标题/备注/子任务、详情 sheet、项目页、OCR 草稿和快速创建子任务的保存/添加路径均先读取 snapshot 再提交。验证：旧桥接/延迟提交残留搜索为空，`git diff --check` 通过，Xcode 27 beta generic iOS build 通过，`TogetherTests/TogetherTests` 通过；仍需真机复测微信输入法一次输入和分段输入中文后的直接保存。
- 2026-06-19：落地已完成任务原生 toolbar 与精确筛选。`WeeklyCompletedSheet` 移除自绘 header，内层使用 `NavigationStack`、`navigationTitle("本周已完成")`、`navigationSubtitle("不含今天，共 N 项")` 和顶部 toolbar “查看全部”；完整 `CompletedHistoryView` 移除内容区 segmented picker，改为 `List` 根视图 + bottom toolbar 搜索 + 单个筛选按钮，并通过 `.matchedTransitionSource` / `.navigationTransition(.zoom)` 进入轻量筛选面板。`CompletedHistoryFilter` 改为支持 `.week`、`.month`、`.all`、`.specificMonth(Date)`、`.specificDay(Date)`，自定义日期会规范化，分组与右侧时间标签按筛选类型变化。`CompletedItemStats` 新增 `firstCompletedAt`，供月份筛选边界使用；同时修复 `LocalItemRepository.completedItemCount` 旧有 forced unwrap SwiftData predicate 隐患。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet`、XcodeBuildMCP `build_run_sim` / `stop_app_sim` 通过；仍需真机确认底部搜索与筛选按钮在大字体和小屏下不拥挤。
- 2026-06-20：调整已完成归档页搜索与筛选结构。`CompletedHistoryView` 顶部保留原生标题“已完成”与 `navigationSubtitle` 当前范围，右侧新增快捷筛选 `Menu`（本周 / 本月 / 全部）；底部改为 `safeAreaBar(edge: .bottom)` 常驻搜索条，右侧精确筛选按钮通过 matched geometry + scale/opacity/offset 呈现同页 popover-like 浮层，只负责具体日期和具体月份。`CompletedHistoryFilter.specificDay` 副标题改为完整 `yyyy年M月d日`；`CompletedHistoryViewModel.subtitle(for:)` 删除“未归类任务”兜底，无子任务和备注时显示“完成于 日期+时间”。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet`、XcodeBuildMCP `build_run_sim` / `stop_app_sim` 通过；仍有既有 Swift 6 actor isolation warning，本轮未处理。
- 2026-06-20：已完成归档页底部搜索与精确筛选视觉切到原生 Liquid Glass。`CompletedHistoryView` 的底部搜索区域在 iOS 26 使用 `GlassEffectContainer`、`.glassEffect(.regular.interactive(), in: Capsule)`、筛选按钮 `.buttonStyle(.glass)` 和浮层 `.glassEffect(.regular, in: RoundedRectangle)`；精确筛选浮层关闭按钮使用 `.glass`，月份确认按钮使用 `.glassProminent`。旧系统保留原来的自定义半透明 fallback。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、XcodeBuildMCP `build_run_sim` / `stop_app_sim` 通过；generic `xcodebuild build` 曾无输出卡住后已中断，未作为最终验证证据。
- 2026-06-20：按真机反馈撤销已完成归档页自定义底部搜索条。`CompletedHistoryView` 删除 `safeAreaBar`、自绘 `TextField` 搜索胶囊、筛选按钮自定义 glass modifier 和 matched geometry source；搜索回到系统 `.searchable(text:prompt:)`，并用 iOS 26 `DefaultToolbarItem(kind: .search, placement: .bottomBar)` 放到底部 toolbar；精确筛选按钮改为 `ToolbarItem(placement: .bottomBar)`。精确筛选内容浮层仍使用原生 `.glassEffect` 材质。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、XcodeBuildMCP `build_run_sim` / `stop_app_sim` 通过。
- 2026-06-20：按真机反馈将已完成归档页精确筛选器改为原生 sheet。底部 toolbar 筛选按钮图标从 `calendar.badge.magnifyingglass` 改为稳定显示的 `line.3.horizontal.decrease.circle` / `.fill`；`CompletedHistoryPreciseFilterSheet` 使用 `.presentationDetents([.height(540)])` 固定高度，内部用固定 `340pt` 内容区承载日期 `DatePicker(.graphical)` 与月份 wheel + 确认按钮，切换“日期/月份”时 sheet 高度不再变化。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、XcodeBuildMCP `build_run_sim` / `stop_app_sim` 通过。
- 2026-06-19：修复“本周已完成”sheet 出现 `共 N 项` 但列表空白的问题。`HomeViewModel.loadWeeklyCompletedSheet` 将列表分页加载与统计加载拆开容错：列表失败只影响列表并记录 `didFailLoadingWeeklyCompletedSheet`，统计失败则回退为已加载列表数量，避免统计成功但列表失败时被误判成“没有历史完成任务”。`WeeklyCompletedSheet` 的空态改为区分真实无数据与列表暂时无法加载；本周已完成 sheet 移除固定 `.presentationCornerRadius(28)`，交回系统原生 sheet 圆角以适配不同屏幕圆角。`LocalItemRepository.fetchCompletedItems` 在完成历史 hydration 失败时降级为轻量 `domainModel()` 行，避免历史页/本周 sheet 因个别关联数据异常整体空白。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet`、XcodeBuildMCP `build_run_sim` 通过；XcodeBuildMCP `stop_app_sim` 因模拟器已处于 Shutting Down 返回失败，不影响构建运行结果。仍需真机安装最新包后确认 sheet 列表能展示 17 项历史任务。
- 2026-06-19：修复首页真机反馈的子任务和本周已完成区细节。子任务展开后，子任务虚线勾选框通过视觉 inset 补偿与父任务完成框右边缘对齐；子任务收起不再直接移除布局，而是测量自然高度并将容器高度动画到 0，避免下方任务闪跳。首页已完成胶囊标题固定为“本周已完成”；本周已完成任务按 `completedAt` 从周五到周一倒序展示，右侧显示“周五/周四/…”而不是日期。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；仍需真机确认收起动画手感和框线对齐。
- 2026-06-19：首页性能与 toolbar 布局收敛。首页数据源改走 `ItemRepository.fetchHomeItems`，只 hydrate 未完成任务与当前工作周已完成任务；已完成工作周范围由 `HomeCompletedDisplayRange` 统一计算为周一 00:00 <= `completedAt` < 周六 00:00。全部已完成历史保留在 `CompletedHistoryView`，从底部 toolbar 独立进入并显示标题“已完成”。首页和例行任务页标题统一使用原生 inline navigation title；顶部左侧为例行任务入口、右侧为收窄头像按钮，底部左侧为已完成历史，底部右侧为 OCR 与 plus。任务日期标签改为 `M月d日` 或具体时间；有子任务的任务行显示 `0/3 子任务` 并取代“进行中”，子任务展开时保留稳定 row 身份和显式 snappy 动画，子任务标题与父任务标题左对齐。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet`、XcodeBuildMCP `build_run_sim` 通过；仍需真机用 140+ 已完成任务验证滚动体感、Reduce Motion、大字体和深色模式。
- 2026-06-14：强化任务本地通知调度机制。任务提醒规则改为：显式 `remindAt` 优先；未设置提醒但有 `dueAt` 的任务，按到期时间调度本地通知；无到期时间且未完成的任务按 space 聚合为每日 18:00 汇总通知，标题显示当天剩余件数。任务创建、更新、移动、改期、推迟、完成/恢复、归档、删除后都会刷新单条任务提醒和 18:00 汇总；App 启动后会从当前 space 的 task/project 数据重建提醒。当前纯单人 Apple-only 边界下没有新增远端 APNs provider，通知投递仍依赖 iOS 本地通知授权和 `UNUserNotificationCenter`。验证：新增 `TogetherTests` 覆盖显式提醒、到期时间兜底、无到期任务 18:00 汇总；`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TogetherTests -quiet` 通过；`git diff --check` 通过。
- 2026-07-09：普通任务接入系统闹钟提醒。`LocalReminderScheduler.syncTaskReminder(for:)` 成为普通任务本地通知 / AlarmKit 闹钟统一调度入口：未完成、未归档、非 legacy 重复、同时具备明确日期、明确时间和 `remindAt` 的普通任务会优先请求并使用现有 AlarmKit 服务；授权拒绝、不可用或调度失败时自动降级为本地通知。成功调度闹钟会取消同任务本地通知，走通知时会取消同任务闹钟；删除、完成、清除时间或提醒会同时取消旧通知和旧闹钟。未新增任务模型字段或 SwiftData migration，`NSAlarmKitUsageDescription` 已扩展为任务与例行任务强提醒。验证：`git diff --check` 通过，App Swift 源码 `swiftc -typecheck` 通过；`xcodebuild build-for-testing/test -destination 'generic/platform=iOS'` 因本机未安装 iOS 26.5 platform 被 Xcode destination 阻塞，未作为通过证据。
  - 2026-07-10：Profile 二次收敛为项目同源的平铺 `ScrollView`：首页只保留提醒与闹钟 sheet、已完成自动归档（`Toggle + Menu`）、应用锁定、iCloud 状态 sheet、关于和必要的数据删除页。首页、提醒 sheet 与同步 sheet 均使用 AppTheme 字体、留白层级和仅用于大分组的弱分隔线，不使用行间分隔线或 iOS 设置式 `List(.insetGrouped)` 白色卡片容器；sheet 用 `.presentationSizing(.fitted)` 按内容决定初始高度。原生 `Toggle`、`Menu` 与 sheet 只承载行为。提醒 sheet 将“临近状态”与实际通知权限明确分开，每日 18:00 汇总仅在通知开启后可启用；通知拒绝时提供系统设置入口。移除 Profile 中的外观强制切换、缓存清理、默认推迟时间、快捷时间预设和重复反馈入口；外观统一跟随系统，历史 `defaultSnoozeMinutes` 持久化字段仅作兼容保留，不触发 SwiftData schema 迁移。同步页“检查 iCloud 状态”只检查状态，不再伪称手动同步或直达 iCloud 设置。`PersistentUserProfile.dailySummaryEnabled` 新建默认值统一为 `false`。
  - 2026-07-10：全局浅色背景从暖米白校准为中性冷白：`background`、`backgroundSoft`、`gradientBottom` 统一提高蓝灰比例，网格线 alpha 从 3.4% 降为 2.6%。深色模式、白色焦点卡、宠物暖色与语义强调色均保持不变；目标是在任务、例行任务与 Profile 的大面积留白中获得更白净通透的基线。
  - 2026-07-12：Profile 重新收敛为更轻、更直接的个人设置页。顶部保留横向 76pt 头像与姓名，移除“独立工作空间”；设置按“提醒 / 整理 / 隐私与数据 / 关于”用 36pt 组间留白分层，不再使用跨组分隔线。“提醒与闹钟” sheet 已删除，任务提醒、临近窗口和每日 18:00 汇总改为首页内联；离散值 `Menu` 只锚定行尾当前选项，标题和整行不触发。通知与 AlarmKit 对用户统一为一个“任务提醒”总开关：开启时申请通知并重建调度，符合条件的任务自动优先 AlarmKit；关闭时清除每日汇总并取消 Together 的任务通知/闹钟，且 `LocalReminderScheduler` 保留关闭状态以阻止后续任务变更重新调度。通知被拒绝时仅在分组内显示“前往设置”。`PRODUCT_SPEC.md`、`DESIGN_GUIDELINES.md` 与 `CONTEXT.md` 已同步。验证：`git diff --check`、全量 App Swift 源码 `swiftc -typecheck` 和 `TogetherTests.swift` 宏展开类型检查通过；新增关闭总开关取消通知与 AlarmKit 的回归测试，但 `xcodebuild test` 因当前 Xcode 26.5 没有可用的 iOS 26.5 Simulator destination 未执行。
- 2026-05-07：所有 widget 深色模式适配。新增 `TogetherWidget/WidgetTheme.swift` 统一 Today 与 Anniversary widget 的深浅色 token；Today widget 的背景、截止时间强调色、完成框、分割线、空态小猫插画在深色模式下自适应；Anniversary widget 的 avatar-derived material background、头像占位渐变、头像描边/阴影、数字强调色、倒计时胶囊和分割线在深色模式下自适应。长期规则已追加到 `AGENTS.md`：新增或修改 widget 时不能只硬编码浅色模式，必须显式处理 `colorScheme`，优先复用 widget theme。构建号提升到 44，避免真机桌面 widget 缓存旧 UI。仍需真机在系统浅色/深色模式下分别删除重加 Today 重点、Today 清单、纪念日三类 widget 验收。
- 2026-05-07：完成纪念日 widget 小号真机修复后的 UI 收尾与经验沉淀。UI：移除纪念日头像组右侧白色圆形心形图标；纪念日累计天数、周年倒计时和下一个节点天数均改用非本地化 `Text(verbatim:)` 展示，避免 SwiftUI `Text` 的本地化数字插值把 `2212` 渲染为 `2,212`。经验：本轮多次返工的核心教训已追加到 `AGENTS.md` Widget 专项规则，包括 snapshot 不得写入原始大图 Data、必须按 family 单独验证小号渲染、添加页 preview 不等于桌面 widget、数字显示需避免隐式本地化格式化。仍需真机安装最新 build 后确认小号纪念日 widget 正式 UI。
- 2026-05-07：继续排查“小号纪念日 widget 桌面纯白、中号/大号正常”。上一轮只移除了小号背景头像模糊，但小号前景头像仍会直接解码 App Group snapshot 中的原始头像 Data；真机历史日志显示两张头像合计约 485KB。新的根因判断：数据层、App Group、provider/timeline 均已排除，问题集中在 `systemSmall` 桌面快照归档时的真实头像解码/渲染成本。处理：`AnniversaryWidgetSnapshotBuilder` 在主 App 写入 snapshot 前把头像转成 widget 专用 320px 方形 JPEG 缩略图；`AnniversaryWidgetView` 对小号旧 snapshot 中过大的头像 Data 做防御性降级，先显示系统头像占位，避免整张小号 widget 被归档成空白；构建号提升到 42，避免 SpringBoard 复用旧 widget timeline 缓存。验证：`git diff --check` 通过；`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/AnniversaryWidgetSnapshotBuilderTests -quiet` 通过，并新增头像缩略图测试；`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过。仍需真机安装 build 42、打开 App 让 snapshot 重写，再删除并重加小号纪念日 widget 验收。
- 2026-05-07：针对“只有小号纪念日 widget 添加到桌面后空白，中号/大号正常”的真机反馈做最小修复。结构对比结论：Today widget 小号没有真实头像 Data 和高斯模糊背景；纪念日三种尺寸共用同一 App Group snapshot/provider/timeline，且中号/大号能显示真实头像与日期，说明后端数据、App Group 和 timeline 不是根因。最小修复只改渲染层：`systemSmall` 纪念日背景不再渲染真实头像高斯模糊层，只保留轻量 fallback gradient；中号/大号保留 avatar-derived background，但给背景头像图片加固定 frame 和 clipped，避免 WidgetKit 桌面快照归档时处理无界大图。验证：`git diff --check` 通过；`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/AnniversaryWidgetSnapshotBuilderTests -quiet` 通过；`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过。仍需真机删除并重加小号纪念日 widget 验收。
- 2026-05-05：定位纪念日小号 / 中号 widget 真机持续显示骨架或旧探针 UI 的真实原因。底层证据来自真机 Console：`[AnniversaryWidget] read snapshot ... exists=true paired=true hasStartDate=true avatars=2 avatarBytes=485435`，说明 App Group snapshot、配对状态、纪念日日期和头像图片均已被 widget extension 成功读取，问题不在后端数据、App Group 同步或 provider timeline。继续用最小探针验证：同一 `CFBundleVersion=36` 反复安装时，桌面 widget 仍可能停在旧骨架 / 旧探针；把构建号提升到 37 后，桌面立即显示新的 `SMALL V2 / MEDIUM V2` 探针，证明根因是 WidgetKit / SpringBoard 对同构建号 extension 的桌面渲染 timeline 缓存没有失效。处理：移除临时探针，恢复正式纪念日 UI，项目构建号提升到 38；保留根视图 `.unredacted()` 作为 placeholder redaction 兜底，但不再把 redaction 当最终根因。后续真机验证 widget UI/provider 改动，必须提升主 App 与 extension `CURRENT_PROJECT_VERSION`，或删除重加 widget / 重启后再判断。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/AnniversaryWidgetSnapshotBuilderTests -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；build 38 已安装到真机，App 与 TogetherWidget appex 的 `CFBundleVersion` 均为 38，构建产物中无 `SMALL V2 / MEDIUM V2` 探针字符串；仍需肉眼确认桌面小号/中号恢复正式 UI。
- 2026-05-04：修复真机添加 widget 后的两项反馈。`TodayFocusWidget` 小号不再复用清单式多行布局，改为“今日重点”单个优先任务视图，保留虚线完成框 AppIntent；`TodayListWidget` 小号继续作为最多 3 条任务的清单。纪念日 widget 骨架图问题按刷新链路加固：`AnniversaryWidgetEntry` 区分 placeholder 并仅对 placeholder 解除系统骨架 redaction；配对状态变化时立即刷新 anniversary snapshot；无当前用户/空间清空 snapshot 时也 reload anniversary timeline；真实纪念日从 `startDate != nil` 判定，支持“在一起 0 天”而不误入空态。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/AnniversaryWidgetSnapshotBuilderTests -only-testing:TogetherTests/TodayWidgetSnapshotBuilderTests -only-testing:TogetherTests/TodayWidgetSnapshotStoreTests -only-testing:TogetherTests/TodayWidgetSnapshotWriterTests -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；仍需真机重新安装/打开 App 后确认纪念日 widget 是否从真实 App Group snapshot 渲染。
- 2026-05-04：落地大号 Today widget 与双人纪念日 widget 第一版。Today 清单 widget 支持 `.systemLarge`，大号显示今日日期/星期并按当前 Today 排序自适应展示最多 6 条任务；小号/中号仍保持既有 3 条展示和完成动画链路。新增纪念日 widget kind `com.pigdog.Together.widgets.anniversary`，主 App 通过 `AnniversaryWidgetSnapshotWriter` 从当前 active pair 的 shared space 重要日期生成 App Group JSON 快照 `anniversary-widget-snapshot.json`，widget extension 只读取 DTO/头像 image data 渲染小/中/大号，不打开主 App SwiftData store；snapshot 保留 `startDate`，extension 按当前时间重算累计天数、周年倒计时和 100 天节点，避免用户不打开 App 时计数停留在旧快照。纪念日视觉采用圆角字体、头像左上覆盖顺序和 avatar-derived blurred background；AppContext 在 Today snapshot 刷新、重要日期变化、无当前用户/空间时同步刷新或清空纪念日 snapshot 并 reload timeline。验证：`git diff --check`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/AnniversaryWidgetSnapshotBuilderTests -only-testing:TogetherTests/TodayWidgetSnapshotBuilderTests -only-testing:TogetherTests/TodayWidgetSnapshotStoreTests -only-testing:TogetherTests/TodayWidgetSnapshotWriterTests -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；未做真机 widget 添加、时间线刷新、头像背景和 App Group 签名验收。
- 2026-05-04：修复真机 widget 全部显示假数据/默认头像与背景四周留白。根因是 WidgetKit 在真实桌面加载/失败/刷新前也可能走 `placeholder(in:)`，旧实现把 demo snapshot 用在非预览路径，导致真机短时或长期显示 520、示例日期和默认头像；同时未关闭 WidgetKit 默认 content margins，avatar-derived 背景无法覆盖整个容器。处理：Today/Anniversary provider 的非 preview placeholder 改为读取 App Group snapshot，只有 widget gallery preview 才使用 demo 数据；App Group snapshot 写入失败从静默 `try?` 改为显式日志，便于定位 entitlement/provisioning profile 未包含 `group.com.pigdog.together.shared`；Today/Anniversary widget 配置均加 `.contentMarginsDisabled()`。验证：`git diff --check`、widget snapshot focused tests、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；仍需真机安装新包、打开 App 写入 snapshot 后重加 widget 验收真实数据与背景贴边。
- 2026-05-04：按 WidgetKit 最佳实践修正上一轮真机 widget 方案。Today widget 保留 `.contentMarginsDisabled()` 让背景贴满，但前景内容改用 `widgetContentMargins` + 最小 padding，修复标题、完成框和“还剩 N 项”被圆角边缘裁切。Anniversary widget 不再让真实桌面 `placeholder(in:)` 读取业务数据或 demo 520；`placeholder` 只返回中性结构占位并解除 redaction，`getSnapshot(context.isPreview)` 才返回 demo，非预览 snapshot/timeline 只读取 App Group 真实 snapshot 或真实空态；空头像 fallback 改为中性头像，不再回退 `.placeholder.avatars`。Anniversary 背景移入 `.containerBackground(for: .widget)`，前景内容独立 padding；App 写入和 Widget 读取均记录 snapshot path、file exists、paired、startDate、avatars 和 avatarBytes，便于真机定位卡在写入、读取、解码还是 timeline。验证：`git diff --check`、focused widget snapshot tests、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；仍需安装新包后打开 App 一次、删除并重加 widget 真机验收。
- 2026-05-05：纪念日 widget “正在同步纪念日”排查中间结论。截图证明当时显示来自 `placeholder(in:)`，不是 App Group 真实空态；后续进一步用真机 Console 和最小探针确认，最终根因不是单 kind reload 缺失，而是同 `CFBundleVersion` 下桌面 widget 渲染缓存未失效。保留的长期规则是：`placeholder` 只能提供中性结构或 last-known snapshot，不承诺“打开 App 后会自动更新”；真实修复和验收必须以提升 build number / 删除重加 widget 后的桌面结果为准。
- 2026-05-04：继续修复 Today widget 与恢复链路真机反馈。UI：widget 完成后补位任务改为在被勾选任务彻底消失后再入场；空态资源从 `EmptyList` 改为 Today 同款 `EmptyCalendar`；单人模式标题旁空间名优先显示当前用户昵称；日志页空白根因是 `CompletedHistoryViewModel.isPairMode` 使用 `hasActivePairSpace`，导致只要有配对关系就过滤掉空态，已改为 `isViewingPairSpace` 并在双人空历史时展示中性空态。数据：Supabase solo baseline 增加一次性全量恢复版本，修复旧游标导致远端旧项目、项目子任务和例行事务不恢复的问题；solo recovery 成功后无论是否展示启动恢复 UI 都刷新 Today / Lists / Projects / Calendar / Routines / widget。验证：`git diff --check`、`SupabaseSoloSyncServiceTests`、`TodayWidgetSnapshotStoreTests`、`TodayWidgetSnapshotBuilderTests`、`AppContextSoloMutationSyncTests`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过；未做真机 widget 动画验收。
- 2026-05-04：执行送审阻塞清单中仓库内可控项。修复 Profile Pro 入口旧“自定义主题”承诺；App 内隐私政策 / 服务条款摘要改为个人 Todo 为主、双人协作为可选扩展；`docs/legal/privacy-policy.md` 与 `docs/legal/terms-of-service.md` 更新为 2026-05-04 版本，隐私政策与本地 `PrivacyInfo.xcprivacy` 对齐，不额外声明广告、行为分析、崩溃分析或诊断数据采集；新增 `docs/superpowers/runbooks/2026-05-04-app-store-connect-submission-materials.md`，提供 ASC 描述、关键词、IAP 元数据、审核备注和隐私标签填写口径；更新送审阻塞清单到 build 36，并新增 Universal App / iPad 截图风险。法律文档已 push 到独立 `together-app-legal` commit `6385a3c`，线上 Privacy Policy / Terms 均已验证显示 2026-05-04 版本。验证：`plutil -lint Together/Info.plist Together/PrivacyInfo.xcprivacy` 通过；安全/隐私快扫未发现硬编码密钥、敏感 token 写 UserDefaults/日志、业务明文 HTTP 或 ATT 调用；`PrivacyInfo.xcprivacy` 已声明 UserDefaults 和 SystemBootTime required reason API；`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过。未做真机验收。
- 2026-05-04：基于当前双人任务提醒、底部 toolbar 系统蓝、双人模式 reload 频闪修复、例行任务 per-space 缓存与单人蓝色例行任务胶囊延迟闪出修复，打包上传 TestFlight build 36。构建号从 35 提升到 36；归档路径 `build/TestFlight-20260504-0306-build36/Together.xcarchive`，导出上传使用 `build/exportOptions-TestFlight-upload.plist`；App Store Connect 返回 `Upload succeeded` / `Uploaded package is processing`。验证链路：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、Release archive、`xcodebuild -exportArchive` 上传通过；未做模拟器或真机 UI 验收。
- 2026-05-04：修复切换双人模式时短时间连续频闪。根因不是数据库刷新本身，而是 Supabase `catchUp` / Realtime 通知会短时间触发多轮 `reloadAfterSync`，首页 `reloadRevision` 又参与 `ScrollView/List` 容器 `.id` 与动画，导致普通同步刷新被放大成整块内容反复销毁重建。处理：`AppContext` 新增 cause-aware single-flight reload 合并器，180ms 内合并 Supabase / pair member / partner avatar / important dates / startup restore 刷新请求，并保留 important dates scheduler 副作用；`HomeViewModel.reload` 新增 `HomeReloadReason`，双人模式切换、同步和启动恢复不再做整页 items spring；`HomeView.tasksContent` 的 startup/empty/timeline identity 移除 `reloadRevision`，只保留日期维度；`SupabaseSyncService.catchUp(notify:)` 允许 Realtime handler 内部补拉不重复广播。验证：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试，仍需 TestFlight/真机切换双人模式确认无连续空态/列表闪烁。
- 2026-05-04：按同一频闪根因排查单人 Today 列表、例行任务列表和头像区域。单人 Today 列表当前没有 `reloadRevision + .id` 的整块重建问题；头像区域保留 `.id(userProfileRevision)` 以支持同名头像文件重读，但 `SessionStore.currentUser` 和 `restorePersistedUserProfileIfNeeded(force:)` 改为用户资料真实变化时才推进 revision，避免同步无变化也重建头像按钮；例行任务列表移除首现 `.task` 与空间 `.task(id:)` 双加载，改为 `RoutinesViewModel.loadIfNeeded()` 和同步 `reload()` 静默刷新，避免普通同步反复进入 loading/empty。验证：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
- 2026-05-04：修复切回单人模式时蓝色例行任务胶囊延迟闪出。根因：`RoutinesViewModel.tasks` 是当前空间单份数据，双人同步会把它覆盖成双人空间任务；切回单人时 Today 先读到双人 tasks，`hasPendingTasks=false`，稍后单人 reload 完成后胶囊才突然插入。处理：`RoutinesViewModel` 新增 per-space `tasksBySpaceID` 缓存，切换空间时先同步恢复当前 space 缓存，再静默刷新；本地例行任务创建/更新/完成/删除后同步更新缓存；异步刷新返回时若用户已切到其他 space，只更新缓存不覆盖当前可见 tasks；首页头像模式按钮切换后立即调用 `restoreCachedTasksForCurrentSpace()` 并后台 `loadIfNeeded()`。验证：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
- 2026-05-04：收敛 Today widget 完成任务后的动画与数据一致性问题。根因：widget extension 过去用局部 SwiftData schema 打开完整 app group store，存在污染/触发主 app 自动删库恢复的风险；同时完成最后一项时 `remainingCount==0` 会跳过打勾动画直接空态。处理：`TodayTaskCompletionIntent` 改为 SQLite 最小事务写入任务完成、重复任务 occurrence completion 与 sync outbox，不再导入 SwiftData 或复制 `@Model`；`PersistenceController` 去掉 schema/probe 失败后的自动删库；widget snapshot 保留完整待办队列并用 `animatingCompletionTaskIDs` 做“填充打勾 -> 整行消失 -> 后续任务补位”；日志空态 `EmptyHistory` 去掉绿色背景块。验证：`git diff --check`、widget snapshot 相关 `xcodebuild test` 通过、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -allowProvisioningUpdates -quiet` 通过；未做真机 widget 手动验收。
- 2026-05-03：继续收敛 Today / Profile / Project / Routines UI 反馈。Today 非当天返回按钮改为与模式胶囊同款的轻量“返回今天”胶囊，黑色文字和浅色背景；例行事务创建会把当前周期 tab 写入 `router.pendingPeriodicCycle` 并传给全局 Composer，周期 tab 的“每季度”加 `lineLimit(1)` 和 `fixedSize` 防止换行；项目标题新增 context menu“编辑标题”，保留展开态点按标题编辑。项目排序从“子任务排序”纠偏为项目级排序：`Project` / `PersistentProject` / Supabase `ProjectDTO` / CloudKit codec / repository / ViewModel 均增加 `sortOrder`，项目列表长按进入排序模式后可上下移动项目；项目子任务排序入口已移除，子任务只保留新增、勾选、编辑。新增 `supabase/migrations/045_add_project_sort_order.sql`，远端 Supabase migration `add_project_sort_order` 已通过 Supabase MCP 应用成功。边界结论：Profile 当前走原生 `NavigationStack` push，LTR 系统语义固定为从右侧进入；“从左侧滑入”属于与原生导航方向冲突的自定义转场需求，不能在现有原生 push 上局部硬改，需单独确认是否接受自定义 overlay / UIKit transition 的稳定性取舍。验证：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
- 2026-05-03：完成 Today 返回按钮、模板空状态、Profile 导航和普通任务排序一轮 UI/数据收敛。Today 非当天返回按钮从玻璃按钮改为轻量浅蓝胶囊并保留 44pt 命中区；模板空状态 `EmptyStateCard` 支持中性背景，模板页不再显示绿色背景块；Profile 入口从 `fullScreenCover` 改为 root `NavigationStack` push，使用系统返回手势，Profile 内日志入口保留在 toolbar；普通 Today 任务和例行事务通过长按进入排序模式并使用 `List.onMove`，双人任务卡片不参与排序；项目子任务因位于自定义卡片而非 `List`，使用同一排序模式下的轻量上下移动控件，避免为了原生 move handle 重构项目卡片。新增 `Item.sortOrder` / `PersistentItem.sortOrder` / Supabase `tasks.sort_order` DTO 映射、CloudKit codable 字段、repository reorder 接口，以及迁移 `supabase/migrations/044_add_task_sort_order.sql`；远端 Supabase migration `add_task_sort_order` 已通过 Supabase MCP 应用成功。验证：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
- 2026-05-03：完成 App Store 上架前仓库内合规收敛并上传 TestFlight build 35。修复内容：App 内隐私政策 / 服务条款与 `docs/legal/*.md` 对齐；移除未实现语音输入对应的麦克风 / 语音识别权限键；`PrivacyInfo.xcprivacy` 补齐邮箱、头像照片、用户内容、购买历史并修正照片类型键；Grace 条款与代码一致为“仅保留 Logbook 全量访问”；新增开发者赠权 runbook 与 App Store 上架人工核对清单；Profile DEBUG 区域仍受 `#if DEBUG` 隔离。验证：`git diff --check`、`plutil -lint Together/Info.plist Together/PrivacyInfo.xcprivacy`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、Release archive、`xcodebuild -exportArchive` 上传通过；归档路径 `build/TestFlight-20260503-1238-build35/Together.xcarchive`；App Store Connect 返回 `Upload succeeded` / `Uploaded package is processing`。未做模拟器测试。法律文档已同步到独立 `together-app-legal` GitHub Pages 仓库 commit `513a2f3`，线上 Privacy Policy / Terms 均已验证显示 2026-05-03 版本。剩余人工项：按 `docs/superpowers/runbooks/2026-05-03-app-store-release-readiness.md` 核对 App Store Connect。
- 2026-05-03：执行支付订阅 clean-room 前置流程并上传 TestFlight build 34。新增 runbook `docs/superpowers/runbooks/2026-05-03-subscription-clean-room-validation.md`，明确 Apple Sandbox / RevenueCat / Supabase / App 四层状态边界和阻塞条件；测试用户 `bb7a3977-3bbc-447b-989a-3fbe6b8d8eb6` 的 Supabase active grant / active `pro` entitlement 已软撤销为 0，RevenueCat 对应 customer active entitlements 为空。代码修复包含购买后 pending/entitlement 延迟轮询和会员页进入 Paywall 前强制刷新 PremiumGate。构建号从 33 提升到 34；归档路径 `build/TestFlight-20260503-1016-build34/Together.xcarchive`；App Store Connect 返回 `Upload succeeded` / `Uploaded package is processing`。验证链路：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、Release archive、`xcodebuild -exportArchive` 上传通过；未做模拟器测试。
- 2026-05-03：基于当前 `main` 未提交修复打包上传 TestFlight build 33，用于验证月订阅购买后退出登录再登录仍保持 Pro。修复内容：`PremiumGate` 合并 RevenueCat SDK、`premium_grants` 与 Supabase webhook 写入的 `premium_entitlements`，新增 `SupabasePremiumEntitlementsLoader` 和回归测试。构建号从 32 提升到 33；归档路径记录在 `build/.last_archive_path`，导出上传使用 `build/exportOptions-TestFlight-upload.plist`；App Store Connect 返回 `Upload succeeded` / `Uploaded package is processing`。验证链路：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、Release `xcodebuild archive ... -destination 'generic/platform=iOS'`、`xcodebuild -exportArchive ...` 上传通过；未做模拟器测试。
- 2026-05-02：基于当前 `main` 未提交修复打包上传 TestFlight build 32，用于验证 build 31 真机反馈的双人任务身份误判与伴侣头像文件缺失补下载问题。构建号从 31 提升到 32；归档路径为 `build/TestFlight-20260502-1716-build32/Together.xcarchive`，导出上传使用 `build/exportOptions-TestFlight-upload.plist`，App Store Connect 返回 `Upload succeeded` / `Uploaded package is processing`。验证链路：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、Release `xcodebuild archive ... -destination 'generic/platform=iOS'`、`xcodebuild -exportArchive ...` 上传通过；未做模拟器测试。真机安装 build 32 后，用户确认伴侣头像与双人任务身份显示均已恢复。
- 2026-05-02：修复 build 31 真机反馈的双人任务身份与头像同步问题。根因：Supabase 中异常任务 `creator_supabase_user_id` 指向当前用户，但本地 UI 仍用历史设备本地 `creator_id` 判断是否可回应，导致创建者侧显示“待我回应 + 接受/拒绝”；伴侣头像远端 `user_profiles/space_members` 有 URL 和版本，但本地缺头像文件时旧逻辑因 metadata 未变化而跳过下载。处理：`SupabaseSyncService` 新增 `SupabaseIdentityMap`，任务/留言 pull 时用 Supabase auth uid 映射本地 user id，并对当前本地 user id 做一次性全量任务/留言身份回填；成员 pull 改为轻量全量校准，头像文件缺失也触发下载。新增任务创建者映射、留言发送者映射、头像缺文件修复测试。验证：`git diff --check`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
- 2026-05-02：基于 `main` commit `96acb2e` 打包上传 TestFlight build 31，用于替代 build 30 继续真机支付/恢复购买/RevenueCat webhook 端到端验证。归档与上传使用 Release scheme archive：`xcodebuild archive -project Together.xcodeproj -scheme Together -configuration Release -destination 'generic/platform=iOS' -archivePath ...` 和 `xcodebuild -exportArchive ... -exportOptionsPlist build/exportOptions-TestFlight-upload.plist`；App Store Connect 返回 `Upload succeeded`，包进入 processing。后续付费链路测试前需先确认安装来源为 TestFlight/Release 且 build 为 31，Profile 中不应出现 `开发者 (DEBUG)` 区域；若出现 DEBUG 区域，说明仍在运行 Xcode Debug Run 或旧包，不应继续支付验收。未做模拟器测试。
- 2026-05-02：修复双人任务创建后“看不到”的根因：用户标题中的“5点”被无 LLM 的智能提示误判为 05:00，创建后立即成为已超时任务；同时双人模式隐藏逾期胶囊，旧逻辑又把逾期任务从主列表移除，导致任务已写入 Supabase 但创建者 Today 不显示。处理：删除 `ComposerSmartSuggestionBar` 整套智能提示/解析逻辑；创建页新增 `TaskCreationDateValidator`，手动选择过去日期或过去时间时弹提示并阻止创建；双人模式没有逾期胶囊承接时，已存在逾期任务仍保留在主列表可见。验证：Supabase MCP 确认最近双人 task/comment 已入库；`rg` 确认智能提示符号无残留；`git diff --check`、`xcodebuild build-for-testing -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' -quiet` 通过；未做模拟器测试。
- 2026-05-02：废弃双人任务聊天自定义 morph overlay / keyboard metrics，改为原生 SwiftUI zoom 导航。`PairTimelineCard` 挂 `.matchedTransitionSource(id:entry.id, in: taskChatZoomNamespace)`，聊天页走 `.navigationDestination(isPresented:)` 和 `.navigationTransition(.zoom(sourceID:in:))`；删除 `TaskChatMorphOverlay`、`TaskChatKeyboardMetrics`、卡片 frame preference 链路。验证：`build_sim -quiet` 通过；`build_run_sim -quiet` 启动成功；模拟器停在登录页，未覆盖真实双人任务聊天键盘路径，仍需 TestFlight/真机用真实 pair 数据复测中文键盘、九宫格和表情键盘。
- 2026-05-02：双人任务卡片聊天方案落地：`task_messages` 成为任务聊天主数据源，`assignmentMessages` 仅保留旧数据兼容；新增任务内 comment、nudge/system timeline 聚合、latest comment 卡片预览、本地未读游标和 morph 聊天面板。验证：`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:TogetherTests/TaskMessageRepositoryTests -only-testing:TogetherTests/TaskMessagePushDTOTests -only-testing:TogetherTests/SendReminderToPartnerTests -only-testing:TogetherTests/TaskChatViewModelTests`、`xcodebuild test -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17'`、`xcodebuild build -project Together.xcodeproj -scheme Together -destination 'platform=iOS Simulator,name=iPhone 17'` 通过。
- 2026-04-29：初始化 Codex 项目记忆、阶段性收尾 Skill、Chronicle 风险评估；未修改业务代码，未运行 Xcode 构建。
- 2026-04-29：build 22 修复接受方 786 云端已配对但本地仍单人 UI 的问题；验证 `PairSpaceSummaryResolverAvatarTests` 与 `LocalPairingServiceUnbindIsolationTests` 通过。
- 2026-04-29：新增发起邀请 loading 状态、纪念日 sheet 内联新增区与纪念日胶囊长按计数切换；双人 APNs/前台 catch-up 与解绑成员校验完成本地实现。验证 `git diff --check`、iOS Simulator build、`profileViewModelExposesInviteCreationLoadingState` 通过；Supabase trigger 已应用，Edge Function 部署需要 `SUPABASE_ACCESS_TOKEN`。
- 2026-04-29：生产后端硬化完成第一轮：Supabase 已加固 `space_members` / `pair_invites` / avatar storage RLS，新增 `accept_pair_invite` 原子接受 RPC、主动过期旧邀请码、关键同步索引、邀请码唯一约束，撤销 trigger function 的客户端可执行权限，将业务表 RLS 从 `public` 收窄到 `authenticated`，补齐缺失外键索引，为核心 enum/status 字段加 DB check constraints，并归档只有过期邀请的旧单人成员 pair shell；客户端接受邀请改走 RPC，双人 avatar 上传路径改用 Supabase `auth.uid()`，并补强 pair 变更不进入 CloudKit 的过滤。验证 `git diff --check`、iOS Simulator build；Edge Function 部署仍需本机 `SUPABASE_ACCESS_TOKEN`。
- 2026-04-29：日志页改为直接 plain list，取消点击行弹详情 sheet，仅保留 swipe 删除/恢复；`CompletedHistoryViewModel.delete` 对 creatorID 漂移的旧完成历史行增加 repository tombstone fallback；`LocalItemRepository.fetchCompletedItems` 初始页改为 active/archived 分路限量候选并合并排序，Logbook hero stats 改用 `fetchCount` 与边界记录查询，避免打开页全量 Item hydration。验证 `git diff --check`、`CompletedHistoryViewModelPairTests`、iOS Simulator build 通过。
- 2026-04-30：修复双人最新空间恢复与生产库多 active pair 残留：`hydratePairSpaceFromCloudIfNeeded` 改为以 Supabase 最新 active pair 为准并补齐本地 shared space；`SpaceDTO` 拉取远端 status/archivedAt；前台 pair 校验会识别远端 space 已 archived；新增 `038_archive_previous_pair_spaces_on_accept.sql`，`accept_pair_invite` 会归档参与者旧 active pair spaces。生产库已归档同一对用户的 11 个旧 active pair spaces，只保留最新双人成员空间 active；最新空间内任务和纪念日均保留。验证 `git diff --check`、`PairSpaceSummaryResolverAvatarTests`、`LocalPairingServiceUnbindIsolationTests`、iOS Simulator build 通过。
- 2026-04-30：修复 build 25 真机反馈的纪念日累计天数体验：生产 Supabase 已应用 `shows_elapsed_days` 字段迁移并回填现有 anniversary；`AnniversaryCapsuleView` 长按改为与点按互斥的手势，避免长按切换被 Button 吞掉或触发打开 sheet；`ImportantDatesManagementView` 标题改回 inline 原生 toolbar 居中。验证 `git diff --check`、iOS Simulator build、完整 `xcodebuild test` 通过；仍需 TestFlight 真机确认长按手感。
- 2026-05-01：修复 build 27 真机反馈的纪念日胶囊 `.numericText` 生硬跳变、Profile 纪念日管理 sheet 与 Today 不一致、双人 APNs 后台推送不稳定。`AnniversaryCapsuleView` 右侧整段“还有/已经 N 天”改为稳定 Text + 显式 `.animation(..., value:)` + `.numericText(value:)`；`ImportantDatesManagementSheet` 成为 Today/Profile 共用入口；`send-push-notification` Edge Function 已部署到 Supabase，APNs 改为 production endpoint 优先，`BadDeviceToken` 时回退 sandbox，并补齐 task created / accepted / declined / completed / nudge 的事件类型和通用通知 tap 打开任务。验证 iOS Simulator build、Deno check 通过；生产 DB 迁移未执行，当前修复不依赖新 DB 列。
- 2026-05-01：定位并修复 TestFlight 真机 APNs 全部无通知的后端根因。生产库 `tasks INSERT/UPDATE`、`task_messages INSERT`、`space_members DELETE` trigger 存在，`device_tokens` 也有两端 64 位 token；失败点是 `notify_push_on_change()` 通过 `net.http_post` 调 Edge Function 时没有有效 Authorization，`net._http_response` 全部为 `401 UNAUTHORIZED_NO_AUTH_HEADER`。长期修复：`send-push-notification` 改为 `--no-verify-jwt` 部署，但函数内部强制校验 `X-Together-Webhook-Secret`；生产 DB 新增私有 `private.app_secrets` 存储 webhook secret，trigger 函数从私有表读取并带自定义 header。验证：无 header 直调函数返回 401；DB `net.http_post` 带私有 secret 调函数返回 200 `No members`；函数版本 11、`verify_jwt=false`；私有 secret 表对 `anon/authenticated` 无 select 权限；`git diff --check` 通过。下一步可进行 TestFlight 真机 APNs 验收。
- 2026-05-01：实现 Today 重要日期胶囊改版，分支 `codex/today-important-date-capsule`。新增 `ImportantDateStoredRecord` 保留 `createdAt`，`ImportantDateCapsulePlanner` 负责 anchor/手动分页全集/7 天自动提示池/排序/solar+lunar 时区稳定计算，`ImportantDateCapsulePreferences` 负责 per-date 选择、自动提示抑制与计数模式 JSON，`ImportantDateCapsulePagerView` 接入 HomeView。`AnniversaryCapsuleView` 不再用长按或旧全局 count key，倒计时显示使用 planner 传入的 `daysUntilOrToday`，避免回到 `ImportantDate.nextOccurrence` 的 `.current` 时区路径。生日编辑只能选择公历/农历年度重复，保存时兜底把 `.none` 规范化为 `.solarAnnual`。验证：`git diff --check main..HEAD`、iOS Simulator build、`ImportantDateCapsulePlannerTests`、`ImportantDateCapsulePreferencesTests`、`ImportantDateRecurrenceOptionsTests`、`ImportantDatesViewModelCapsuleTests` 通过；`build_run_sim` 在 iPhone 17 Pro simulator 启动成功并截图。仍需带真实 pair 数据做手动滑动、VoiceOver 与大字体视觉验收。
- 2026-05-01：修复真机反馈的 Today 顶部列表遮罩和重要日期胶囊切换问题。`HomeView.topChrome` 移除 20pt 渐变遮罩，`standardTimelineList` / `pairTimelineList` 移除 10pt 空白首行；`ImportantDateCapsulePagerView` 最终改为 SwiftUI 原生横向 `ScrollView` 分页、`scrollTargetBehavior(.paging)`、`scrollPosition` 和 `scrollTransition`，移除自定义 `DragGesture` / settle 状态 / blur / 横向 offset，避免粉色胶囊文字不可读和相邻日期胶囊重叠。验证：`git diff --check`、重要日期相关 22 个测试、iOS Simulator build 通过；仍需真机 pair 数据确认滑动手感。

- 2026-05-07：打包上传 TestFlight widget build。首次使用 build 44 归档 `build/TestFlight-20260507-1518-build44/Together.xcarchive` 上传时，App Store Connect 返回 90360：`TogetherWidget.appex` 缺少 `CFBundleDisplayName`。处理：`TogetherWidget/Info.plist` 新增 `CFBundleDisplayName=Together`，所有 target 构建号提升到 45；重新归档 `build/TestFlight-20260507-1531-build45/Together.xcarchive` 并使用 `build/exportOptions-TestFlight-upload.plist` 上传。验证：`plutil -lint TogetherWidget/Info.plist Together/Info.plist`、`git diff --check`、Release archive、归档内 Widget App Group entitlement 与 `CFBundleDisplayName` 检查通过；App Store Connect 返回 `Upload succeeded` / `Uploaded package is processing`。仍需 TestFlight processing 完成后安装 build 45 真机验收所有 widget 的真实数据、深色模式和桌面缓存刷新。

- 2026-07-11：完成“可靠性与核心流程”收敛。持久化启动失败不再自动删除 store，而是进入可重试错误页；移除 Apple 登录门槛，`PersonalIdentityService` 优先复用既有 profile / space，禁止改写历史 user、space、creator ID，空库只在用户明确开始后创建本地身份，CloudKit 初次导入后可合并临时空身份。个人数据删除改由 `PersonalDataDeletionService` 按完整 manifest 删除全部 10 类 SwiftData 对象、提醒、头像、Widget 快照和个人偏好，验证为空后才创建新身份；删除失败留在页面并提供重试。SwiftData schema、store URL 和 CloudKit private container 均未改变，`PersistentTaskList`、`listID`、legacy Project 模型继续保留用于旧数据/schema 兼容。
- 2026-07-11：OCR 确认页支持查看可复制原文、任务新增/删除/移动/相邻合并和子任务拆分，所有结构操作只改内存草稿，仍需用户确认后入库。Widget 与通知统一使用 `AppDeepLink`：`together://today` 回到根首页，`together://task/<uuid>` 刷新并验证任务后只发布一次高亮，缺失任务显示可重试错误；Widget 完成框继续只执行 AppIntent，任务文字打开详情。纪念日 Widget、不可达 Decisions 运行时、Lists 页面/ViewModel 已删除；Lists 持久化模型和 repository 未删除。首页搜索及紧急/逾期/无日期/有提醒组合筛选按用户最终决定彻底移除，首页恢复原有日期分组、紧急排序和逾期汇总。验证：残留搜索仅保留产品规格中的历史/禁止项、隐私政策“数据共享”和 DEBUG 显式 reset；`git diff --check`、四个 plist/entitlements `plutil -lint`、Xcode 27 beta generic iOS `build-for-testing` 通过。按用户要求未再等待模拟器测试守护进程；升级保留、同 iCloud 多设备恢复、重装恢复、离线合并、远端删除传播、Widget 点击和 OCR 相机/输入仍需真机验收。
- 2026-07-12：Today Widget 合并为单一“今日概览”配置并沿用 `today-list` kind；旧 `today-focus` 配置与自定义 `WidgetTheme` 删除。今日范围只计算逾期和今天到期任务，未来任务仅作为独立“下一项”预览；Snapshot 兼容旧 JSON，不修改 SwiftData / CloudKit schema。Image2 参考稿落地时不硬编码画布尺寸：移除 `.contentMarginsDisabled()`，直接使用 WidgetKit 系统边距与默认 `.containerBackground(.background, for: .widget)`，不加渐变、内部卡片或阴影；数字统一 Rounded + monospaced。小号显示待办数、线性进度和 `+`，不完成任务；中号顶部概览并最多显示两条 44pt 可完成任务；大号按真实高度在 3–5 行间选择，分为逾期/今天并在空间允许时显示下一项。新增 `together://new-task` 深链，三个尺寸的 `+` 直接打开现有任务创建 Sheet；完成失败仍回退到任务页。验证：深链与 Snapshot 测试已编译，`git diff --check`、四个 plist/entitlements `plutil -lint`、Xcode 27 beta generic iOS `build-for-testing` 通过；按用户要求未启动模拟器，仍需提升构建号或删除重加 Widget 后做真机三尺寸、tinted 模式、完成与新建入口验收。

- 2026-07-15：修复 iPhone 前台接收 Mac 远端删除后列表长期不刷新的 CloudKit import 时序竞态。静默推送仍用于提前触发合并刷新，但 `AppBootstrapper` 现在把成功的 `NSPersistentCloudKitContainer` `.import` 事件作为数据已落入 SwiftData 的权威信号；身份恢复完成或 App 已 ready 后调用 `AppContext.handleSuccessfulCloudImport()`，复用现有 180ms single-flight 刷新首页、项目、例行任务和 Widget。合并器使用 revision 保留刷新执行期间到达的后续信号，避免 import 与提前刷新重叠时再次丢失；同步刷新改为真正 reload 已加载的例行任务。未改 SwiftData schema、store URL、CloudKit container 或 tombstone 语义；新增测试覆盖前台缓存仍持有任务、repository 已应用远端删除时，成功 import 会移除缓存任务。验证：`git diff --check`、改动 Swift 文件 parse、主 App iPhoneOS target 编译、`TogetherTests` iPhoneSimulator target 编译以及 iOS 26.4 Simulator 直接安装启动通过；本机 Xcode 只有 iOS 26.5 SDK、没有匹配的 26.5 Simulator runtime，标准 `xcodebuild test` 在 destination 解析阶段被环境阻塞，不能视为测试已执行。
- 2026-07-15：建立同级本地 Swift Package `/Users/papertiger/Desktop/TogetherCore`，iPhone 与原生 macOS target 都通过 `../TogetherCore` 引用同一份纯业务规则。第一阶段只共享普通任务创建默认值：创建即归入本地“今天”，`hasExplicitTime=false`；macOS repository 不再把新任务写成 `dueAt=nil`，避免 iPhone 将其归入按创建日期显示的独立分组。暂未移动生产 SwiftData `@Model`、repository 或 CloudKit container 装配，不触发 schema/store 迁移。正式 CI/多人协作前需把三目录收进 monorepo，或把 TogetherCore 建为独立版本化仓库。
- 2026-07-16：iPhone 生产持久化切换到 `TogetherCore.TogetherSchemaV1` 与 `TogetherMigrationPlan`，10 个本地重复 `@Model` 定义改为共享类型别名，iPhone 只保留 domain conversion / update 扩展；生产仍使用 App Group 内原 `Together.store` 和 `iCloud.com.pigdog.Together` private database，不删除或重建 store。新增 iPhone 兼容测试覆盖 unversioned disk store 以共享 V1 重开、共享模型集合、store 路径与 container identifier；`TogetherCore swift test`、macOS legacy V1 到共享 V1 精确 metadata/磁盘重开测试、iPhone generic build、test target 编译和签名构建通过。签名 App 已通过 `devicectl device install app` 覆盖安装到真机“猪皮小狗”（未卸载）；解锁后生产 App 正常启动，覆盖安装前的“今天 8 项”等既有数据在测试前后均完整可见，3 项真机共享 Schema 兼容测试全部通过，启动日志显示 `PersistenceController` 进入 ready 且未出现迁移或 store 错误。随后使用两个唯一 canary 在 Mac 与实体 iPhone 的生产 Store/CloudKit private database 完成双向闭环：Mac 创建 -> iPhone 导入并编辑/完成 -> Mac 导入并删除 -> iPhone 导入 tombstone，以及 iPhone 创建 -> Mac 导入并编辑/完成 -> iPhone 导入并删除 -> Mac 导入 tombstone；8 个分段测试全部通过，两个 canary 均已删除，临时生产 E2E 驱动已从代码移除。
- 2026-07-17：Production CloudKit schema 已部署。iPhone 前台同步刷新改为监听成功完成的 `NSPersistentCloudKitContainer` import，并通过 single-flight revision 合并连续信号；每次成功 import 立即刷新首页、项目、例行任务与 Widget，再以约 0.8 秒和 4 秒的 latest-wins 重试覆盖 SwiftData 合并可见性竞态。诊断日志仅在启动参数 `-TogetherCloudKitDiagnostics` 存在时输出。Production 签名 build 49 已覆盖安装真机，确认 CloudKit 与 APNs 均为 Production、`get-task-allow=false`；Mac 创建任务到达 iPhone 后，在 iPhone 持续前台时从 Mac 删除，iPhone import 成功且首页计数从 16 降至 15，任务无需切后台或重启即消失。新增回归测试覆盖远端删除在延迟 fetch 后从前台缓存移除；iPhone 全量 129 项测试、macOS 单元测试、macOS Debug 真签名 build 与两仓库 `git diff --check` 均通过。`TogetherCore` 已作为公开独立仓库发布 `1.0.0`，iPhone 与 macOS 工程改为使用 GitHub 远端版本依赖，并分别提交 `Package.resolved` 锁定验证版本；不再依赖本机 `../TogetherCore` 目录结构。
- 2026-07-18：双端色彩语义重新对齐。iPhone 设计系统删除未承担主界面职责的青绿色 `accent` / `accentSoft`；默认工具栏与普通操作继续由中性色和系统语义色承载，珊瑚色负责完成、紧急、今天/选中日期与温暖反馈，baby blue 只负责保存、推迟、定期任务选中和进度等轻量操作。空状态改用中性抬升表面，反馈页主要按钮改为自适应高对比的主文字色底；未修改数据模型、同步或业务语义。
- 2026-07-27：首页顶部模式入口从左上角循环图标改为原生分段控件，用户界面统一使用“待办 / 定期”和“待办任务 / 定期任务”；控件采用 144pt 紧凑双段胶囊，使用系统自适应的中性轨道与选中块，不使用品牌色填充，超大动态字体下自动退化为显示当前模式的原生 `Menu`。内部 `Task / Routines / PeriodicTask` 类型与持久化模型不变。同步更新 `PRODUCT_SPEC.md`、`DESIGN_GUIDELINES.md` 与 `CONTEXT.md`。验证：旧“例行任务 / 例行事务 / 周期事务”用户文案残留搜索为负，`git diff --check`、改动 Swift 文件 parse、同结构 SwiftUI compile probe 通过；完整 build/test 与模拟器视觉验收被本机 Xcode 26.5 Simulator SDK 缺少匹配 26.5 runtime 阻塞，当前仅安装 26.4 与 27.0 runtime。
- 2026-07-27：待办任务内联展开态的日期与时间入口均改为锚定原按钮的纯 SwiftUI 系统 Popover：日期使用宽 330pt、内部 306pt 的原生 Graphical DatePicker，高度随内容测量；时间使用约 276 × 196pt、136pt 可视高度的 Wheel DatePicker。两者均保留完成、始终可见的 `systemRed` 清除以及点击外部取消的 draft 语义，选择器与操作区无分割线，底部操作左右 18pt / 底部 8pt；新建任务日期 / 时间 Sheet、定期任务目标日 Menu、SwiftData schema 与提醒规则不变。曾尝试通过零尺寸 `UIViewControllerRepresentable` locator 修改 `UIPopoverPresentationController.permittedArrowDirections` 隐藏时间 Popover 箭头，真机确认会破坏系统出现 / 缩回动画，已完整删除该桥接；日期与时间 Popover 的箭头、定位和转场均接受系统行为，不再干预。验证：`git diff --check`、改动 Swift 文件 parse 和同结构日期 / 时间 SwiftUI popover compile probe 通过；generic iOS build 在 destination 解析阶段因本机缺少 iOS 26.5 platform 被拦截，未进入源码编译，仍需匹配环境下做真机视觉验收。
- 2026-07-27：用户确认将首页内联时间入口进一步收敛为系统 compact DatePicker，以换取参考图中的无箭头浮层和原位展开 / 收回动画。待办与定期任务现在直接由 `.datePickerStyle(.compact)` 承载时间入口，选择即时更新当前详情 draft，已配置时间通过原生长按菜单和 VoiceOver 自定义动作清除；删除原固定尺寸 Wheel 时间 Popover、内部清除 / 完成操作及其尺寸常量。日期 Graphical Popover、新建任务日期 / 时间 Sheet、收起卡片后统一持久化、SwiftData schema 与提醒规则不变。同步更新 `PRODUCT_SPEC.md` 与 `DESIGN_GUIDELINES.md`。验证：`git diff --check`、改动 Swift 文件 parse、Xcode 27 beta iOS Simulator `build-for-testing` 通过，`TogetherTests/TogetherTests` 129 项全部通过；模拟器生产启动受无 iCloud 账号下的 CloudKit 启动状态影响停留空白页，未完成实际点击动画与四列工具条视觉验收，仍需真机复验。
- 2026-07-27：首页内联时间入口再次按用户确认替换 `.compact`：待办与定期任务共用根视图级无箭头锚定浮层，内部继续使用原生 Wheel DatePicker，外壳约 276×216pt、滚轮可视高度 152pt，以稳定显示选中值及上下各两行。浮层根据按钮上下空间自适应展开方向，通过按钮侧动态变换原点执行无回弹缩放 + 淡变并原路收回；Reduce Motion 仅淡变。底部无分割线，使用 `systemRed`“清除时间”和 baby blue“完成”，滚轮 / 清除仅修改临时值，完成提交，点外部取消。新增纯几何测试覆盖上下方向与水平边缘约束；日期 Graphical 系统 Popover、新建任务 Sheet、模型、提醒与持久化语义不变。验证：`git diff --check`、Xcode 27 beta iOS Simulator `build-for-testing` 与完整 `TogetherTests/TogetherTests`（含 2 项新增几何测试）通过；仍需真机确认滚轮实际可见行数、入口锚点连续性、横竖屏、Dynamic Type、深色模式与 Reduce Motion 手感。
- 2026-07-28：首页内联时间浮层转场由近似的动态缩放原点升级为 SwiftUI 原生共享几何：根层 host 创建并向触发按钮注入同一 `Namespace`，按钮使用 `isSource: true`，浮层使用 `isSource: false`，由 `matchedGeometryEffect(properties: .frame)` 在按钮真实 frame 与自适应浮层 frame 之间执行 280ms 无回弹 `.smooth` 弹簧；出现与收起使用同一路径，动画可在当前呈现值上反向。触发按钮在浮层活跃时仅视觉隐藏但保留布局和 anchor，确保关闭可准确缩回；Reduce Motion 完全跳过共享几何，只保留 140ms 淡变。浮层尺寸、滚轮、清除 / 完成与草稿语义不变。验证：旧动态缩放转场残留搜索为空，`git diff --check`、Xcode 27 beta iOS Simulator `build-for-testing` 与完整 `TogetherTests/TogetherTests` 通过；共享几何的实际缩放质感与快速反向操作仍需真机确认。
- 2026-07-28：经 `grill-with-docs` 需求澄清，用户最终确认时间选择采用“系统呈现”：待办“时间”和定期“目标时间”统一使用 SwiftUI 原生 `.popover`，接受系统箭头，由 iOS 完整负责相对入口的定位、可用空间下的展开方向以及出现 / 收起动画。删除根视图 Overlay host、按钮 frame 偏好、上下方向几何计算和 `matchedGeometryEffect`；Popover 内容仍为 276pt 宽、252×152pt 原生 Wheel DatePicker、无分割线、系统红色“清除时间”和 baby blue“完成”，并保留滚轮 / 清除只改临时值、完成提交、点外取消的语义。验证：旧自定义浮层符号与当前规格残留搜索为空，`git diff --check`、Xcode 27 beta iOS Simulator `build-for-testing` 与完整 `TogetherTests/TogetherTests` 通过；编译仍报告仓库既有 Swift 6 actor-isolation warning，系统 Popover 的箭头方向和动画手感仍需真机确认。
- 2026-07-28：首页内联日期 Popover 由系统自由选择横向 / 纵向位置改为显式 `arrowEdge: .top`：箭头固定在 Popover 顶部，日历从日期按钮下方沿纵向展开，避免宽屏空间下系统选择左侧箭头并向右展开。Graphical DatePicker、330pt Popover、清除 / 完成、草稿提交和系统原生出现 / 收起动画均不变。验证：当前 Xcode 27 beta SDK 接口确认 `arrowEdge` 可限制 Popover 边缘，`git diff --check` 与 iOS Simulator `build-for-testing` 通过；完整测试在既有和全新临时 iOS 27 beta Simulator 上均因测试宿主 `launchd job spawn failed` 未进入测试用例，临时设备已删除，需在可正常启动 App 的模拟器或真机继续视觉与回归验收。
- 2026-07-28：待办与定期共用的任务创建器改为键盘上方悬浮形态。根层继续使用 SwiftUI 系统 Sheet 管理生命周期，底部 `+` 通过 `matchedTransitionSource` / `.navigationTransition(.zoom)` 原位展开和收回，Zoom 路径延迟约 320ms 后再聚焦标题；深链或非 toolbar 入口保留即时聚焦与系统默认呈现。Sheet 背景透明但保留系统暗层，禁止拖拽关闭；点击卡片外放弃草稿，卡片内空白只收起键盘，并提供 VoiceOver Escape / 自定义关闭动作。实体内容卡在键盘显示时贴近键盘上方 12pt、隐藏时居中；横屏、低高度和无障碍大字体使用完全内部滚动的退化布局，不侵入状态栏。确认按钮仅随既有 `hasMeaningfulContent` 出现，作为卡片同级的 60pt 原生 Regular Liquid Glass 控件使用 `.materialize`，在同一个 360ms、`extraBounce: 0` 事务中占用固定区域并使卡片同步缩小；移除原 overshoot、symbol bounce 和按压弹簧，Reduce Motion 直接切换布局。新增纯布局测试覆盖居中高度、键盘可用区、按钮占位和全滚动退化条件；改动 Swift 文件 parse、Liquid Glass / Zoom SDK compile probe 与 `git diff --check` 通过。完整 build/test 被当前 Xcode 报告 iOS 26.5 platform 未安装、无 eligible destination 阻塞，未执行模拟器视觉验收。
- 2026-07-28 真机修正：系统 Sheet 即使设置透明背景，仍会在悬浮卡背后保留可见的大尺寸圆角外壳。创建器外层因此改为透明 `fullScreenCover`，继续使用同一套系统 Zoom 转场，由创建器根层自行绘制轻暗背景并保持卡片安全区约束；这消除了额外 Sheet 外壳。移除通用 `keyboardDidHide` 自动恢复焦点，避免用户主动收起键盘后标题输入框立刻抢回焦点；子菜单和行内属性编辑器仍在各自关闭回调中按需恢复。模板列表背景改为透明，避免 `List` 在实体卡内形成第二块白色矩形。
- 2026-07-28 二次真机修正：创建器根层绘制的全屏暗层会被系统 Zoom 一并快照，转场中形成跟随卡片放大的灰色矩形。经 `grill-with-docs` 收敛后，Zoom 目标删除全部可见全屏背景，只保留透明命中层与实体卡片；首页根页面通过独立状态执行 180ms 无回弹的 3pt 模糊、0.78 饱和度和 9% 暗层，创建与关闭同步正反播放，Reduce Motion 只保留暗层淡变。创建器在主动关闭和保存成功前通过 `onWillDismiss` 提前驱动背景退场，外部路由关闭由 `activeComposer` 状态兜底。顶部类型统一为“模板 / 待办 / 定期”，使用浅灰实体轨道与白色自适应实体滑块，不再使用玻璃观感。验证：`git diff --check`、修改 Swift 文件 parse 和背景动画 API compile probe 通过；完整 `build-for-testing` 仍因当前 Xcode 找不到 Together scheme 的可用 destination 而未进入源码编译，需真机复验 Zoom 快照、背景独立动画、深色模式与 Reduce Motion。
- 2026-07-29：经 `grill-with-docs` 最终确认，任务创建器彻底移除 Sheet / fullScreenCover 与 toolbar Zoom 路径，改由 `AppRootView` 同层 `ZStack` 直接承载 `FloatingComposerView`。原页面独立执行 180ms 无回弹的 3pt 模糊、0.78 饱和度和 9% 暗层；卡片独立执行 220ms、`0.96 -> 1`、`y: 8 -> 0` 的淡变缩放位移并严格反向收起，Reduce Motion 仅短淡变，标题焦点延迟到卡片稳定后请求。顶部“模板 / 待办 / 定期”删除共用轨道，只保留当前项浅灰实体胶囊。创建器待办日期改用详情态 Graphical DatePicker 系统 Popover，待办与定期时间共用详情态 Wheel Popover 及清除 / 完成语义，提醒和定期目标日继续使用原生 Menu；日期与时间不再打开二级 Sheet。新增 ADR `docs/adr/0001-floating-composer-overlay.md`。验证：旧创建器 `fullScreenCover` / Zoom / 行内日期时间 Sheet 残留搜索为负，改动 Swift 文件 parse 与 `git diff --check` 通过；使用 iOS 26.5 SDK、临时排除资产目录并补充 TogetherCore module path 后，主 App 的 iPhoneOS 与 iPhoneSimulator target 以及 TogetherTests target 均完成真实 Swift 编译和链接。由于本机仅安装 iOS 26.4 / 27.0 Simulator runtime、Xcode SDK 为 iOS 26.5，标准 scheme destination 不可用，测试仅完成编译、未实际执行；仍需匹配 runtime 后运行测试并做真机动画、键盘、Popover、深色模式、Dynamic Type 与 Reduce Motion 验收。
- 2026-07-29：悬浮创建器进一步收敛为固定双区单页面编辑，ADR-0002 取代 ADR-0001 中的局部 Sheet / Menu 与缩放入场结论。提交按钮槽位始终占位，标题、备注和子任务位于固定正文滚动视口，属性工具栏下方只追加一个统一高度的内联属性区；待办日期、时间、提醒以及定期周期、目标日、目标时间、提醒均在该区域原位互斥切换，卡片顶部、正文、工具栏和提交按钮不随属性切换移动。待办日期默认今天且创建器不提供清除日期；时间清除会同时清除提醒，无时间时提醒面板只引导切换到时间；待办与定期草稿在单次创建会话内分别缓存。整卡从屏幕上边缘以无回弹 `.smooth` 进入/退出，内部级联不缩放，主按钮仅淡入；Reduce Motion 取消尺寸与位移动画。新增纯状态/布局测试覆盖固定按钮槽、统一属性高度、模式草稿隔离和时间/提醒依赖。验证：`git diff --check`、主 App Swift 编译/链接和 TogetherTests 编译/链接通过；标准资源 build 与测试执行仍被本机 iOS 26.5 SDK 缺少匹配 26.5 Simulator runtime 阻塞，未完成模拟器点击、旋转、Dynamic Type、深色模式与 Reduce Motion 视觉验收。
- 2026-07-29 follow-up：按真机截图与 `grill-with-docs` 决策修正创建器细节。外层总高度继续固定，但无效草稿时实体卡向下填满主按钮槽；顶部类型、标题与备注起点保持原位，新增空白只进入中间正文滚动视口，属性工具栏跟随卡片底边下移。草稿有效后只让卡片底边以 300ms 无回弹 `.smooth` 收短，正文视口从底部缩短、工具栏同步上移，主按钮从卡片后方升起并淡入；重新无效时严格反向播放，内部文字、控件和间距不缩放、不重新居中。时间滚轮改用临时值，待办与定期均由“保存”提交并收起，未保存离开则放弃，“清除时间”直接清除正式时间及关联提醒。日期属性区增至 352pt，常规布局以 336pt 固定高度承载六周 Graphical DatePicker，属性之间切换时保持日历视图身份；紧凑空间才允许内部滚动。顶部模式与属性选中态统一为 `systemGray5` 实色连续圆角矩形，无轨道、玻璃、渐变、描边、阴影或选中块移动动画；顶部三段文字不随选中状态变色或变粗，属性工具栏和编辑区删除独立背景层。验证：`git diff --check` 与 TogetherTests target 的 iPhoneSimulator Swift 编译/链接通过；测试执行仍因 Xcode 要求 iOS 26.5、机器仅安装 26.4/27.0 runtime 而无法启动，真机仍需复验日历六周高度、时间保存和按钮升降动效。
- 2026-07-29 转场重设：悬浮创建器保留根层 Overlay 与独立背景弱化。入场删除整屏 `.move(edge: .top)`，改为插值 `composerPresentationProgress`；卡片从第一帧起使用最终布局尺寸，根据实际居中或顶部锚点计算到屏幕上缘外 24pt 的路径，整组位移的同时仅对实体卡表面做顶部锚定的向下裁切，阴影在遮罩之后生成，提交按钮在进度 64% 后显现。入场为 360ms 无回弹 `.smooth`；退场不反向裁切，保持卡片完整并用 260ms 无回弹 `.move(edge: .top)` 沿屏幕上缘整体滑出。标题焦点延后到 400ms，避免键盘在卡片尚未稳定时改变几何。Reduce Motion 继续只做 140ms 淡变。新增布局测试覆盖居中与顶部锚定的上缘路径以及主按钮呈现区间。
- 2026-07-29 选中态统一：悬浮创建器顶部“模板 / 待办 / 定期”和属性工具栏删除 `systemGray5` 浅灰选中底块，统一改为与定期周期选择一致的 24×3pt Baby Blue 底部短线。每个选项持续保留等高指示槽，切换由共享几何执行约 220ms 无回弹 `.smooth`；Reduce Motion 下直接切换。顶部选中文字只提升为主文字色而不加粗，属性编辑入口补充 VoiceOver `isSelected`，紧急与子任务即时操作不显示编辑页指示条。验证：`git diff --check`、改动 Swift 文件 parse、主 App 和 TogetherTests target 的 iPhoneSimulator Swift 编译/链接通过；标准 scheme 构建与测试执行仍被 Xcode 26.5 SDK 与已安装 26.4/27.0 Simulator runtime 不匹配阻塞，需真机复验短线移动、大字体与 Reduce Motion。
- 2026-07-29 创建器真机修正：顶部“模板 / 待办 / 定期”删除每项额外横向 padding，默认最小宽度收敛为 64pt、项间距保持 4pt，解决三分类过度铺满卡片的问题。子任务使用 SwiftUI 本地 `FocusState`，旧 `dismissKeyboardOnly` 只注销标题和备注的 UIKit text view，导致从子任务输入直接点击日期时键盘仍为第一响应者，而属性区已同时展开。现统一向 responder chain 发送 resign action，并新增待展开属性状态：有键盘时先暂存目标属性，收到 overlap 归零后再执行属性区动画，从状态层禁止键盘与日历同时占位；退出创建器或快速连点时同步取消待展开状态。新增策略测试覆盖有/无键盘的延迟判断。验证：`git diff --check`、改动 Swift 文件 parse 和 TogetherTests target 的 iPhoneSimulator Swift 编译/链接通过；标准测试执行仍受 Xcode 26.5 SDK 与已安装 26.4/27.0 runtime 不匹配限制，需真机复验子任务到日期的键盘交接。
- 2026-07-29 创建器分类与模板页重设：顶部“模板 / 待办 / 定期”在紧凑容器内改为严格三等分，常规字号使用 200pt 总宽、4pt 间距和 64pt 单项宽，无障碍字号使用 248pt 总宽和 80pt 单项宽；Baby Blue 底部短线与既有选中语义不变。模板页删除每项深灰圆角矩形及嵌套卡片感，改为无静态底色的扁平系统列表：前置模板图标、17pt 标题、可选备注、时间/提醒/子任务/紧急轻量元数据与系统分隔线；按压只降低整行不透明度，无内容时使用原生 `ContentUnavailableView`，无障碍字号下元数据自动纵排。新增纯布局测试验证常规与无障碍字号下三项精确等宽。验证：`git diff --check`、改动 Swift 文件 parse 和 TogetherTests target 的 iPhoneSimulator Swift 编译/链接通过；编译只报告仓库既有 Swift 6 actor-isolation 等 warning，测试执行仍受 Xcode 26.5 SDK 与已安装 26.4/27.0 Simulator runtime 不匹配限制，需真机复验模板行密度、分隔线、深色模式和 Dynamic Type。
- 2026-07-30 创建器转场生命周期收敛：ADR-0003 将整卡入退场改为 `hidden / entering / visible / exiting` 单一状态机，路由关闭后继续保留视图直到 250ms 退场完成，快速反向操作通过 revision 忽略过期 completion。入场使用一个可插值进度：卡片首帧即锁定顶部和最终内部坐标，只从约 60pt 顶部实体切片向下揭示表面；类型切换、标题、备注、已有子任务和属性工具栏由同一进度在约 120ms 内做 4pt 轻级联，阴影只改变强度。标题焦点延后到 360ms 整卡稳定后请求，退场保持完整表面沿屏幕上缘划出。裁切未完成或退场后内容停止命中并从 VoiceOver 顺序隐藏；Reduce Motion 仅短淡变。新增纯函数测试覆盖表面揭示高度、完整退场距离、命中阈值和阶段边界。验证：`git diff --check`、Swift parse、主 App 与 TogetherTests iPhoneSimulator target 编译/链接通过；标准测试执行仍被 Xcode 26.5 SDK 与已安装 26.4/27.0 Simulator runtime 不匹配阻塞，需真机复验入场节奏、中断关闭、键盘顶部锚点、VoiceOver 与 Reduce Motion。
- 2026-07-30 创建器转场性能修正：真机掉帧审查确认 ADR-0003 的连续进度会逐帧写入 Environment，并同时驱动整卡长距离位移、动态 mask、阴影强度和全屏 blur / saturation，扩大 SwiftUI 更新与 GPU 合成成本。ADR-0004 改为 `hidden / mounted / presenting / visible / dismissing` 离散生命周期；创建器在最终锚点隐藏挂载并请求标题焦点，整组只以 240ms 的 `opacity + y: 8 -> 0` 原位显现，退出先注销键盘并以 200ms 向下淡出，内部级联只使用最多 4pt 位移与透明度。背景改为独立 9% 黑色 scrim，不再模糊或降饱和首页；卡片取消动态裁切和逐帧阴影变化。新增阶段语义测试，并保留 revision completion 守卫。验证：旧连续进度、travel、mask timeline 与背景 blur 残留搜索为空，`git diff --check`、Swift parse、全部 App 源码 iOS 26.5 Simulator SDK typecheck、生成 Together 模块后的全部 TogetherTests 源码与 Testing 宏 typecheck 均通过；标准 build/test 仍因 Xcode 26.5 SDK 与本机 26.4/27.0 Simulator runtime 不匹配而无法启动，真机仍需复验首次键盘协同、快速关闭、Reduce Motion 和实际帧率。
- 2026-07-30 创建器内部级联可见性修正：原实现让整卡显现与所有内部元素共用同一个 `isPresented` 信号，20ms 级联被父层 240ms 整体淡入遮蔽，真机观感等同整块出现。现增加一次性、可取消的内容揭示状态：卡片开始显现约 36ms 后，再按类型切换、标题、备注、已有子任务、属性工具栏顺序启动既有 20ms 错峰；只动画 `opacity + 4pt offset`，不修改 frame、字体、材质、模糊或阴影，也不在退出、属性切换或模式切换时重播。Reduce Motion 继续只依赖整卡短淡变。验证：Swift parse、`git diff --check`、全部 App 源码 iOS 26.5 Simulator SDK typecheck，以及生成 Together 模块后的全部 TogetherTests 源码与 Testing 宏 typecheck 均通过；实际 60/120Hz 帧率和级联可见度仍需真机 Instruments 验收。

- 2026-08-01：修复真机冷启动首次停留“正在加载任务”、再次打开停在启动欢迎页的问题。真机分段日志确认 SwiftData store 与个人身份均正常，根因是 ready 首帧同时触发 Home `loadIfNeeded`、日期 task、首次 scene active、隐藏的定期任务视图和 post-launch，形成 3 次首页查询 + 2 次定期任务查询；随后提醒全量重建会在 AlarmKit `.notDetermined` 时于冷启动静默请求授权，使 post-launch 长时间不结束。处理：首次日期 task 不重复查询、Routines `loadIfNeeded` 合并并发加载；隐藏的定期任务页不再预加载；只有真实进入 background 后的下一次 active 才刷新；提醒重建与 Widget 刷新移出首屏关键链；冷启动未获 AlarmKit 授权时降级为普通本地通知，权限仅由用户主动操作申请。未删除或重建任何本地数据。验证：`git diff --check` 通过；Xcode 27 beta 真机 Debug build 通过；新增 `coldLaunchReminderSyncDoesNotRequestAlarmAuthorization()` 真机测试通过；修复方案在同一台真机连续两次冷启动均约 1.4 秒进入 ready，收窄 reload 边界后又完成一次最终冷启动，首页单次查询加载 11 项任务。已知后续优化：后台提醒维护仍需扫描 135 项 active task，日志窗口内未完成 project fetch / resync，但已不阻塞首页，可另行将提醒查询收窄为仅含提醒的记录。
- 2026-08-01 follow-up：上述数据与启动阶段收窄后，真机仍可能视觉停在启动页或“正在加载任务”；日志已证明同一 `HomeViewModel` 完成 `items=11 / loaded` 且 `HomeView` 重绘，因此不是数据恢复或 Observation 丢失。最终根因是任务卡展开动效新增的 `TaskSourceGeometryReporter`：`updateUIView` 无条件重复写入 `TaskFrameRegistry.reporters`，而该内部弱引用注册表被 `@Observable` 追踪，形成“UIView 更新 -> 注册表变更 -> SwiftUI 再更新”的闭环；真机约 0.1 秒内单行探针触发超过 1000 次零尺寸 layout，阻止新帧提交。修复：将 `reporters` 标记为 `@ObservationIgnored`、同一 reporter 重复注册直接 no-op，并实现 representable `sizeThatFits` 接受任务行 proposal；原启动阶段 0.3 秒淡变保持不变。验证：`git diff --check`、Xcode 27 beta 真机 Debug build、真机回归测试 `taskFrameRegistryReporterRegistrationDoesNotPublishGeometryChanges()` 通过；普通 Debug 包重新安装并冷启动截图确认任务列表正常显示，提醒维护完整读取 134 项 active task、0 个 project，完成 resync 与 Widget 刷新。未删除或重建任何本地数据。
- 2026-08-02：任务行到详情卡按 ADR-0007 重构为窗口级分解式共享转场。折叠行固定为标题、可选真实备注、Baby Blue 环形子任务进度与仅已配置的时间 / 实际提醒提前量 / 紧急摘要；虚线完成框是唯一折叠态直接操作。完成框、标题、备注、进度、时间、提醒和紧急分别注册真实窗口 Frame，由同一 `UIViewPropertyAnimator` 会话从列表来源移动到详情目标；时间、提醒、紧急分流到底部，日期和详情专属内容同步淡入，取消旧逐行级联。关闭前先保存最新编辑、隐藏共享实时内容并归一化键盘与滚动位置，再按最新属性和来源 Frame 严格回程；元素清除时同步丢弃旧 Frame，覆盖层和共享节点由单一串行状态机统一释放，保留 5 个任务 50 轮重复开合回归。详情备注在稳定态完整显示，长内容位于固定底部属性栏上方的独立滚动区；空正文提供扁平“添加备注 / 添加子任务”。Reduce Motion 继续使用短淡变。验证：`git diff --check` 通过；用 Xcode 26.6 iOS 26.5 SDK、临时排除资产目录后，主 App、Widget 依赖和 `TogetherTests` target 均完成真实 Swift 编译与链接；新增共享属性显隐、提醒提前量、旧 Frame 清理测试并成功编译。标准 Simulator test 未执行，因为本机 SDK 为 26.5、已安装 runtime 仅 26.4 / 27.0，scheme 无 eligible destination；仍需真机验收共享元素路径、长备注滚动、键盘关闭归位、Reduce Motion、120Hz 流畅度与高频跨任务点按。
- 2026-08-02 follow-up：ADR-0007 的共享转场端点改为双向预备接管。新建 `UIHostingController` 共享节点先以透明状态完成一个主线程布局周期，再原子替换 SwiftUI 实时内容；展开完成时也先恢复并布局实时身份区，再移除共享节点，关闭完成后恢复来源行并等待布局周期才释放 Overlay。完成框、标题、环形进度、时间、提醒和紧急使用固定视觉尺寸，仅插值位置；备注仍允许随边界裁切揭示。卡片表面起点由这些共享元素的紧凑外接“任务身份包络”加 11pt 内边距计算，不再采用整行宽度；伴随内容始终保持最终字号和尺寸，随卡片边界增长被裁切揭示，卡面中心、尺寸与连续圆角独立形变到最终详情卡。共享元素组不再整体缩放，抬升感由 1.5pt 位移、卡面形变、阴影和背景景深共同表达。新增纯策略测试覆盖稳定视觉边界与身份包络。验证：`git diff --check`、相关 Swift parse 通过；Xcode 26.6 / iOS 26.5 Simulator SDK 下临时排除 `Assets.xcassets` 后，主 App 与 `TogetherTests` target 均真实编译和链接成功。测试执行仍受本机 26.4 runtime 与 26.5 SDK 不匹配限制，需真机重点复验首尾帧、快速跨任务开合和 120Hz 流畅度。

- 2026-08-08：首页行内创建与详情聚焦层级回归修复。`FocusDepthEffect` 删除逐元素黑色 overlay，将背景退后统一为轻微缩放、2pt 模糊和透明度变化，首页画布只保留单层暗化，避免标题、任务行和 toolbar 各自产生矩形灰块；聚焦期间底部 toolbar 隐藏。创建卡片按列表实际可用高度响应键盘：首次出现及视口缩小时滚到中央，键盘收起、视口恢复时不重复滚动。详情展开后，所有非当前任务行覆盖关闭命中层并从 VoiceOver 顺序隐藏；点击另一任务只保存并关闭当前详情，不再切换打开另一项。验证：`git diff --check` 通过；Xcode 27 beta 在 iOS 26.4.1 iPhone 17 Pro Simulator 上完成真实 build 和两轮 `TogetherTests` 执行，均 `TEST SUCCEEDED`；模拟器深色模式截图确认创建卡完整位于键盘上方、底栏不重叠、详情背景无逐行矩形遮罩，并实际点击验证“第一项展开 -> 点击第二项 -> 仅关闭当前详情”。系统 Xcode 26.6 缺少匹配 platform，验证命令必须显式使用 `/Users/papertiger/Downloads/Xcode-beta 3.app/Contents/Developer`。
- 2026-08-09：首页待办信息密度与编辑卡 UI 精修。折叠行按“标题 / 可选备注 / 紧凑属性”组织：28pt 虚线完成框与标题行对齐，紧急旗标固定在标题右侧；子任务使用进度圆环，时间和提醒使用图标加数值。展开详情取消分割线和独立“收起”按钮，标题、备注、36pt 子任务行与底部属性间距收紧；日期、时间、提醒统一为 44pt 浅灰纯色胶囊，紧急为同高圆形按钮，按压提供合成缩放、透明度和选择反馈。只有激活编辑的子任务显示确认和垃圾桶，中文输入提交继续通过 `TextInputSnapshotReader` 读取第一响应者可见文本。修复了 `inlineDetailDraft` 被所有行共同读取导致活跃任务紧急状态泄漏到其他任务的问题；非活跃任务行在详情展开时不再改变尺度、透明度或属性。待办创建卡改为按需揭示备注/子任务、底部同款属性栏和圆形确认按钮，删除语音入口；创建 Overlay 首选高度由 400pt 收敛为 300pt，并以渐进 12% scrim 和单卡阴影建立层级。根 `NavigationStack` 忽略键盘底部安全区变化，使 OCR Dock 不再被输入法顶起。验证：`git diff --check` 与 `DEVELOPER_DIR='/Users/papertiger/Downloads/Xcode-beta 3.app/Contents/Developer' xcodebuild build -project Together.xcodeproj -scheme Together -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO -quiet` 通过，仅报告仓库既有 actor-isolation warning。按本轮约定未运行模拟器、UI 测试或性能测试；微信输入法、键盘期间 Dock 层级、Dynamic Type、长备注/多子任务和最终视觉密度由用户真机验收。
- 2026-08-09 follow-up：首页 Morph 信息密度进一步收敛。主任务可见完成框调整为 24×24pt / 7pt 连续圆角，子任务为 20×20pt / 6pt 连续圆角，均用独立 44pt 命中区保持可用性；折叠备注与子任务、时间、提醒摘要统一为 14pt 次级行高。折叠摘要在展开开始后于原位 90ms 淡出，详情备注入口、子任务和添加入口只做 24ms 错峰透明度揭示，底部属性按钮最后承接，Reduce Motion 下合并为一次短淡变。详情属性栏纳入同一 `TaskMorphDisclosure` 测量范围，预挂载测得的自然高度直接用于展开，移除“估算高度展开后再纠正”的跳变；日期、时间、提醒可见控件收敛为 34pt 高 / 11pt 连续圆角，紧急和确认使用 34×34pt 圆形，外层命中区仍为 44pt，按压补充 0.96 缩放、明度压暗和触觉反馈。待办创建卡删除固定 268/300pt 高度和底部 `Spacer`，以自然内容高度计算表面，属性栏紧跟正文；键盘只推动整卡底边保持约 10pt 间距，不改变卡片高度或内部间距。详情保留现有列表几何外撑与景深尺度，仅对背景增加浅色约 10%、深色约 6% 的合成变暗；创建 overlay 使用更明确的浅色 18% / 深色 12% scrim。验证：`git diff --check` 与 Xcode 27 beta 3 generic iOS build 通过，仅有仓库既有 actor-isolation warning；按用户约定未运行模拟器、UI、性能或真机测试，最终手感和微信输入法链路由用户真机验收。

- 2026-08-14：首页待办时间线顶部 chrome 改为滚动方向驱动的单一接管状态。初始状态在原生 toolbar 中居中显示 38pt “待办 / 定期”分段切换器，与右侧 38pt 头像等高，命中区均不小于 44pt。首个日期标题进入转换区且用户持续上滑后，切换器淡出，安全区左上角显示 22pt 日期与 12pt “星期·任务数”；深处有意下滑可直接恢复切换器，再上滑恢复日期。日期分组标题改为普通非吸附 header，跨日期按滚动方向短距离置换，数量变化使用原生 numeric content transition。Morph 与程序化滚动不驱动 chrome；Reduce Motion 降级为短淡变，无障碍动态字体保留 Menu 并禁用日期接管。核心实现位于 `HomeTimelineChromeState` / `HomeTimelineChromeModel`，纯状态测试位于 `HomeMorphSessionTests.swift`。验证：全 App 源码 typecheck、改动 Swift 与新增测试源码 parse、`git diff --check` 通过；标准 build/test 因 Xcode 26.6 / iOS 26.5 SDK 与本机可用 iOS 27 Simulator runtime 不匹配未执行，仍需真机验收触发阈值、跨日期方向、Dynamic Type 与 Reduce Motion。
- 2026-08-14 follow-up：真机确认顶部 chrome 初版在滚动时掉帧，且接管后顶部与列表同时显示同一日期。根因是滚动 offset 和 header 几何每帧都直接修改 `@Observable` 的完整 `HomeTimelineChromeState`，使根 toolbar 与首页持续失效重算；同时列表 header 没有来源退出状态。修复将高频工作状态与滚动采样改为 `@ObservationIgnored`，仅在顶部展示模式、当前日期或方向真正改变时发布可视状态；当顶部表示某日期时，对应列表 header 只做透明退出并保留布局，避免双显和任务行跳位。头像由 38pt 调整为 40pt 做圆形光学补偿，44pt 命中区不变。验证：全 App 源码 typecheck、当前 Together 模块下 `HomeMorphSessionTests.swift` 的 Testing 宏 typecheck、Swift parse 与 `git diff --check` 通过；未运行 Simulator 或真机 Instruments，实际帧时间仍需真机复验。这是项目第二次出现“高频几何上报误入 Observation”回归，应将“几何采样留在非观察协调器，只在语义边界发布状态”补入 SwiftUI 项目 Skill 的长期 failure shield。

## Open Questions

- 是否启用 Codex Memories 的用户级配置。
- Chronicle 是否在低敏任务中短时试用。
- 全局创建入口最终是单按钮直达、展开式快捷菜单，还是任务创建与 OCR 导入两个明确入口。
- OCR 草稿是否需要跨启动持久化，还是仅在当前导入会话内短期保存。
