# Together Design Guidelines

## 1. 文档职责
- 本文件是项目「一起 / Together」的唯一设计规范主文档。
- 负责：视觉基线、安全区域、页面设计规则、动效规则、Pencil 出图约束。
- 不负责：产品范围、信息架构和页面模块清单；这些以 `PRODUCT_SPEC.md` 为准。

## 2. 结论
- 本仓库所有 UI 设计和实现，默认按 iOS 原生设计规范执行。
- 当前首页改为统一任务区，并作为单人任务首页的设计母版继续深化。
- 动效与交互丝滑度是核心体验组成，不是最后补的装饰层。

## 3. 适用范围
- Pencil 线框图和高保真视觉稿
- SwiftUI 页面实现
- UIKit 容器或桥接页面
- 动效设计、导航转场、悬浮操作按钮

## 4. 全局视觉基线

### 事实
- 当前项目默认视觉基线继续使用 `mobile-02-cleanminimal_light`。
- 现有首页 UI 的干净留白、轻层次、弱阴影、低饱和配色是正确方向。

### 设计结论
- 产品表达应是“效率优先、带一点温度”，不是“情侣感优先”。
- 允许保留黑 / 白 / 粉的品牌倾向，但只能作为强调色，不应破坏效率工具的清晰层级。
- 不允许转向厚重拟物、强发光、过度装饰或廉价甜腻风格。

## 5. 全局硬规则
1. 顶部内容不得侵入安全区域。
2. 底部内容不得侵入 Home Indicator 与系统手势区域。
3. 滚动内容与固定底栏必须分层。
4. 安全区域按运行时约束处理，不写死机型像素。
5. 自定义玻璃和半透明效果必须服从系统层级与可读性。
6. 英文与数字默认使用 Rounded 风格。
7. 全局字体系必须统一，不允许页面临时拼接字体方案。

## 6. SwiftUI 设计落地规则
1. iOS 17+ 优先使用 `safeAreaPadding`、`safeAreaInset`、`GeometryProxy.safeAreaInsets`。
2. 背景层可全屏延展，主要信息容器必须尊重安全区域。
3. FAB、顶部操作按钮、分段切换、悬浮胶囊按钮都必须保留足够触控热区。
4. 列表卡片和底部工具条的视觉层次要清楚，但不要靠重阴影和大模糊堆出“假高级”。
5. 原生 iOS 组件优先使用 iOS 26 液态玻璃原生能力；需要玻璃质感时，优先 `glassEffect`、`GlassEffectContainer` 和系统 glass button style，不手搓高饱和假玻璃。
6. 当期望视觉与 iOS 原生容器行为冲突时，先明确取舍再落地：例如系统 `toolbar` 可获得原生按钮和标题行为，但会强制应用导航栏布局与控件底座；若需要头像、状态胶囊、标题和按钮完全自由排列，应改为自定义顶部栏，而不是混用两套顶部结构。

## 7. 首页统一任务区专项规则
- 首页使用原生 `NavigationStack` / `toolbar` 承载标题和操作：标题为“任务”，顶部左侧为例行任务入口，顶部右侧头像进入 Profile；底部 toolbar 左侧为 OCR、右侧为新建入口，已完成归档统一由 Profile“日志”进入。
- 首页不再展示自绘顶部日期区、周视图、月历或项目切换入口。
- 主内容使用 `ScrollView` / `LazyVStack` 形成连续任务流，保留当前任务 row 视觉，并将任务详情作为 row 内联展开区呈现。
- 底部原生 toolbar 保留新建任务入口，周期事务入口暂留；底部内容不得压住 Home Indicator。
- 列表 / 时间线卡片应优先表达：
  - 日期分组
  - 标题
  - 次级信息（具体时间、位置、标签、提醒、子任务进度）
  - 状态
- 首页活跃任务按日期分组：有截止 / 发生日期的任务进入对应日期组；无截止任务按创建日期分组，并整体排在有日期任务之后。分组标题平时作为普通列表内容直接平铺；滑到顶部后由页面内透明吸附副本接管，不使用胶囊、描边、白色横条或任何可见容器。吸附状态通过内容顶部 mask 避免任务标题与分组标题重叠，同时保留网格背景和展开态聚焦 / 模糊层级。
- 任务标题右侧不再显示任何日期、时间、状态、提醒、重复或子任务进度；折叠态标题获得完整横向空间但仍保持单行。副标题区域统一承载具体时间、提醒、重复、子任务进度等任务属性；没有任务属性时才回退展示状态。备注内容显示在任务属性下方，无备注则不显示。
- 首页卡片高度由内容决定，不写死固定高度。
- 任务详情不再使用独立详情 sheet：折叠任务行点击后在原位置展开，展开区内提供标题编辑、备注、子任务新增/编辑/勾选/删除，以及日期、时间、提醒、重复设置入口。
- 内联详情的日期、时间、提醒、重复继续复用系统感设置 sheet；子任务不进入集合设置 sheet，避免“任务 -> 详情 sheet -> 子设置 sheet”的多层路径。
- 首页内联详情必须保持平铺视觉：备注、子任务和属性不得逐项包裹成描边圆角卡片；子任务沿用任务行 action gutter，子任务虚线框与父任务虚线框右对齐，子任务标题与父任务标题左对齐。
- 展开态顺序固定为任务标题、备注、子任务列表、属性工具条；折叠态不为右侧日期或属性保留固定空间，展开态仍需保证无子任务任务也能收起；内联详情中的属性和子任务状态修改先进入 draft，收起卡片后再统一保存并触发列表重排。
- 日期、时间、提醒、重复在内联详情底部使用无背景轻量图标文字工具条，点击仍打开现有设置 sheet。
- 同一时间只展开一个任务；展开、收起、添加子任务和删除子任务必须保持稳定 row 身份，使用高度变化 + 子区块级联动画，避免下方任务闪跳。
- 键盘出现时必须滚动当前编辑标题或子任务到键盘上方；不允许为了视觉效果牺牲输入法避让。
- 已完成任务的视觉降噪要明显，但仍要保留可回看性。
- 首页不是历史归档页：只保留今天已完成的少量最近任务；本周已完成历史通过原生 sheet 查看，完整历史进入“已完成”归档页。
- 已完成归档页优先使用系统 `List`、原生 navigation toolbar、系统 `.searchable` / `DefaultToolbarItem(kind: .search, placement: .bottomBar)` 和分页加载；row 视觉贴近首页任务，但不默认挂载子任务级联展开动画，避免历史浏览影响首页执行动画性能。
- 已完成归档页顶部右侧保留本周 / 本月 / 全部快捷 `Menu`，标题副标题显示当前范围；底部精确筛选按钮必须是原生 `ToolbarItem(placement: .bottomBar)` 并显示明确图标，只负责具体月份和具体日期；精确筛选内容使用原生 sheet，日期与月份两种模式必须保持同一内容高度，避免 sheet 高度随切换跳变。

## 8. 详情与次级面板规则
- 普通任务详情优先在首页任务流内联展开，不再使用独立详情 sheet。
- 日期、时间、提醒、重复等单项设置可以使用原生 sheet；子任务编辑必须留在内联详情区。
- 筛选、排序、分组面板优先使用轻量浮层，不要做复杂弹窗堆叠。

## 9. OCR 附件浮层规则
- OCR 入口使用原生 SwiftUI toolbar `Menu`，菜单仅提供“相机 / 照片”系统命令，不自定义菜单背景、圆角、阴影、尺寸或 morph 动画。
- 相机和照片使用同一个 SwiftUI `.sheet(item:)` 来源会话。相机默认 `.medium`；照片支持 `.medium/.large` 并默认 `.large`。来源切换、识别中和失败只改变 Sheet 内状态，不创建第二个 presentation controller。
- OCR 主路径优先使用 AVFoundation 自定义相机和 inline PhotosPicker；系统 picker 只能作为权限、设备能力或大屏照片选择入口。
- OCR 相机和照片状态是 Sheet 内的沉浸式媒体页：相机预览或照片网格填充内容区，不再包成内部圆角卡片，也不显示额外导航标题；Sheet 圆角、阴影、拖拽和外部安全区完全交给系统。
- 媒体控制按钮优先使用 iOS 26 原生 Liquid Glass 按钮；照片页“全部照片”使用系统 `PhotosPicker`。
- 识别成功后来源 Sheet 必须先 dismiss，再在其 `onDismiss` 中由根层 SwiftUI `.sheet(item:)` 展示确认页；禁止同时 present 两个 Sheet，也不使用固定延迟轮询 presentation 状态。
- OCR 确认页使用原生 `.medium` / `.large` detent、系统 grabber、`NavigationStack` 与 toolbar；短内容默认半屏，长内容和键盘输入单向升级为全屏。
- OCR 确认页属于批量文字编辑流程：禁止拖拽直接关闭，取消操作统一处理草稿放弃确认；导入失败必须保留当前输入。
- OCR 草稿只使用统一任务语言：任务标题、备注、子任务、日期、时间、提醒、重复；不得再出现“项目草稿 / 待办草稿”分区或项目卡片视觉。
- OCR 草稿视觉应对齐首页内联详情：平铺标题与备注、子任务复用 action gutter 与虚线框语言、属性使用浅灰胶囊按钮。
- OCR 结果必须在用户确认后才写入真实任务；识别中、失败、重试和返回来源保留在浮层流程，确认编辑由原生 sheet 承载。

## 10. 动效规则

### 原则
- 原生
- 克制
- 连续
- 可读
- 稳定 60fps

### 默认方法论
- 只要实现自定义动画，默认采用“状态驱动的分阶段连续动画”。
- 先定义清晰状态，再拆分为 2 到 3 个连续阶段；禁止边做动画边临时拼装状态。
- 动画优先表达“反馈 / 层级 / 连续性”，不要为了装饰牺牲信息可读性。
- 同一交互中的多个变化，优先做同位连续变化，再做列表重排或跨区移动。

### 优先场景
- 任务完成与状态切换
- 首页列表层级变化
- 主按钮展开 / 收起
- 任务内联详情展开 / 收起
- 筛选 / 排序 / 分组切换
- 未来单人 / 双人模式切换

### 禁止事项
- 禁止廉价、花哨、堆砌式动画。
- 禁止在动画过程中做重排序、重计算、同步 IO。
- 禁止为了转场效果让文字失焦、可读性下降或点击区域错位。
- 禁止把自定义动画做成离散跳变、硬切替换或无状态来源的拼接效果。

## 10. Pencil 出图规则
- 绘制 iPhone 页面时必须显式预留顶部和底部安全区域。
- 首页任务流要先表达层级和节奏，再表达装饰。
- 若标注动效，必须写清：
  - 触发条件
  - 起止状态
  - 节奏类型
  - 是否有降级方案

## 11. Open Questions
- 是否需要为动效建立单独的 design token，例如 `duration / spring / elevation transition`。
- ScrollView 任务流失去 `List.onMove` 和原生 swipe 后，排序、删除、推迟是否长期放在 context menu / 内联详情，或单独设计编辑模式。
