# Together Design Guidelines

## 1. 文档职责
- 本文件是项目「一起 / Together」的唯一设计规范主文档。
- 负责：视觉基线、安全区域、页面设计规则、动效规则、Pencil 出图约束。
- 不负责：产品范围、信息架构和页面模块清单；这些以 `PRODUCT_SPEC.md` 为准。

## 2. 结论
- 本仓库所有 UI 设计和实现，默认按 iOS 原生设计规范执行。
- 当前首页改为统一任务区，并作为单人任务首页的设计母版继续深化。
- 动效与交互丝滑度是核心体验组成，不是最后补的装饰层。

## 3. 适用范围
- Pencil 线框图和高保真视觉稿
- SwiftUI 页面实现
- UIKit 容器或桥接页面
- 动效设计、导航转场、悬浮操作按钮

## 4. 全局视觉基线

### 事实
- 当前项目默认视觉基线继续使用 `mobile-02-cleanminimal_light`。
- 现有首页 UI 的干净留白、轻层次、弱阴影、低饱和配色是正确方向。

### 设计结论
- 产品表达应是“效率优先、带一点温度”，不是“情侣感优先”。
- 视觉以中性色为主，珊瑚色承载任务完成、紧急和温暖状态，baby blue 承载轻量操作与定期任务语义。
- 全 App 浅色一级画布统一使用系统纯白，不保留可见冷灰渐变；深色画布维持当前中性明度渐变。首页不再绘制网格线，并允许在待办 / 定期根页面叠加低密度环境粒子；背景不得出现明显色相、可识别纹理、发光、拖尾、闪烁或与任务文字争夺注意力的线条。
- 不使用青绿色作为品牌色、全局 tint、选择色或主要操作色。
- 不允许转向厚重拟物、强发光、过度装饰或廉价甜腻风格。

### 色彩职责
- 页面背景、内容表面、标题、正文和弱信息使用 `AppTheme` 的自适应中性色，默认工具栏与普通操作继承主文字或系统语义色。
- 珊瑚色只用于小面积、高识别度的任务状态：完成勾、紧急旗标、今天、选中日期、逾期与短暂完成反馈；不铺满列表行或大面积界面。
- Baby blue 用于保存、推迟、排序完成、关注及其 Live Activity / Dynamic Island 强调、定期任务选中短线和进度等轻量操作；浅色模式下不得作为小字号正文颜色或承载仅靠颜色识别的信息。
- 删除、永久移除和不可逆操作使用系统 destructive role 或危险语义色，不复用珊瑚色。
- 状态必须同时使用文字、图标、形状或描边表达，不能只依赖颜色。

## 5. 全局硬规则
1. 顶部内容不得侵入安全区域。
2. 底部内容不得侵入 Home Indicator 与系统手势区域。
3. 滚动内容与固定底栏必须分层。
4. 安全区域按运行时约束处理，不写死机型像素。
5. 自定义玻璃和半透明效果必须服从系统层级与可读性。
6. 中文与普通英文界面文本默认使用 Apple 系统字体，不再使用自带圆角中文字体。
7. 独立的日期、时间、数量、进度等数字显示使用系统 Rounded 风格；同一段中文或中英混排文本不按字符拆分字体。
8. 全局字体系必须统一，不允许页面临时拼接字体方案。

## 6. SwiftUI 设计落地规则
1. iOS 17+ 优先使用 `safeAreaPadding`、`safeAreaInset`、`GeometryProxy.safeAreaInsets`。
2. 背景层可全屏延展，主要信息容器必须尊重安全区域。
3. FAB、顶部操作按钮、分段切换、悬浮胶囊按钮都必须保留足够触控热区。
4. 列表卡片和底部工具条的视觉层次要清楚，但不要靠重阴影和大模糊堆出“假高级”。
5. 原生 iOS 组件优先使用 iOS 26 液态玻璃原生能力；需要玻璃质感时，优先 `glassEffect`、`GlassEffectContainer` 和系统 glass button style，不手搓高饱和假玻璃。
6. 当期望视觉与 iOS 原生容器行为冲突时，先明确取舍再落地：例如系统 `toolbar` 可获得原生按钮和标题行为，但会强制应用导航栏布局与控件底座；若需要头像、状态胶囊、标题和按钮完全自由排列，应改为自定义顶部栏，而不是混用两套顶部结构。

## 7. 首页统一任务区专项规则
- 待办与定期共用一层全屏环境粒子：四层视差缓慢向上、只带轻微水平摆动，模式切换保持连续相位。深色模式使用柔和暖白，现有约 `16%` 活跃单元、尺寸、颜色和 `10%...20%` 不透明度保持不变；浅色模式改用低饱和冷银灰，以约 `12%` 活跃单元和约 `0.5...1.1pt` 主半径形成更少但更清晰的点，并以真机同距离观感校准到与深色相同的感知显著度。两种模式顶部 `10%`、底部 `16%` 平滑淡出；增强对比度下整体不透明度再降低约 `40%`。圆形抗锯齿必须在点空间计算，不得对经过 `fract / floor` 的网格局部距离取屏幕导数，否则网格边界会显现为矩形线框。
- 粒子背景只提供环境层次，不响应滚动。进入详情、创建、Sheet 或离开根页面时，常规模式用约 `550ms` 无反向、无回弹的物理减速停住；返回后约 `420ms` 从当前相位平滑加速。Reduce Motion 立即停止并用约 `140ms` 淡变；低电量模式及严重 / 临界热状态减速后冻结；场景失活立即暂停。用户关闭“动态背景”时减速后淡出，重新开启时淡入并加速。
- 首页使用原生 `NavigationStack` / 顶部 `toolbar` 承载模式切换和头像操作：约 `44pt` 的单一主控行在 `ToolbarItem(.principal)` 中使用 `HStack` 承载“待办 / 定期”两个无外壳 Button，辅助功能大字号下使用当前模式 `Menu`。普通字号下不显示轨道、胶囊、玻璃、底部横线、下划线、描边或移动指示物；当前项使用约 `21.5pt Bold` 主文字色，非当前项使用约 `17pt Regular` 三级文字色。iOS 26+ 对顶部 Toolbar 内容使用 `.sharedBackgroundVisibility(.hidden)`；顶部右侧头像始终进入 Profile。底部由同一 `NavigationStack` 的系统 `.bottomBar` 承载，左侧为 OCR 原生 `Menu`、右侧为当前模式的新建 `Button`，已完成归档统一由 Profile“日志”进入。
- 首页模式切换区保持 `144 × 44pt`，两项各使用 `72 × 44pt` 的稳定布局与命中范围；选择变化时两项文字中心不移动，以同位置的 Regular / Bold 双层文字在约 `260ms` 内同步缩放和交叉淡变，视觉字号比约为 `1.265×`，全程不增加位移、底线或过冲。按下项缩放至 `0.94`、透明度降至约 `0.82`，松手约 `180ms` 无过冲恢复。Reduce Motion 取消文字和按压缩放，只保留约 `160ms` 的透明度 / 内容淡变。圆形头像使用约 `40pt` 视觉尺寸并置于 `44 × 44pt` 命中区域，与切换区保持视觉中心等高；头像图像直接填满圆形视觉区域，只允许贴着图片边缘的一道自适应细描边。隐藏 Toolbar 共享背景，不增加留白环、第二层圆形底座或阴影。
- 首页不再展示自绘顶部日期区、周视图、月历或项目切换入口。
- 主内容使用 `ScrollView` / `LazyVStack` 形成连续任务流。每个已有或创建中的任务 UUID 都只对应一个列表拥有的 `TaskMorphContainer`，由容器自身在 `.compact / .expanded` 间改变真实行高；创建草稿先以临时列表行呈现，保存后同位置原子替换或在最终分组局部显现。SwiftUI 持有任务内容、Draft、焦点和持久化意图，不使用根级 Modal、创建 / 详情 Overlay、独立创建卡或详情 Sheet。
- 底部入口只使用 `ToolbarItem(placement: .bottomBar)`、原生 `Button` / `Menu`、SF Symbols 与 `ToolbarSpacer`；安全区、Liquid Glass、容器分组和按压反馈完全交给系统。禁止额外叠加 `safeAreaInset` Dock、自绘圆形材质、描边、阴影或按钮缩放层。待办页 `+` 创建待办，定期页 `+` 创建定期任务，创建器内部不显示类型切换；`+` 不形变为创建卡，也不承担创建表面身份。
- 任务流只按计划日期分组，普通任务不存在“未排期”或按创建日期生成的分组。iOS 26+ 使用原生顶部 `safeAreaBar` 承载当前日期，使日期始终清晰且位于 Toolbar 下方左侧；它不是 Toolbar 日期副本，也不替换或隐藏模式切换器。首组日期只在栏中显示，后续日期标题作为列表内的真实分组交接来源随内容滚动；来源标题的底边越过日期栏下边界时只切换一次当前分组身份，并在栏后隐藏但保留布局高度。日期栏保持稳定视图身份，并与定期页摘要一致，对完整标题使用原生 Numeric Text 内容转场；下一组向上或向下交接时不产生双显、任务跳位或标题高度空隙。几何采样只能输出“是否越过边界”的离散值，禁止把连续坐标、滚动方向或逐帧视觉参数写入可观察状态。iOS 26+ 的 Reduce Motion 降级为整组短淡变。iOS 18–25 使用 `LazyVStack(pinnedViews: [.sectionHeaders])` 的原生吸附降级。日期 Header 不增加背景遮罩、材质、圆角、描边、阴影或分割线；日期使用约 `22pt Semibold`，星期与任务数量使用约 `12pt Medium` 并按首基线对齐，任务完成或新建只让数量执行原生数字内容转场。日期标题不可点击，也不参与任何自定义边缘压缩或堆叠。
- 首页存在逾期任务时，“当前已逾期 N”与日期栏由同一个顶部 `safeAreaBar` 按上下顺序承载，系统 `.soft` 边界统一位于日期栏下方，不叠加自定义 blur、mask 或材质。逾期任务只在逾期 Sheet 中展示，不与“今天 / 明天”等主时间线标题保持同级，也不重复生成“昨天”任务组。Sheet 顶部右侧使用至少 `44pt` 命中范围的系统文字按钮“全部改到今天”；Dynamic Type 宽度不足时允许按钮移到标题下方右侧。批量处理中在按钮文字前使用原生确定型圆环 `ProgressView` 按已处理项数推进，不使用自绘转圈动画。批量成功项作为一组克制淡出并自然收拢，全部成功后系统关闭 Sheet；部分失败保留并显示可重试状态。Reduce Motion 只做内容与透明度变化，不做空间位移。
- 待办与定期的滚动边缘只使用系统 `scrollEdgeEffectStyle`，不再按任务行位置计算 offset、scale、blur、opacity、遮没或命中阈值。iOS 26+ 待办列表对顶部与底部统一使用 `.soft`；顶部 `safeAreaBar(edge: .top, spacing: 0)` 让系统自动把清晰度边界延伸到日期栏底部。定期页继续用 ScrollView 原生 `.safeAreaInset(edge: .top, spacing: 0)` 承载固定维度与摘要 Header，顶部效果关闭、底部使用系统 `.soft`。系统负责底部 toolbar、安全区与内容边界的适配，详情和创建状态不修改该效果。日期 / 周期 Header 仍可作为详情焦点景深的一部分：浅色使用与其他非活跃任务相同的约 `0.8pt` 模糊和 `0.52` opacity，深色保持约 `0.5pt` 模糊和 `0.52` opacity，两者都保留 `0.98` scale 且不得按滚动位置变化。禁止恢复自定义滚动边缘 offset、遮罩、模糊、逐行汇聚 / 压缩 / 淡出、深层禁用命中、整屏速度尾随、逐行弹簧回弹、通知卡片墙、文字扭曲或用真实布局变化制造瀑布感。
- 折叠任务使用“标题 → 可选备注 → 紧凑元数据文本”：标题行右侧只保留状态图标，紧急任务显示珊瑚色旗标，已关注任务显示 baby blue `scope`，两者同时存在时按“紧急 → 关注”并排且保持固定尺寸；标准内容尺寸下，标题使用约 `17pt Semibold` 并最多显示两行，备注与属性摘要统一使用约 `14pt Medium`，三者都必须随 Dynamic Type 缩放。标题保持统一字号和字重，超出两行时采用尾部截断，进入详情后完整显示；不得按标题长度使用 `minimumScaleFactor` 压缩字号，也不得在紧凑列表中使用自动跑马灯。辅助功能大字号下应放宽或取消常规两行限制，并让 VoiceOver 始终读取完整标题。备注与属性摘要使用相同的次级字号、字重和视觉行高，不通过排版层级区分；待办属性不显示图标，按“时间 → 提醒 → 子任务进度”顺序保留现有文本值，并用“ · ”连接。紧凑行保持左右各 `28pt`、上下各 `4pt` 的外部间距，标题与备注或属性摘要之间统一为 `4pt`；相邻单标题任务因此保留约 `8pt` 的内容间空白，完整行和完成框命中区仍不小于 `44pt`。备注与属性同时存在时属性另起一行置于备注下方。展开详情不重复顶部和底部两套属性，属性入口集中在底部。折叠态低对比度细实线完成框与标题行视觉等高并对齐，是唯一直接状态操作，其他行主体统一打开详情。
- 普通任务详情由来源任务行在真实列表中展开。打开时先无动画建立同一 UUID 的活跃紧凑实体并预挂载零高详情，随后由当前行局部 `KeyframeAnimator` 驱动真实高度；下方任务从首帧开始随 `LazyVStack` 自然下移。其他任务可按稳定序号叠加不改变真实行高的有界合成缩放与向焦点外侧位移；每行的合成运动约 `360ms`，上方从约 `15ms` 开始按约 `60ms` 逐行向上传播，并维持约 `8...24pt` 额外上移；下方从约 `20ms` 开始按约 `75ms` 逐行传播，并维持约 `12...36pt` 额外下移，使整体更快但仍能读出逐行水波。不得解析或逐帧写入 `UIScrollView.contentOffset`，不得主动滚动、插入占位、复制前景卡或强制居中。长内容继续由外层列表承接。
- 完成框与标题是唯一稳定身份区，两者作为刚性组保持内部间距和顶部对齐，在约 `460ms` 内沿内部轨迹 `(0, 0) -> (-7pt, +14pt) -> (-16pt, -12pt)` 的连续左下再左上弧线直接到达终点；该轨迹补偿展开态顶部新增的 `14pt` 真实布局空间，使屏幕上的下潜与上升接近等幅，最终视觉尺度约 `1.12`，不得在终点追加回落、Overshoot 或 Spring。紧凑备注与元数据先溶解并收起真实高度；展开态备注、每个子任务、添加行、底部属性与错误信息按最终宽度预排版后，以视觉行从 `(+14pt, +24pt)` 沿左上弧线经过约 `(+5pt, +9pt)` 回到原位，透明度从 `0` 到 `1`。详情行可在身份组途中进入，但第一行最早约 `480ms` 落定，不得先于身份组；相邻行默认错开约 `50ms`、总错峰不超过 `220ms`，各行落定后独立开放交互。详情内部行距为 `0pt`，备注、添加行等视觉高度约 `32pt`；详情首行向标题槽位回收约 `20pt`，使标题与首个副标题保持约 `4pt` 的视觉间距；底部属性轨道向前一行回收约 `6pt`，避免波浪终点和 44pt 命中框制造假留白。交互控件仍须保留至少 `44pt` 的命中视图，命中视图可用局部负外边距避免撑大视觉间距。只有活跃行测量紧凑 / 完整标题和详情自然高度；连续进度不得进入根级 Observation。
- 详情关闭前先保存当前 draft 并刷新紧凑行摘要；保存失败保持当前详情。日期、紧急、完成状态或周期改变时，先在原槽位完成 `.expanded -> .compact`，再以独立列表动画释放到最终 section/index。点击另一任务、可见 Toolbar 控件或日期/周期标题只关闭当前详情，必须再次点击才能打开目标或执行原控件动作，旧保存或动画完成回调不得改变新会话。
- 待办详情、定期详情、待办创建和定期创建共用一个 `HomeMorphSession`；任意时刻只允许一个活跃列表容器。外层列表仍可滚动；非活跃任务的完成框、Swipe、菜单和重排暂停。已有详情中点击另一任务主体或普通 toolbar 项只保存并收起当前项；创建 Draft 中点击另一任务、模式、OCR、头像、日期或底部 `+` 只结束输入并保留展开草稿，不执行被点击目标。
- 待办和定期页点按 `+` 时立即分配最终任务 UUID，并在今天未完成分组末尾或当前周期未完成区域末尾、已完成任务之前建立临时空行；稳定视图立即挂载，真实占位高度以约 `220ms` 无回弹展开，使下方日期标题与后续任务只随 `LazyVStack` 的真实布局自然让位。可见内容同步执行约 `160ms` 的短淡入，不附加位移、缩放、模糊或列表级过渡，也不为下方内容建立独立 offset。紧凑行同时带有按完整详情估算的一次性展开预留空间。当前会话先请求标题焦点，约 `140ms` 后对稳定 Draft ID 执行一次约 `220ms` 的平滑底部预定位，并在列表开始上移的同一时刻启动现有详情逐行展开；预留空间随同一布局进度等量收回，使整个 Draft 在键盘安全视口上方保持连续，并让动作行与键盘上沿保留约 `10pt` 呼吸空间。不得等待键盘稳定后再做第二次滚动；短列表的空白留在 Draft 上方，Dynamic Type 或超长内容继续由外层列表自然滚动。编辑期间日期 / 周期变化只更新 Draft，不移动行；提交或取消使用现有详情反向收起动画。明确取消时，同一 Draft 的外层真实占位在约 `320ms` 内由当前高度连续收至零，紧凑占位在最后约 `120ms` 淡出，随后才移除对象；下方 Header 与任务只随真实布局自然上移，不建立独立 offset。保存到相同位置时临时行原子替换为持久化行并明确排除该退出动画，最终位置变化时才准备目标分组并使用轻量行显现；失败保留 UUID、全部输入和错误状态。
- 创建项完全复用对应已有任务的标题、备注、子任务与属性编辑器，并在属性轨道后增加一条克制的“取消 / 添加”动作行，不建立 Overlay、独立卡片或键盘工具栏。动作组在内容宽度内居中，两个纯文字按钮使用相同的稳定槽位和至少 `44pt` 命中范围，中间只保留短而低对比的竖向分隔；“取消”为中性文字，“添加”为自适应黑白主文字，不使用胶囊、背景、描边或阴影，避免与上方属性胶囊重合。创建行完成框保持可见但禁用并从辅助功能树隐藏；Swipe、菜单、重排和完成动作全部禁用，统计、数量和进度不计入草稿。“取消”立即放弃且不二次确认；“添加”在首个非空白字符输入后约 `160ms` 内激活，空标题时保持低对比禁用。创建态标题使用单行 TextField，长内容输入时水平滚动，以确保软件键盘“完成”提交整个任务；保存后的标题仍按列表现有多行规则展示。备注“完成”只结束备注，新子任务“完成”只添加子任务并保留 Draft。空标题提交时在标题下显示“请输入任务标题”，配合轻微水平回位与轻触觉并重新聚焦；Reduce Motion 不位移。保存阶段不显示转圈或延迟进度，“添加”原位轻微降权并锁定重复提交；保存成功后文字在稳定槽位内向中心收束，由同一中心以连续矢量笔画写出勾号，并以接近临界阻尼、无过冲的约 `440ms` 形变落定，完成触觉与末笔同步，随后复用约 `320ms` 反向收起。失败则直接恢复“添加”并保留草稿与错误。Reduce Motion 只使用约 `120ms` 交叉淡变。VoiceOver Escape 与“取消”等价。手动收起键盘、列表滚动以及日期 / 时间 / 提醒选择器不会结束创建。
- 创建类型完全由页面决定。模板界面、模板数据、模板仓库和“存为模板”入口均不存在；任何创建界面不得出现“模板 / 待办 / 定期”切换。
- 定期创建只显示定期字段，但与待办创建和两类详情共享同一个会话仲裁、列表容器和运动参数；不另建 Overlay、创建卡或第二套业务状态机。
- 已有详情保持连续列表形态：不使用矩形背景色块、圆角卡、描边、分割线、阴影或屏幕级暗化；活跃行后方只允许使用上下透明衰减的柔和亮度场建立焦点，不能出现可识别边界。浅色亮度场以纯白局部净化粒子，中心只保留外围显著度的约 `15%...20%`，不使用石墨灰染暗焦点；净化随约 `460ms` 展开与约 `320ms` 收起同步变化。活跃区域整体约放大至 `1.05`、标题身份组最终约 `1.12`，上下各增加约 `14pt` 真实呼吸空间；浅色其他任务统一使用约 `0.8pt` 模糊和 `0.52` 透明度且不额外压暗，深色继续使用约 `1.45pt` 模糊和 `0.49` 透明度，不再按与焦点任务的距离改变清晰度。浅色模式下，已有任务详情与创建 Draft 的标题使用近黑主文字，备注、子任务、属性与创建动作使用更深的炭黑正文，未配置项仍保留一级较弱层级；该调整不改变深色模式。相邻任务约缩至 `0.95`，远处约缩至 `0.87...0.88`；上方上移约 `8...24pt`，从约 `15ms` 开始、每级约 `60ms`、最多 `260ms`；下方下移约 `12...36pt`，从约 `20ms` 开始、每级约 `75ms`、最多 `340ms`。背景行的独立合成运动约 `360ms`，让上一行明显推进后下一行再启动。日期 / 周期标题在浅色使用与其他任务一致的约 `0.8pt` 模糊和 `0.52` opacity，深色保持约 `0.5pt` 模糊和 `0.52` opacity，并继续使用约 `0.98` scale；顶部与底部 Toolbar、头像保持系统原生清晰外观，底部项目在详情期只接管首次点按用于保存并收起。禁止全屏模糊、连续几何采样和按帧写入状态。身份组约 `460ms` 直接到达终点，详情与背景波最慢一侧均约 `700ms` 完成。收起约 `320ms`，详情从底部向顶部逆序退出并与真实高度共享同一连续时钟，身份组沿同一波浪精确反向；完成判定不得监听动画帧阈值。待办与定期统一完成框、标题轴线、静态层级、详情节奏和单行属性轨道；字段及日期分组 / 周期维度保持各自语义。同一属性轨道的胶囊在普通对比度下统一使用系统 `tertiarySystemFill`，增强对比度下提升为 `secondarySystemFill`；配置状态由文字、图标及必要的语义色表达，不用不同中性底色区分。定期详情在非辅助功能字号下将四个属性胶囊等分可用宽度，辅助功能字号恢复内容宽度的局部横向滑动。属性轨道使用约 `2pt` 间距、`8pt` 文字胶囊水平内边距，并让可见左缘与 `20pt` 子任务完成框对齐。正文添加入口及未配置属性在浅色焦点区提高前景对比度，不改变胶囊表面；真实子任务之间保留约 `3pt` 的可辨间隔。空间不足时局部横向滑动并隐藏滚动条，不自动换行。Reduce Motion 取消所有位移、缩放、级联和任务 / Header 模糊，只保留约 `220ms` 高度与淡变，非活跃任务透明度约 `0.65`。
- 底部 toolbar 创建路径为“系统按钮反馈 → 分配 UUID → 回到待办今天或保留当前定期周期 → 当前未完成分组末尾建立带展开预留空间的紧凑空行 → 激活标题与键盘 → 一次平滑预定位 → 完整复用详情逐行展开 → 保存 / 放弃复用反向动画 → 原地替换或最终分组局部显现”。预定位只使用当前创建行的稳定 ID，不读取位置；展开时预留空间与真实详情高度反向交接，确保标题、备注 / 子任务入口、任务属性和动作行位于键盘上方并保留约 `10pt` 间距。短列表把剩余空白留在 Draft 上方，不监听键盘通知，不执行键盘稳定后的第二次滚动、跨层飞行、额外 Spring、缩放或回弹。所有延后操作必须校验会话 token，保存、放弃、模式变化或旧任务回调不得重新激活输入框或展开旧 Draft。
- 根页面保持 SwiftUI 所有权；底部入口由 `NavigationStack` 的系统 toolbar 持有，根层不持有创建 / 详情 Host、动态模糊层或重复前景卡。详情与创建始终在列表内展开。
- 启用 Reduce Motion 时取消额外空间运动，只保留约 `220ms` 高度与淡变；系统键盘布局避让和对象所有权不变。Reduce Transparency 不改变布局和对象所有权。
- 卡片与属性控件必须遵循安全区域、Dynamic Type、VoiceOver Escape 和至少 44pt 命中区。键盘出现时只滚动最小必要距离，输入区域与键盘至少保留 16pt。
- 已完成任务明显降噪但仍可回看；首页只内联展示今天已完成的少量记录，本周历史使用原生 Sheet，完整历史进入“已完成”归档页。本周 Sheet 顶部“查看全部”只使用无玻璃外壳的文字按钮，保留至少 `44pt` 命中高度；列表顶部边缘显式使用系统 `.soft`，不得使用 `.hard` 或自定义遮罩替代。
- 已完成归档页优先使用系统 `List`、原生 navigation toolbar、`.searchable` 和分页加载；精确月份 / 日期筛选使用原生 Sheet。

### 7.1 定期任务视图

- 定期任务页不展示独立“总览”大卡片；新建入口复用系统底部 toolbar `+`，空态可保留明确的新建行动。
- 待办与定期属于同级工作模式，使用顶部两个无外壳文字 Button 直接切换；选中态按前述约 `21.5pt Bold` / `17pt Regular` 双层文字语法同步改变字号、颜色与字重，按压反馈只作用于当前按下项，不增加轨道、选中底块、外围玻璃、品牌色填充或第二层容器。切换时保持背景渐变静止，待办层轻微缩小 / 模糊 / 淡出后由维度区和定期任务层接替；两侧当前任务流在外层交接启动约 `70ms` 后，把每个视觉单元接入同一波浪。待办页严格按“未完成任务行 → 今天已完成标题 → 今日完成任务行 → 本周已完成入口”的屏幕顺序参与，退出时严格反向；只有本周完成入口的空态同样作为单个波浪单元。定期页继续以每条任务为波浪单元。所有单元复用详情备注 / 子任务 / 属性当前的三次贝塞尔路径：从右下方约 `(+14pt, +24pt)` 出发，经约 `(+5pt, +9pt)` 的弯曲控制区回到原位，并同步平滑显现。前 8 个单元保持约 `50ms` 错峰，单元运动约 `240ms`；超长列表继续保留独立延迟但将累计等待限制在约 `460ms` 内。日期 / 周期 Header 不参与级联，任务行内部元素不再拆分成第二套动画；Reduce Motion 下取消按钮缩放回弹、页面空间运动与逐行波浪，仅保留 `180ms` 淡变，不使用导航 Push 或全屏遮罩揭幕。
- 维度选择与当前维度摘要固定在任务流上方，不参与任务列表滚动，也不使用胶囊或大背景容器。维度名称与末尾管理入口等分页面宽度，选中态使用 baby blue 底部短线；用户可隐藏任意维度但至少保留一个，隐藏不删除该维度任务。切换每天 / 每周 / 每月等周期时，固定维度区、选中短线和摘要保持稳定，旧周期任务流只短淡出，不再整块左右移动；滚动位置先无动画回到列表顶部，再让新周期中的全部任务行使用与待办 / 定期模式切换相同的逐行贝塞尔波浪入场。Reduce Motion 下周期内容只交叉淡变。
- 当前维度摘要左侧显示周期、未完成数和剩余时间，右侧使用系统圆形 `Gauge` 表达完成数 / 总数；不再使用横跨页面的线性进度条。
- 任务主体使用 `ScrollViewReader + ScrollView + LazyVStack` 连续任务流；标题右侧不展示临期、目标日或进度等属性。标题区与普通任务共享“标题 → 主副标题 → 可选属性摘要”层级：有备注时备注优先、目标节奏位于下一行；无备注时目标节奏作为主副标题，无目标才回退状态。
- 点击任务行后在真实列表位置单向让位，只通过当前行真实高度将后续任务向下推开；固定维度区保持原位。活跃期间首次点击维度、另一任务或其他区域只收起当前详情，不自动执行目标动作。
- 展开区域按“稳定标题区 → 备注 → 维度 / 目标日 / 目标时间 / 提醒属性工具条”组织；紧凑态备注与目标摘要先溶解，展开态备注和属性作为一个组进入。目标时间与待办共用同一个 SwiftUI 系统时间 Popover，完成后只更新当前 draft；收起时统一持久化。
- 周期、目标日和提醒保持系统 `Menu`；触发区不小于 44pt，但不显示跨列矩形高亮。按压只使用短透明度或轻缩放反馈，收起后必须立即恢复；菜单内使用系统 `checkmark` 标识当前选项，清除项放在分隔线下并使用 destructive role。
- 定期任务展开卡与待办任务共享 32pt 紧凑入口行、0pt 详情行距和 44pt 属性栏；卡片高度必须按当前可见行计算，不得用待办任务的子任务空间或固定详情高度补齐。
- 定期任务展开容器不得为了居中插入半屏透明 runway 或其他无命中占位；必要时由外层 `ScrollView` 滚动到详情锚点，首项无法继续向上滚动时保持在列表顶部。容器不得使用超出 ScrollView 顶边的负向上扩展。
- 待办任务与定期任务在展开态完成时，先保存草稿并播放完成反馈，再在原槽位收起详情；容器保持到收起结束，随后才执行列表重排。
- 删除同时保留原生右滑和长按菜单入口；已完成的当前周期任务仍可展开编辑，完成框只负责当前周期完成与恢复。

## 8. 详情与次级面板规则
- 普通任务详情由 SwiftUI 列表拥有容器原地展开；`LazyVStack` 持有唯一任务实体和完整行高。不使用 UIKit Viewport Driver、来源空位、窗口级详情 Overlay 或独立详情 Sheet。
- 已有任务的日期、时间、提醒入口打开同一个日期时间 Sheet；紧急直接切换；子任务编辑必须留在详情卡内。
- 日期时间编辑继续使用无导航栏、无完成按钮的原生 Sheet 容器；内容面不得使用 `Material`、Liquid Glass、半透明叠层或额外描边。浅色模式使用纯白实色，深色模式使用对应的不透明语义表面。Sheet 保留固定六周日历，但 detent 按完整内容紧凑计算，提醒行后只保留必要底部间距和安全区；圆角交由系统 presentation 根据设备与展示形态自适应。待办创建项可使用卡内属性区，定期创建器保持自身的定期属性结构。
- 已有任务的日期、时间与提醒采用即时 draft 更新语义：日期与时间入口不再切换不同初始页面；日期不提供清除；时间未配置时显示“添加时间”，配置后以原生 compact 时间字段触发系统选择器，并由系统控件独占唯一可见胶囊，不在外层重复包裹胶囊背景。时间字段右侧显示唯一的清除图标按钮；清除操作不放入长按菜单且不显示文字，图标按钮保留明确的无障碍名称。添加 / 清除时间时，仅时间 compact 字段与清除图标使用约 `420ms` 的低弹性原生 spring：时间字段以右缘为锚轻微模糊收放，清除图标从时间字段右缘析出并在清除时回融；该效果只表达液态运动感，不增加 Liquid Glass 材质或外层胶囊。时间数字和提醒文案仍使用约 `220ms` 的克制内容转场；Reduce Motion 下直接更新。提醒只在存在具体时间时可用，清除时间同步清除提醒。创建项最终仍由“添加”统一持久化。
- 筛选、排序、分组面板优先使用轻量浮层，不要做复杂弹窗堆叠。

### 8.1 Profile
- 顶部个人资料使用横向布局和 76pt 头像，只展示姓名与进入编辑页的方向提示；不使用无信息量的身份副标题。
- 设置区依靠 32–36pt 组间距、紧凑分组标题和稳定对齐建立层级，不使用白色分组卡片、逐行分割线或大面积装饰图标。
- 任务提醒直接在 Profile 首页调整，不使用独立 sheet。通知和 AlarmKit 对用户呈现为一个“任务提醒”开关，具体投递方式由系统能力自动决定。
- “每日摘要”作为“任务提醒”的条件设置使用一个总开关，行尾固定显示 `09:00 · 18:00`；开启后同时提供早间摘要与晚间总结，不拆成两个开关、时间选择器或独立设置页。早晚含义由通知文案表达，Profile 行保持单层、克制。
- `Menu` 必须锚定在行尾当前值按钮；标题与整行保持静态，避免用户在空白处轻触时意外打开菜单。
- 条件设置仅在父开关开启后出现；Reduce Motion 下只做短淡变，不使用位移或弹性级联。
- “显示”分组先使用系统 `Menu` 提供“外观”选项，支持“跟随系统 / 浅色 / 深色”，默认跟随系统；选择后立即作用于整个 App。随后使用 `sparkles` 图标提供“动态背景”开关；两项偏好均按设备保存，不参与 iCloud 同步，动态背景仅影响待办 / 定期根背景。
- 系统权限被拒绝时使用分组内的简短说明和明确操作按钮，不常驻展示多行权限状态。
- Profile 首页及由其进入的滚动页面在 iOS 26+ 对顶部与底部统一使用系统 `.scrollEdgeEffectStyle(.soft)`；不自定义边缘遮罩或滚动联动效果。

### 8.2 待办履历与任务回顾

- 履历是信息层，不改变首页折叠任务行的密度。普通待办展开详情在现有编辑内容之后仅显示一行“创建至今 + 自然相对时长”。
- 展开详情不显示推迟摘要、事件列表或“更新前未记录”等历史完整性文案；完整指标与时间线只在已完成任务回顾中展示。
- 已完成归档中的任务主体点击进入原生 navigation push 的“任务回顾”；恢复与删除继续使用归档已有 swipe / menu，回顾页正文保持只读。
- 任务回顾使用平铺分组，顺序固定为“结果 → 计划 → 时间线”。结果区突出最终完成耗时，并以次级文字列出正式创建、首次 / 最终完成、推迟与恢复；计划区成对展示首次计划、最后计划和最终计划偏差；时间线展示全部有效事件。
- 回顾页不使用庆祝横幅、英雄数字卡、分数、红绿评价或大面积 Liquid Glass。时间线时刻显示到分钟；只有日期的计划只显示日期，不显示午夜时间。事件多时由系统滚动自然承载，不嵌套第二个 ScrollView。
- Dynamic Type 采用单列重排，不截断指标标签；VoiceOver 每个指标读成完整“名称 + 值”，时间线按时间顺序逐项访问。所有展开和导航入口命中高度不小于 `44pt`。

### 8.3 计划复盘

- Profile“整理”分组新增常驻“计划复盘”行，行尾显示本周完成数量；周五可把次级文案改为“本周可回顾”，但不显示红点、角标、弹窗或主动通知。
- 页面使用原生 navigation push。标题为“计划复盘”，toolbar 中的系统 `Menu` 切换“本周 / 本月”，标题与筛选控件遵循顶部安全区域，不自绘悬浮日期胶囊。
- 内容顺序为：周期摘要、核心指标、可用时的趋势、当前计划风险、值得回看的任务。使用平铺 section 与留白建立层级，不把每一组包进大圆角卡片。
- 核心指标在标准字号下使用两列等宽网格：完成数量、完成耗时中位数、首次计划兑现率、最终计划按时率、推迟任务占比、无计划完成数量。两项按时率可使用小型系统 `Gauge` 辅助，但必须同时显示文字百分比和分母；其他指标使用文字，不引入大型环图或自定义图表。
- 辅助功能字号下核心指标改为单列；数值不得为了维持两列而缩小。趋势只用中性句子，例如“完成耗时中位数比上周多 1 天”，不使用红涨绿跌、箭头奖励或“更高效 / 退步”等结论。
- 当前计划风险按“已逾期、已推迟但仍未完成”顺序展示，每项任务只出现一次。普通任务没有“取消排期 / 长期未排期”状态；历史记录缺失以中性兼容文案表达。风险标题使用语义层级而非高饱和警告底色；点按任务回到首页对应详情，目标不存在时安全回退。
- 无完成样本时显示简短说明并继续展示当前风险；任一比较周期不足五项有效样本时整段隐藏趋势，不展示空图表或低置信度百分比。
- 周 / 月切换使用克制的内容淡变或系统内容转场；Reduce Motion 下直接切换。页面不使用滚动联动大标题、背景粒子或持续动画。

### 8.4 周五快捷推迟

- 待办的原生右滑主操作按操作时本地星期自适应：周五显示“推迟到下周一”，其他日期显示“推迟到明天”。周五长按菜单同时提供两个完整文字命令；右滑不得并列两个推迟按钮。
- 快捷动作沿用现有推迟图标与语义色，不额外增加“周五限定”角标或解释弹窗。执行成功后依照现有列表重排反馈；失败保留任务并显示现有操作错误。
- VoiceOver 标签必须读出完整目标日期语义。跨过午夜、前后台恢复或时区改变后，菜单在展示 / 执行时重新依据当前本地日历计算，不缓存昨日的快捷目标。

## 9. OCR 附件浮层规则
- OCR 入口使用系统底部 toolbar 内的原生 SwiftUI `Menu`，菜单提供“相机 / 照片 / 粘贴文字”系统命令，不自定义按钮或菜单的背景、圆角、阴影、尺寸或 morph 动画。
- 相机和照片使用同一个 SwiftUI `.sheet(item:)` 来源会话。相机默认 `.medium`；照片支持 `.medium/.large` 并默认 `.large`。来源切换、识别中和失败只改变 Sheet 内状态，不创建第二个 presentation controller。
- “粘贴文字”使用默认 `.medium`、支持升级到 `.large` 的原生来源 Sheet；页面使用系统背景、系统 `PasteButton`、简短标题与说明，内部 ScrollView 顶部显式使用系统 `.soft` 边缘效果，底部不额外设置边缘效果；不复制剪贴板按钮外观，不增加自定义材质或装饰性插画。
- App 不得为判断剪贴板内容而预读 `UIPasteboard`。只有用户明确点击系统 `PasteButton` 后才接收文字；空白或不可导入内容在当前来源 Sheet 内提示，不创建空草稿。
- OCR 主路径优先使用 AVFoundation 自定义相机和 inline PhotosPicker；系统 picker 只能作为权限、设备能力或大屏照片选择入口。
- OCR 相机和照片状态是 Sheet 内的沉浸式媒体页：相机预览或照片网格填充内容区，不再包成内部圆角卡片，也不显示额外导航标题；Sheet 圆角、阴影、拖拽和外部安全区完全交给系统。
- 媒体控制按钮优先使用 iOS 26 原生 Liquid Glass 按钮；照片页“全部照片”使用系统 `PhotosPicker`。
- 识别或文字解析成功后来源 Sheet 必须先 dismiss，再在其 `onDismiss` 中由根层 SwiftUI `.sheet(item:)` 展示确认页；禁止同时 present 两个 Sheet，也不使用固定延迟轮询 presentation 状态。
- OCR 确认页使用原生 `.medium` / `.large` detent、系统 grabber、`NavigationStack` 与 toolbar；短内容默认半屏，长内容和键盘输入单向升级为全屏。
- OCR 确认页属于批量文字编辑流程：禁止拖拽直接关闭，取消操作统一处理草稿放弃确认；导入失败必须保留当前输入。
- OCR 草稿只使用统一任务语言：任务标题、备注、子任务、日期、时间、提醒、紧急；不得再出现“项目草稿 / 待办草稿”分区或项目卡片视觉。
- OCR 草稿视觉应对齐首页内联详情：平铺标题与备注、子任务复用 action gutter 与低对比度细实线完成框语言、属性使用浅灰胶囊按钮。
- OCR 与粘贴文字结果必须在用户确认后才写入真实任务；识别中、失败、重试和返回来源保留在浮层流程，确认编辑由原生 sheet 承载。

## 10. 动效规则

### 原则
- 原生
- 克制
- 连续
- 可读
- 稳定 60fps

### 默认方法论
- 只要实现自定义动画，默认采用“状态驱动的分阶段连续动画”。
- 先定义清晰状态，再拆分为 2 到 3 个连续阶段；禁止边做动画边临时拼装状态。
- 动画优先表达“反馈 / 层级 / 连续性”，不要为了装饰牺牲信息可读性。
- 同一交互中的多个变化，优先做同位连续变化，再做列表重排或跨区移动。
- 未完成待办与定期任务删除时，触觉与持久化请求立即开始；成功后整行使用 SwiftUI 原生 `opacity + move(edge: .top)`，以无回弹 `.smooth` 曲线在约 `200ms` 内淡出并由真实列表自然向上收拢，失败时原行保持可见。Reduce Motion 下取消位移，仅以约 `120ms` 淡出。不得处理单独文字或控件，不建立等待阶段、多段删除状态机、全屏效果层、粒子、消融或自定义 shader。

### 优先场景
- 任务完成与状态切换
- 首页列表层级变化
- 主按钮展开 / 收起
- 任务内联详情展开 / 收起
- 筛选 / 排序 / 分组切换
- 未来单人 / 双人模式切换

### 禁止事项
- 禁止廉价、花哨、堆砌式动画。
- 禁止在动画过程中做重排序、重计算、同步 IO。
- 禁止让活跃前景内容失焦、可读性下降或点击区域错位；稳定背景平面可以按焦点景深规范主动降低文字可读性。
- 禁止把自定义动画做成离散跳变、硬切替换或无状态来源的拼接效果。

## 10. Pencil 出图规则
- 绘制 iPhone 页面时必须显式预留顶部和底部安全区域。
- 首页任务流要先表达层级和节奏，再表达装饰。
- 若标注动效，必须写清：
  - 触发条件
  - 起止状态
  - 节奏类型
  - 是否有降级方案

## 11. Open Questions
- 是否需要为动效建立单独的 design token，例如 `duration / spring / elevation transition`。
- ScrollView 任务流失去 `List.onMove` 和原生 swipe 后，排序、删除、推迟是否长期放在 context menu / 内联详情，或单独设计编辑模式。
