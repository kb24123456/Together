# Together Design Guidelines

## 1. 文档职责
- 本文件是项目「一起 / Together」的唯一设计规范主文档。
- 负责：视觉基线、安全区域、页面设计规则、动效规则、Pencil 出图约束。
- 不负责：产品范围、信息架构和页面模块清单；这些以 `PRODUCT_SPEC.md` 为准。

## 2. 结论
- 本仓库所有 UI 设计和实现，默认按 iOS 原生设计规范执行。
- 当前首页改为统一任务区，并作为单人任务首页的设计母版继续深化。
- 动效与交互丝滑度是核心体验组成，不是最后补的装饰层。

## 3. 适用范围
- Pencil 线框图和高保真视觉稿
- SwiftUI 页面实现
- UIKit 容器或桥接页面
- 动效设计、导航转场、悬浮操作按钮

## 4. 全局视觉基线

### 事实
- 当前项目默认视觉基线继续使用 `mobile-02-cleanminimal_light`。
- 现有首页 UI 的干净留白、轻层次、弱阴影、低饱和配色是正确方向。

### 设计结论
- 产品表达应是“效率优先、带一点温度”，不是“情侣感优先”。
- 视觉以中性色为主，珊瑚色承载任务完成、紧急和温暖状态，baby blue 承载轻量操作与定期任务语义。
- 不使用青绿色作为品牌色、全局 tint、选择色或主要操作色。
- 不允许转向厚重拟物、强发光、过度装饰或廉价甜腻风格。

### 色彩职责
- 页面背景、内容表面、标题、正文和弱信息使用 `AppTheme` 的自适应中性色，默认工具栏与普通操作继承主文字或系统语义色。
- 珊瑚色只用于小面积、高识别度的任务状态：完成勾、紧急旗标、今天、选中日期、逾期与短暂完成反馈；不铺满列表行或大面积界面。
- Baby blue 用于保存、推迟、排序完成、定期任务选中短线和进度等轻量操作；浅色模式下不得作为小字号正文颜色或承载仅靠颜色识别的信息。
- 删除、永久移除和不可逆操作使用系统 destructive role 或危险语义色，不复用珊瑚色。
- 状态必须同时使用文字、图标、形状或描边表达，不能只依赖颜色。

## 5. 全局硬规则
1. 顶部内容不得侵入安全区域。
2. 底部内容不得侵入 Home Indicator 与系统手势区域。
3. 滚动内容与固定底栏必须分层。
4. 安全区域按运行时约束处理，不写死机型像素。
5. 自定义玻璃和半透明效果必须服从系统层级与可读性。
6. 英文与数字默认使用 Rounded 风格。
7. 全局字体系必须统一，不允许页面临时拼接字体方案。

## 6. SwiftUI 设计落地规则
1. iOS 17+ 优先使用 `safeAreaPadding`、`safeAreaInset`、`GeometryProxy.safeAreaInsets`。
2. 背景层可全屏延展，主要信息容器必须尊重安全区域。
3. FAB、顶部操作按钮、分段切换、悬浮胶囊按钮都必须保留足够触控热区。
4. 列表卡片和底部工具条的视觉层次要清楚，但不要靠重阴影和大模糊堆出“假高级”。
5. 原生 iOS 组件优先使用 iOS 26 液态玻璃原生能力；需要玻璃质感时，优先 `glassEffect`、`GlassEffectContainer` 和系统 glass button style，不手搓高饱和假玻璃。
6. 当期望视觉与 iOS 原生容器行为冲突时，先明确取舍再落地：例如系统 `toolbar` 可获得原生按钮和标题行为，但会强制应用导航栏布局与控件底座；若需要头像、状态胶囊、标题和按钮完全自由排列，应改为自定义顶部栏，而不是混用两套顶部结构。

## 7. 首页统一任务区专项规则
- 首页使用原生 `NavigationStack` / 顶部 `toolbar` 承载模式和头像操作：顶部中间使用原生分段控件切换“待办 / 定期”，超大动态字体下退化为当前模式 `Menu`；顶部右侧头像进入 Profile。底部改为根层自定义 Dock，左侧为 OCR 原生 `Menu`、右侧为当前模式的新建入口，已完成归档统一由 Profile“日志”进入。
- 首页不再展示自绘顶部日期区、周视图、月历或项目切换入口。
- 主内容使用 `ScrollView` / `LazyVStack` 形成连续任务流。真实任务行和编辑内容继续由 SwiftUI 持有；详情或创建交互会话期间，根控制器或窗口拥有的 UIKit 瞬态呈现 Host 独占统一背景平面、命中屏障、唯一前景任务表面、来源与落点几何以及主视觉进度。前景内容通过 SwiftUI 承载，UIKit 不接管业务状态、持久化或长期页面布局；不使用独立详情 Sheet。
- 自定义底部 Dock 始终挂载并在安全区内保留稳定高度，按钮使用原生 `Button` / `Menu`、SF Symbols 与系统材质或 Liquid Glass，不手搓高饱和假玻璃。待办页 `+` 创建待办，定期页 `+` 创建定期任务，创建器内部不显示类型切换。`+` 只表达创建意图，不承担任务表面的视觉身份，也不参与跨屏 Morph。
- 任务流按日期分组：有截止 / 发生日期的任务进入对应日期组；无截止任务按创建日期分组，并整体排在有日期任务之后。分组标题使用 `LazyVStack(pinnedViews: [.sectionHeaders])` 的单一原生吸附视图，不创建叠加副本或可见容器。
- 任务标题区固定为“标题 → 可选真实备注 → 可选共享属性带”。共享属性带只显示已有子任务进度与已配置的时间、提醒、紧急；未配置属性不占位，不使用状态文案填充副标题。折叠态虚线完成框是唯一直接状态操作，其他行主体统一打开详情。
- 普通任务详情由来源任务行原位抬升；同一潜伏任务表面保持顶部身份区和内容列稳定，边界轻微向外展开、底边向下生长。来源位置保留同高安静空位，周围任务不补位；只有安全区域或键盘即将被侵入时才最小必要上移，长内容由卡片内部滚动承接。展开和收起从当前可见形态与速度反向，不强制屏幕居中。
- 潜伏任务表面在紧凑态与列表融合，不显示常驻卡片外壳；抬升过程中才显出自适应连续圆角和克制阴影。备注、子任务和属性栏通过同一边界裁切揭示；标题、完成框和身份内容列保持同一锚点。日期与时间可使用原生编辑 Sheet，入口决定首次页面，时间保持 5 分钟步进，清除时间同步清除提醒。
- 详情关闭前先保存当前 draft 并刷新紧凑行摘要；保存失败保持当前详情。排序位置未变时严格反向返回来源；日期、紧急或周期改变时，背景在景深状态下准备最终 section/index，表面直接以同一 `1 -> 0` 语法落入新位置。点击另一任务只关闭当前详情，必须再次点击才能打开目标，旧保存或动画完成回调不得改变新会话。
- 待办详情、定期详情、待办创建和定期创建共用一个根级焦点会话；任意时刻只允许一张前景任务表面。点击活跃表面外任意位置只放弃创建项或保存并收起详情，背景命中不得穿透；当前会话存在时其他任务停止普通命中。
- 待办和定期页点按 `+` 时立即分配最终任务 UUID。创建表面直接在稳定、键盘安全的焦点位建立，不要求从按钮形变而来；编辑期间背景不显示创建项或最终空位，草稿属性变化不移动卡片。提交时才建立真实排序落点，本地持久化成功后表面使用与详情关闭相同的 `1 -> 0` 语法收束并融入最终行；失败保留 UUID、全部草稿、焦点位置和错误状态。
- 创建项使用与详情一致的实体表面、连续圆角和轻阴影。待办创建包含标题、备注、子任务及属性入口；定期创建只包含定期字段。标题为空时禁用“添加”；“放弃”不确认、不持久化，在当前稳定焦点位短促收束消失。
- 创建类型完全由页面决定。模板界面、模板数据、模板仓库和“存为模板”入口均不存在；任何创建界面不得出现“模板 / 待办 / 定期”切换。
- 定期创建只显示定期字段，但与待办创建和两类详情共用同一个根 Host、背景景深、会话仲裁和运动参数，不保留独立浮层或局部状态机。
- 创建项或详情卡活跃时，前景任务表面始终清晰、近乎不透明；应用导航栏、顶部控件、非活跃任务、分组标题和自定义底部 Dock 组成一个稳定背景平面，同步缩小、模糊并降低明度。背景布局与主要轮廓仍可辨认，文字不再适合持续阅读；系统状态栏和键盘保持实时。自定义 Dock 保留固定安全区、布局和命中位置，背景停止滚动和普通操作。
- 创建路径为“按钮即时按压反馈 → 在稳定焦点位建立创建表面 → 表面稳定后聚焦标题并请求键盘”。提交成功后列表在真实落点接纳同一身份，落点不可见时只在提交后进行一次连续定位。详情展开为 `0 -> 1`，详情关闭和创建落地为统一的 `1 -> 0`；几何、边界、阴影、内容揭示和背景景深服从同一视觉进度，实体表面全程不透明且没有明显过冲或多次回弹。
- 根级全屏底板延伸到状态栏与 Home Indicator 后方；Toolbar、头像、模式切换、列表和 Dock 始终保持同一视图树、布局和安全区占位。焦点只改变背景 Host 的缩放、单层模糊和自适应暗化，不替换或隐藏这些结构。
- 启用“降低透明度”时不使用动态模糊，改用更实暗化与清晰实体边界；启用 Reduce Motion 时详情只做原位短高度变化，创建卡直接在安全位置建立，背景只暗化，不缩放、不动态模糊，提交只保留短促列表让位与清晰度转换。
- 卡片与属性控件必须遵循安全区域、Dynamic Type、VoiceOver Escape 和至少 44pt 命中区。键盘出现时只滚动最小必要距离，输入区域与键盘至少保留 16pt。
- 已完成任务明显降噪但仍可回看；首页只内联展示今天已完成的少量记录，本周历史使用原生 Sheet，完整历史进入“已完成”归档页。
- 已完成归档页优先使用系统 `List`、原生 navigation toolbar、`.searchable` 和分页加载；精确月份 / 日期筛选使用原生 Sheet。

### 7.1 定期任务视图

- 定期任务页不展示独立“总览”大卡片；新建入口复用自定义底部 Dock `+`，空态可保留明确的新建行动。
- 待办与定期属于同级工作模式，使用顶部原生分段控件直接切换。控件保持紧凑稳定的双段胶囊宽度，轨道、选中块和文字使用系统自适应的中性色层级，不使用品牌色填充；切换时保持网格画布静止，待办层轻微缩小 / 模糊 / 淡出后由维度区和定期任务层从下方去模糊接替，前五行使用 20ms 轻级联；Reduce Motion 下仅保留 180ms 淡变，不使用导航 Push 或全屏遮罩揭幕。
- 维度选择与当前维度摘要固定在任务流上方，不参与任务列表滚动，也不使用胶囊或大背景容器。维度名称与末尾管理入口等分页面宽度，选中态使用 baby blue 底部短线；用户可隐藏任意维度但至少保留一个，隐藏不删除该维度任务。
- 当前维度摘要左侧显示周期、未完成数和剩余时间，右侧使用系统圆形 `Gauge` 表达完成数 / 总数；不再使用横跨页面的线性进度条。
- 任务主体使用 `ScrollViewReader + ScrollView + LazyVStack` 连续任务流；标题右侧不展示临期、目标日或进度等属性。标题区与普通任务共享“标题 → 主副标题 → 可选属性摘要”层级：有备注时备注优先、目标节奏位于下一行；无备注时目标节奏作为主副标题，无目标才回退状态。
- 点击任务行后在原位置展开，复用首页的大圆角白色焦点卡、下方阴影、背景弱化、稳定高度和键盘避让语义；展开时固定维度区保持原位并随背景一起模糊、降噪和停止切换，点击聚焦卡外的任务区或空白区只收起当前卡片。
- 展开卡按“稳定标题区 → 维度 / 目标日 / 目标时间 / 提醒属性工具条”组织；标题与备注在标题区原位编辑。目标时间与待办共用同一个 SwiftUI 系统时间 Popover，由系统负责箭头、定位、展开方向和动画，并复用临时选择及清除 / 完成语义；完成后只更新当前 draft。定期任务提醒支持目标时刻及提前量，默认使用本地通知；iOS 26+ 可明确选择 AlarmKit 闹钟，旧系统降级为通知。目标日与目标时间可独立为空，收起时统一持久化。
- 周期、目标日和提醒保持系统 `Menu`；触发区不小于 44pt，但不显示跨列矩形高亮。按压只使用短透明度或轻缩放反馈，收起后必须立即恢复；菜单内使用系统 `checkmark` 标识当前选项，清除项放在分隔线下并使用 destructive role。
- 定期任务展开卡与待办任务共享 32pt 紧凑入口行、2pt 详情行距和 44pt 属性栏；卡片高度必须按当前可见行计算，不得用待办任务的子任务空间或固定详情高度补齐。
- 定期任务聚焦卡不得为了居中插入半屏透明 runway 或其他无命中占位；展开后直接滚动到详情锚点，首项无法继续向上滚动时保持在列表顶部。焦点白卡不得使用超出 ScrollView 顶边的负向上扩展。
- 待办任务与定期任务在展开态完成时，先保存草稿并播放完成反馈，再收起详情；焦点白卡保持到收起结束，随后恢复页面背景并执行列表重排。
- 删除仅放在长按菜单；已完成的当前周期任务仍可展开编辑，完成框只负责当前周期完成与恢复。

## 8. 详情与次级面板规则
- 普通任务详情由 UIKit 瞬态呈现 Host 中的唯一前景任务表面从真实来源行原位抬升，SwiftUI 时间线只保留无身份副本的等高来源空位；不使用独立详情 Sheet，UIKit Host 也不得成为长期页面或业务状态所有者。
- 日期、时间、提醒等单项设置可以使用原生 sheet；紧急直接切换；子任务编辑必须留在详情卡内。
- 日期时间编辑继续使用无导航栏的原生 Sheet；待办创建项可使用卡内属性区，定期创建器保持自身的定期属性结构。
- 已有任务的日期时间编辑采用即时 draft 更新语义：日期与时间入口决定 Sheet 首次页面；日期不提供清除，时间页保留“清除时间”，并同步清除提醒。创建项最终仍由“添加”统一持久化。
- 筛选、排序、分组面板优先使用轻量浮层，不要做复杂弹窗堆叠。

### 8.1 Profile
- 顶部个人资料使用横向布局和 76pt 头像，只展示姓名与进入编辑页的方向提示；不使用无信息量的身份副标题。
- 设置区依靠 32–36pt 组间距、紧凑分组标题和稳定对齐建立层级，不使用白色分组卡片、逐行分割线或大面积装饰图标。
- 任务提醒直接在 Profile 首页调整，不使用独立 sheet。通知和 AlarmKit 对用户呈现为一个“任务提醒”开关，具体投递方式由系统能力自动决定。
- `Menu` 必须锚定在行尾当前值按钮；标题与整行保持静态，避免用户在空白处轻触时意外打开菜单。
- 条件设置仅在父开关开启后出现；Reduce Motion 下只做短淡变，不使用位移或弹性级联。
- 系统权限被拒绝时使用分组内的简短说明和明确操作按钮，不常驻展示多行权限状态。

## 9. OCR 附件浮层规则
- OCR 入口使用自定义底部 Dock 内的原生 SwiftUI `Menu`，菜单仅提供“相机 / 照片”系统命令，不自定义菜单背景、圆角、阴影、尺寸或 morph 动画。
- 相机和照片使用同一个 SwiftUI `.sheet(item:)` 来源会话。相机默认 `.medium`；照片支持 `.medium/.large` 并默认 `.large`。来源切换、识别中和失败只改变 Sheet 内状态，不创建第二个 presentation controller。
- OCR 主路径优先使用 AVFoundation 自定义相机和 inline PhotosPicker；系统 picker 只能作为权限、设备能力或大屏照片选择入口。
- OCR 相机和照片状态是 Sheet 内的沉浸式媒体页：相机预览或照片网格填充内容区，不再包成内部圆角卡片，也不显示额外导航标题；Sheet 圆角、阴影、拖拽和外部安全区完全交给系统。
- 媒体控制按钮优先使用 iOS 26 原生 Liquid Glass 按钮；照片页“全部照片”使用系统 `PhotosPicker`。
- 识别成功后来源 Sheet 必须先 dismiss，再在其 `onDismiss` 中由根层 SwiftUI `.sheet(item:)` 展示确认页；禁止同时 present 两个 Sheet，也不使用固定延迟轮询 presentation 状态。
- OCR 确认页使用原生 `.medium` / `.large` detent、系统 grabber、`NavigationStack` 与 toolbar；短内容默认半屏，长内容和键盘输入单向升级为全屏。
- OCR 确认页属于批量文字编辑流程：禁止拖拽直接关闭，取消操作统一处理草稿放弃确认；导入失败必须保留当前输入。
- OCR 草稿只使用统一任务语言：任务标题、备注、子任务、日期、时间、提醒、紧急；不得再出现“项目草稿 / 待办草稿”分区或项目卡片视觉。
- OCR 草稿视觉应对齐首页内联详情：平铺标题与备注、子任务复用 action gutter 与虚线框语言、属性使用浅灰胶囊按钮。
- OCR 结果必须在用户确认后才写入真实任务；识别中、失败、重试和返回来源保留在浮层流程，确认编辑由原生 sheet 承载。

## 10. 动效规则

### 原则
- 原生
- 克制
- 连续
- 可读
- 稳定 60fps

### 默认方法论
- 只要实现自定义动画，默认采用“状态驱动的分阶段连续动画”。
- 先定义清晰状态，再拆分为 2 到 3 个连续阶段；禁止边做动画边临时拼装状态。
- 动画优先表达“反馈 / 层级 / 连续性”，不要为了装饰牺牲信息可读性。
- 同一交互中的多个变化，优先做同位连续变化，再做列表重排或跨区移动。

### 优先场景
- 任务完成与状态切换
- 首页列表层级变化
- 主按钮展开 / 收起
- 任务内联详情展开 / 收起
- 筛选 / 排序 / 分组切换
- 未来单人 / 双人模式切换

### 禁止事项
- 禁止廉价、花哨、堆砌式动画。
- 禁止在动画过程中做重排序、重计算、同步 IO。
- 禁止让活跃前景内容失焦、可读性下降或点击区域错位；稳定背景平面可以按焦点景深规范主动降低文字可读性。
- 禁止把自定义动画做成离散跳变、硬切替换或无状态来源的拼接效果。

## 10. Pencil 出图规则
- 绘制 iPhone 页面时必须显式预留顶部和底部安全区域。
- 首页任务流要先表达层级和节奏，再表达装饰。
- 若标注动效，必须写清：
  - 触发条件
  - 起止状态
  - 节奏类型
  - 是否有降级方案

## 11. Open Questions
- 是否需要为动效建立单独的 design token，例如 `duration / spring / elevation transition`。
- ScrollView 任务流失去 `List.onMove` 和原生 swipe 后，排序、删除、推迟是否长期放在 context menu / 内联详情，或单独设计编辑模式。
